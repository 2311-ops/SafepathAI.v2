---
phase: 01.1-animated-logo-splash-screen
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - mobile/test/features/splash/splash_screen_test.dart
  - mobile/test/features/splash/splash_redirect_gate_test.dart
autonomous: false
requirements: []
must_haves:
  truths:
    - "A deterministic widget test proves the splash animation runs once on mount, the controller is never re-forwarded, and splashAnimationCompleteProvider flips false->true exactly once"
    - "A deterministic test proves reduced motion (disableAnimations true) still fully shows the lockup and flips the same completion gate within ~250ms"
    - "A deterministic test proves the AnimationController is disposed with no 'used after dispose' errors"
    - "A deterministic router test proves the app holds on /splash for the full minimum duration, then routes authenticated -> /home and unauthenticated -> / and recovery -> /reset-password"
    - "The full existing flutter test suite and flutter analyze pass with zero regressions"
    - "A human confirms the splash reads as calm (no flashing/bounce), the gradient is continuous into Welcome, and it plays exactly once on a real cold launch"
  artifacts:
    - mobile/test/features/splash/splash_screen_test.dart
    - mobile/test/features/splash/splash_redirect_gate_test.dart
  key_links:
    - "Redirect gate test uses the REAL routerProvider (via SafePathApp or UncontrolledProviderScope + routerProvider) with authApiProvider overridden by FakeAuthApi — proving the refreshListenable wiring, not a stand-in router"
    - "pumpAndSettle at boot must transit /splash to the destination; any pre-existing test that breaks solely on the initialLocation change is fixed by using pumpAndSettle at boot, never by weakening the splash"
---

<objective>
Prove the splash behaves exactly as the UI-SPEC Testing Contract requires — deterministically, with no network — and confirm zero regressions against the existing auth/routing suite, then get a human visual sign-off on the motion feel.

Purpose: The splash gates access to the SOS-critical app shell; its exactly-once navigation, completion gate, reduced-motion path, and disposal must be provably correct, and the initialLocation swap to /splash must not regress any existing router test.
Output: Two deterministic test files plus a full-suite regression pass and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-animated-logo-splash-screen/01.1-UI-SPEC.md
@mobile/lib/features/splash/presentation/splash_screen.dart
@mobile/lib/features/splash/application/splash_providers.dart
@mobile/lib/core/router/app_router.dart
@mobile/test/core/router/auth_flow_navigation_test.dart
@mobile/test/helpers/fake_auth_api.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic SplashScreen widget tests</name>
  <files>mobile/test/features/splash/splash_screen_test.dart</files>
  <read_first>
    - .planning/phases/01.1-animated-logo-splash-screen/01.1-UI-SPEC.md (Testing Contract — the coverage list is binding)
    - mobile/lib/features/splash/presentation/splash_screen.dart (the widget under test)
    - mobile/lib/features/splash/application/splash_providers.dart (splashAnimationCompleteProvider)
    - mobile/test/core/router/auth_flow_navigation_test.dart (test conventions: setUpAll sets GoogleFonts.config.allowRuntimeFetching = false; pump/pumpAndSettle patterns)
    - mobile/test/helpers/fake_auth_api.dart (not required by these isolated widget tests but shows the fake pattern)
  </read_first>
  <action>
    Create splash_screen_test.dart. In setUpAll set GoogleFonts.config.allowRuntimeFetching = false (matches every other test). Build the widget in isolation with a ProviderContainer wrapped in UncontrolledProviderScope + MaterialApp(home: SplashScreen()) so the container can be read directly. Write these deterministic tests (no network, no real timers beyond the WidgetTester clock):
    - "content renders": after one pump, find SafePathLogo (one) and the 'SafePath AI' Text (one).
    - "animation runs once and flips the gate exactly once": pump one frame, assert container.read(splashAnimationCompleteProvider) is false; advance with tester.pump(Duration(milliseconds: 1400)); assert it is now true. Register a listener via container.listen(splashAnimationCompleteProvider, ...) that counts transitions to true and assert the count is exactly 1 after pumping a further 1000ms (proves no re-forward / no double-flip).
    - "reduced motion path": wrap the MaterialApp subtree in a MediaQuery with MediaQueryData(disableAnimations: true); pump one frame then tester.pump(Duration(milliseconds: 250)); assert the 'SafePath AI' Text is present (full lockup shown, never blank) AND container.read(splashAnimationCompleteProvider) is true (short fade completes and gates the same way).
    - "disposes cleanly": pump the SplashScreen, then pump a replacement widget (e.g. MaterialApp(home: SizedBox()) in the same UncontrolledProviderScope) to trigger dispose; call await tester.pumpAndSettle() and assert tester.takeException() is null (no 'used after being disposed' AnimationController error, no leaked ticker).
    Use tester.pump(Duration) for deterministic time advancement — do NOT rely on wall-clock delays.
  </action>
  <verify>
    <automated>cd mobile && flutter test test/features/splash/splash_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/features/splash/splash_screen_test.dart contains `splashAnimationCompleteProvider`, `UncontrolledProviderScope`, `disableAnimations: true`, and `takeException`
    - The test asserts the gate is `false` before completion and `true` after `Duration(milliseconds: 1400)`
    - The test asserts exactly one transition to `true` (no double-flip / no re-forward)
    - `cd mobile && flutter test test/features/splash/splash_screen_test.dart` exits 0
  </acceptance_criteria>
  <done>The widget test suite proves: content renders, the gate flips exactly once at completion, the reduced-motion path shows the full lockup and flips within ~250ms, and disposal leaves no exceptions.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic redirect gate tests through the real router</name>
  <files>mobile/test/features/splash/splash_redirect_gate_test.dart</files>
  <read_first>
    - mobile/lib/core/router/app_router.dart (the routerProvider, initialLocation '/splash', the /splash gate, the _AuthRefreshListenable wiring — this is what the test proves works)
    - mobile/test/core/router/auth_flow_navigation_test.dart (the canonical pattern for booting the real router: ProviderScope overrides authApiProvider/familyApiProvider/profileApiProvider with fakes, SafePathApp, _fakeSession() helper, emitPasswordRecovery/emitSignedIn)
    - mobile/test/helpers/fake_auth_api.dart (FakeAuthApi: initialSession, emitPasswordRecovery(); a null initialSession -> unauthenticated, a set session -> authenticated)
    - mobile/lib/features/auth/presentation/welcome_screen.dart (Welcome shows 'Create your circle' — the discriminator between splash and Welcome, since both show the 'SafePath AI' wordmark)
  </read_first>
  <action>
    Create splash_redirect_gate_test.dart booting the REAL routerProvider (reuse the buildApp() shape from auth_flow_navigation_test: ProviderScope overriding authApiProvider — and familyApiProvider/profileApiProvider if SafePathApp needs them — then SafePathApp). setUpAll sets GoogleFonts.config.allowRuntimeFetching = false. Write these deterministic tests:
    - "holds on /splash for the minimum duration then routes unauthenticated to Welcome": FakeAuthApi with null initialSession. pumpWidget; after one pump assert find.byType(SplashScreen) findsOneWidget AND find.text('Create your circle') findsNothing (proving we are on splash, not Welcome). Advance tester.pump(Duration(milliseconds: 700)) and assert find.byType(SplashScreen) is still findsOneWidget (the gate holds the full minimum duration). Then await tester.pumpAndSettle() and assert find.byType(SplashScreen) findsNothing AND find.text('Create your circle') findsOneWidget (landed on Welcome).
    - "routes authenticated to Home": FakeAuthApi with initialSession set to a locally-built _fakeSession(); pumpWidget then await tester.pumpAndSettle(); assert find.byType(SplashScreen) findsNothing AND find.text('Your circle') findsOneWidget (the LandingStub home content).
    - "recovery routes to reset-password": FakeAuthApi null initialSession; pumpWidget then fakeApi.emitPasswordRecovery(); await tester.pumpAndSettle(); assert the reset-password screen is shown (find its known heading/content) and find.byType(SplashScreen) findsNothing.
    - "existing routing unchanged after splash": in the unauthenticated case, after settling on Welcome, tap 'I already have an account' (or 'Create your circle') and pumpAndSettle, asserting the existing destination appears — proving the existing redirect rules still work once /splash is left.
    Use the _fakeSession() Supabase.Session construction pattern from auth_flow_navigation_test verbatim (locally built, never networked).
  </action>
  <verify>
    <automated>cd mobile && flutter test test/features/splash/splash_redirect_gate_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/features/splash/splash_redirect_gate_test.dart contains `find.byType(SplashScreen)`, `initialSession`, and `emitPasswordRecovery`
    - The test asserts SplashScreen is present after boot and absent after pumpAndSettle
    - The test asserts unauthenticated -> 'Create your circle' (Welcome) and authenticated -> 'Your circle' (Home)
    - The test boots the real `routerProvider`/`SafePathApp` (not a test-only GoRouter with a hand-written redirect)
    - `cd mobile && flutter test test/features/splash/splash_redirect_gate_test.dart` exits 0
  </acceptance_criteria>
  <done>The gate tests prove, through the real router, that the app holds on /splash for the minimum duration and then leaves it to the correct existing destination for authenticated, unauthenticated, and recovery states — confirming the refreshListenable re-evaluation wiring works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate (flutter analyze + flutter test)</name>
  <files>mobile/test/features/splash/splash_screen_test.dart, mobile/test/features/splash/splash_redirect_gate_test.dart</files>
  <read_first>
    - mobile/test/core/router/auth_flow_navigation_test.dart (boots the real router at initialLocation, uses pumpAndSettle at boot — should transit /splash transparently)
    - mobile/test/core/router/pending_invite_redirect_test.dart (boots the real routerProvider; pumpAndSettle at boot)
    - mobile/test/widget_test.dart (boots SafePathApp; asserts 'SafePath AI' after pumpAndSettle — Welcome wordmark, still present post-splash)
  </read_first>
  <action>
    Run `flutter analyze` and the full `flutter test` suite from the mobile/ directory and confirm zero regressions. The initialLocation swap to /splash means every test that boots the real routerProvider/SafePathApp now transits the splash before reaching its destination; tests that use pumpAndSettle() at boot (auth_flow_navigation_test, pending_invite_redirect_test, widget_test) advance through the 1400ms animation transparently and should stay green. If any pre-existing test fails SOLELY because it asserted on the boot frame before the destination settled, fix that test by ensuring it awaits pumpAndSettle() at boot (so it transits the splash) — do NOT weaken the splash, shorten its duration below the 1200-1600ms window, remove the gate, or change initialLocation to work around a test. reset_password_screen_test uses a test-only router (initialLocation '/reset-password') and is unaffected. Report the final analyze + test counts in the SUMMARY.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `cd mobile && flutter analyze` exits 0
    - `cd mobile && flutter test` exits 0 (entire suite green, including the two new splash test files and all pre-existing auth/router tests)
    - No pre-existing test was modified except (if strictly necessary) to await pumpAndSettle() at boot; the splash duration, gate, and initialLocation are unchanged from plan 01.1-01
  </acceptance_criteria>
  <done>flutter analyze and the full flutter test suite pass with zero regressions; the splash implementation was not weakened to satisfy any test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual sign-off on cold-launch splash motion</name>
  <action>Pause execution and hand off to a human for a visual review of the running app. Do not auto-approve — the motion feel (calmness, no flashing, gradient continuity, plays-once) cannot be judged by automated tests. Present the what-built and how-to-verify steps below and wait for the resume-signal before marking the phase complete.</action>
  <what-built>An animated logo splash on cold launch: the WelcomeScreen hero gradient shows immediately, then the SafePath logo + 'SafePath AI' wordmark fade+scale+rise in over ~1.4s (opacity-only ~0.22s under system reduced-motion), then the app hands off to Home (if signed in) or Welcome (if not). Navigation is gated via the existing router redirect on splashAnimationCompleteProvider; /splash is never on the back stack.</what-built>
  <how-to-verify>
    1. Run the app on an emulator/device: `cd mobile && flutter run`.
    2. Cold launch (fully killed app) while SIGNED OUT: confirm the splash appears instantly with the teal gradient (no white/black flash at frame 0), the logo + 'SafePath AI' animate in calmly (no bounce, no flashing, no pulsing glow), it plays once, then hands off to the Welcome screen with a continuous gradient (no jarring color jump).
    3. Cold launch while SIGNED IN: confirm the same splash plays fully, then lands on Home ('Your circle').
    4. Enable OS "Reduce motion" / "Remove animations", cold launch again: confirm a short opacity-only fade (no scale/rise), full lockup shown, same handoff.
    5. Press back immediately after reaching Welcome/Home: confirm you never return to the splash.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any motion/timing/flash/continuity issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `cd mobile && flutter analyze` exits 0.
- `cd mobile && flutter test` exits 0 with both new splash test files present and all pre-existing tests green.
- Human confirms the cold-launch motion is calm, non-flashing, plays once, and hands off with gradient continuity.
</verification>

<success_criteria>
- Deterministic tests cover: animation-once, exactly-once gate flip, reduced motion, disposal, and the authenticated/unauthenticated/recovery redirect outcomes through the real router.
- Zero regressions: full flutter test suite + flutter analyze pass without weakening the splash.
- Human visual sign-off obtained.
</success_criteria>

## Artifacts this phase produces
- `mobile/test/features/splash/splash_screen_test.dart` — new deterministic widget test file (animation-once, gate-flip-once, reduced motion, disposal, content).
- `mobile/test/features/splash/splash_redirect_gate_test.dart` — new deterministic router gate test file (min-duration hold, authenticated -> /home, unauthenticated -> /, recovery -> /reset-password, existing routing preserved).

<output>
Create `.planning/phases/01.1-animated-logo-splash-screen/01.1-02-SUMMARY.md` when done.
</output>
