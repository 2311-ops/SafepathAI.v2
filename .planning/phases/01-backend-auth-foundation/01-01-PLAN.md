---
phase: 01-backend-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/SafePath.sln
  - backend/src/SafePath.Domain/SafePath.Domain.csproj
  - backend/src/SafePath.Domain/Entities/User.cs
  - backend/src/SafePath.Domain/Entities/RefreshToken.cs
  - backend/src/SafePath.Domain/Enums/Role.cs
  - backend/src/SafePath.Domain/Enums/PermissionLevel.cs
  - backend/src/SafePath.Domain/Enums/InvitationStatus.cs
  - backend/src/SafePath.Application/SafePath.Application.csproj
  - backend/src/SafePath.Application/Common/Interfaces/IApplicationDbContext.cs
  - backend/src/SafePath.Application/Common/Interfaces/IPasswordHasher.cs
  - backend/src/SafePath.Application/Common/Interfaces/IJwtTokenGenerator.cs
  - backend/src/SafePath.Application/Common/Interfaces/ICurrentUserService.cs
  - backend/src/SafePath.Application/Common/Models/AuthResult.cs
  - backend/src/SafePath.Application/Auth/RegisterCommand.cs
  - backend/src/SafePath.Application/Auth/LoginCommand.cs
  - backend/src/SafePath.Application/Auth/RefreshTokenCommand.cs
  - backend/src/SafePath.Application/Auth/LogoutCommand.cs
  - backend/src/SafePath.Application/DependencyInjection.cs
  - backend/src/SafePath.Infrastructure/SafePath.Infrastructure.csproj
  - backend/src/SafePath.Infrastructure/Persistence/ApplicationDbContext.cs
  - backend/src/SafePath.Infrastructure/Persistence/EntityConfigurations/UserConfiguration.cs
  - backend/src/SafePath.Infrastructure/Persistence/EntityConfigurations/RefreshTokenConfiguration.cs
  - backend/src/SafePath.Infrastructure/Persistence/Migrations/
  - backend/src/SafePath.Infrastructure/Identity/BCryptPasswordHasher.cs
  - backend/src/SafePath.Infrastructure/Identity/JwtTokenGenerator.cs
  - backend/src/SafePath.Infrastructure/Identity/CurrentUserService.cs
  - backend/src/SafePath.Infrastructure/DependencyInjection.cs
  - backend/src/SafePath.Api/SafePath.Api.csproj
  - backend/src/SafePath.Api/Controllers/AuthController.cs
  - backend/src/SafePath.Api/Program.cs
  - backend/src/SafePath.Api/appsettings.json
  - backend/tests/SafePath.Domain.Tests/SafePath.Domain.Tests.csproj
  - backend/tests/SafePath.Application.Tests/SafePath.Application.Tests.csproj
  - backend/tests/SafePath.Application.Tests/Auth/RegisterCommandTests.cs
  - backend/tests/SafePath.Application.Tests/Auth/LoginCommandTests.cs
  - backend/tests/SafePath.Application.Tests/Auth/RefreshTokenCommandTests.cs
  - backend/tests/SafePath.Application.Tests/Auth/LogoutCommandTests.cs
  - backend/tests/SafePath.Api.IntegrationTests/SafePath.Api.IntegrationTests.csproj
  - backend/tests/SafePath.Api.IntegrationTests/AuthEndpointsTests.cs
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-05]
user_setup:
  - service: supabase
    why: "Managed Postgres database the backend connects to via Npgsql/EF Core (project has no DB provisioned yet)"
    env_vars:
      - name: ConnectionStrings__DefaultConnection
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string (Direct connection, port 5432, URI form) — store via `dotnet user-secrets`, never commit"
    dashboard_config:
      - task: "Create a new Supabase project (region closest to you), capture the direct connection string and database password"
        location: "Supabase Dashboard -> New project"

must_haves:
  truths:
    - "A new user can POST /auth/register with email+password+role and gets back an access token + refresh token, and a hashed User row is persisted in Postgres"
    - "A registered user can POST /auth/login with correct credentials and receives an access+refresh JWT pair; wrong credentials return 401 without revealing which field was wrong"
    - "POST /auth/refresh rotates the refresh token single-use; presenting a revoked token again revokes every active refresh token for that user"
    - "POST /auth/logout revokes the caller's refresh token so a later refresh with it fails"
    - "The assigned Role (Guardian/Member/Caregiver/OrgAdmin) is persisted and emitted as a role claim in the access token"
  artifacts:
    - path: "backend/src/SafePath.Domain/Entities/User.cs"
      provides: "Plain POCO User entity (Id, Email, PasswordHash, FullName, Role) — no framework base class"
      min_lines: 12
    - path: "backend/src/SafePath.Infrastructure/Identity/JwtTokenGenerator.cs"
      provides: "HMAC-SHA256 access-token issuance + CSPRNG 64-byte refresh-token generation"
      contains: "SecurityAlgorithms.HmacSha256"
    - path: "backend/src/SafePath.Infrastructure/Identity/BCryptPasswordHasher.cs"
      provides: "BCrypt password hashing at work factor 12"
      contains: "workFactor: 12"
    - path: "backend/src/SafePath.Application/Auth/RefreshTokenCommand.cs"
      provides: "Refresh-token rotation with reuse detection"
    - path: "backend/src/SafePath.Infrastructure/Persistence/ApplicationDbContext.cs"
      provides: "EF Core context with Users + RefreshTokens DbSets, implements IApplicationDbContext"
      contains: "DbSet<User>"
  key_links:
    - from: "backend/src/SafePath.Api/Controllers/AuthController.cs"
      to: "backend/src/SafePath.Application/Auth/RegisterCommand.cs"
      via: "controller dispatches command to handler (Mediator or direct handler injection)"
      pattern: "RegisterCommand"
    - from: "backend/src/SafePath.Application/Auth/RegisterCommand.cs"
      to: "backend/src/SafePath.Infrastructure/Persistence/ApplicationDbContext.cs"
      via: "handler writes User via IApplicationDbContext.Users"
      pattern: "Users\\.Add"
    - from: "backend/src/SafePath.Api/Program.cs"
      to: "backend/src/SafePath.Infrastructure/Identity/JwtTokenGenerator.cs"
      via: "AddInfrastructure DI registration + JwtBearer signing key from config"
      pattern: "AddInfrastructure"
---

<objective>
Stand up the ASP.NET Core Clean Architecture backend solution (Domain / Application / Infrastructure / Api + xUnit test projects) and deliver the core authentication slice end-to-end against a real Supabase Postgres database: register (with role), login, refresh-token rotation with reuse detection, and logout. This is the backend half of the Walking Skeleton — the first real DB read/write plus the API shell every later phase builds on.

Purpose: AUTH-01 (register), AUTH-02 (JWT access+refresh, stay logged in), AUTH-03 (logout revokes refresh), AUTH-05 (role assigned during setup) — the non-negotiable auth foundation. Uses custom identity (not Microsoft.AspNetCore.Identity) per locked decision 6, and .NET 9 (not .NET 10) per locked decision 4.
Output: A buildable `backend/SafePath.sln` with 4 source projects + 3 test projects, an initial EF Core migration applied to Supabase, and 4 working `/auth/*` endpoints with green unit + integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-auth-foundation/01-RESEARCH.md
@.planning/phases/01-backend-auth-foundation/01-PATTERNS.md
@.planning/phases/01-backend-auth-foundation/01-VALIDATION.md
</context>

<locked_decisions_applied>
- D4: Target **.NET 9** (existing SDKs 9.0.201 / 9.0.205 are sufficient — NO .NET 10 install task). Pin `Microsoft.EntityFrameworkCore`, `Microsoft.EntityFrameworkCore.Design`, `Npgsql.EntityFrameworkCore.PostgreSQL`, and `Microsoft.AspNetCore.Authentication.JwtBearer` to their **9.0.x** lines (NOT RESEARCH.md's 10.x pins). Verify the mutually-compatible triple (EF Core 9 + Npgsql provider 9 + .NET 9) at install with `dotnet add package ... --version 9.*`.
- D6: Custom `User` POCO + fixed `Role` enum + `IIdentityService`-style abstractions — NOT `Microsoft.AspNetCore.Identity`.
  **SUPERSEDED (2026-07-08):** The backend now validates Supabase-issued JWTs directly (`Program.cs` bearer auth against `Supabase:Url`/`Audience`) instead of issuing its own HS256 tokens. `AuthController.cs` was removed and replaced by a read-only `MeController.cs`. The custom `LoginCommand`/`LogoutCommand`/`RegisterCommand`/`RefreshTokenCommand` handlers, `BCryptPasswordHasher`/`JwtTokenGenerator`, and their interfaces/tests built for this decision have been deleted as orphaned code (see 01-03-SUMMARY.md addendum). `User`/`Role` POCOs remain in use; the custom identity/token-issuance machinery does not.
- D5: All family/role authorization is backend Application-layer (no Postgres RLS as primary). No `CREATE POLICY` SQL in this phase.
- Mediator choice (Claude's discretion, RESEARCH Supporting table): use `Mediator.SourceGenerator` 3.0.2 (MIT, no license key) OR a hand-rolled typed `ICommandHandler<TCommand,TResult>` + DI. Do NOT use MediatR (commercial license key required for v13+ per RESEARCH Pitfall 2).
</locked_decisions_applied>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold xUnit test projects and write failing auth-command tests (RED)</name>
  <files>backend/SafePath.sln, backend/tests/SafePath.Domain.Tests/SafePath.Domain.Tests.csproj, backend/tests/SafePath.Application.Tests/SafePath.Application.Tests.csproj, backend/tests/SafePath.Application.Tests/Auth/RegisterCommandTests.cs, backend/tests/SafePath.Application.Tests/Auth/LoginCommandTests.cs, backend/tests/SafePath.Application.Tests/Auth/RefreshTokenCommandTests.cs, backend/tests/SafePath.Application.Tests/Auth/LogoutCommandTests.cs, backend/tests/SafePath.Api.IntegrationTests/SafePath.Api.IntegrationTests.csproj, backend/tests/SafePath.Api.IntegrationTests/AuthEndpointsTests.cs</files>
  <read_first>
    - .planning/phases/01-backend-auth-foundation/01-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact test class names to create: RegisterCommandTests, LoginCommandTests, RefreshTokenCommandTests, LogoutCommandTests)
    - .planning/phases/01-backend-auth-foundation/01-RESEARCH.md (Architecture Patterns §1-2, lines 230-298 — the behaviors under test: 20-min access token, 64-byte refresh, single-use rotation, reuse-detection full-wipe)
  </read_first>
  <behavior>
    - RegisterCommandTests: valid email+password+role persists a User whose stored PasswordHash verifies against the plaintext via BCrypt AND is not equal to the plaintext; a second register with the same email is rejected; the returned AuthResult carries a non-empty access token and refresh token; the persisted Role round-trips (AUTH-01, AUTH-05).
    - LoginCommandTests: correct credentials return an access+refresh pair; an unknown email OR a wrong password both return the same failure result (AUTH-02, enumeration-safe).
    - RefreshTokenCommandTests: a valid refresh token is rotated (old one marked revoked, a new one issued); presenting an already-revoked token returns failure AND marks all of that user's active refresh tokens revoked (reuse detection) (AUTH-02).
    - LogoutCommandTests: logout marks the presented refresh token revoked; a subsequent refresh with it fails (AUTH-03).
    - AuthEndpointsTests (integration, WebApplicationFactory): POST /auth/register returns 200 with tokens; POST /auth/login with those credentials returns 200; POST /auth/login with a wrong password returns 401.
  </behavior>
  <action>Create `backend/SafePath.sln` via `dotnet new sln`. Scaffold three xUnit projects under `backend/tests/` targeting `net9.0`: `SafePath.Domain.Tests`, `SafePath.Application.Tests` (references SafePath.Application; add `Moq` or `NSubstitute` for mocking IApplicationDbContext/IJwtTokenGenerator/IPasswordHasher, and an EF Core InMemory or SQLite-in-memory provider package for the DbContext-backed handler tests), and `SafePath.Api.IntegrationTests` (references SafePath.Api; add `Microsoft.AspNetCore.Mvc.Testing` 9.0.x). Add all projects to the solution. Write the four Application unit test classes and the integration test class described in `<behavior>`, asserting the described behaviors against the (not-yet-existing) command/handler and endpoint types. These MUST fail to compile / fail at run right now — that is the RED state. Reference the exact class names from 01-VALIDATION.md so the per-task filter commands resolve. Do NOT implement production types in this task.</action>
  <verify>
    <automated>cd backend && dotnet build SafePath.sln 2>&1 | grep -Eiq "error|does not exist|could not be found" && echo "RED-OK: fails to build as expected" || (echo "UNEXPECTED: build succeeded before implementation" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `backend/SafePath.sln` exists and lists the three test projects (`dotnet sln backend/SafePath.sln list` shows them).
    - The four Application test files and `AuthEndpointsTests.cs` exist and reference `RegisterCommand`, `LoginCommand`, `RefreshTokenCommand`, `LogoutCommand`, and the `/auth/*` endpoints by name.
    - `dotnet build` fails (types not yet defined) — RED confirmed. No production `.cs` under `backend/src/` implements the commands yet.
  </acceptance_criteria>
  <done>Solution + 3 test projects exist; failing auth tests written per 01-VALIDATION.md names; build fails because production types are absent (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold source projects and implement the auth slice (GREEN)</name>
  <files>backend/src/SafePath.Domain/SafePath.Domain.csproj, backend/src/SafePath.Domain/Entities/User.cs, backend/src/SafePath.Domain/Entities/RefreshToken.cs, backend/src/SafePath.Domain/Enums/Role.cs, backend/src/SafePath.Domain/Enums/PermissionLevel.cs, backend/src/SafePath.Domain/Enums/InvitationStatus.cs, backend/src/SafePath.Application/SafePath.Application.csproj, backend/src/SafePath.Application/Common/Interfaces/IApplicationDbContext.cs, backend/src/SafePath.Application/Common/Interfaces/IPasswordHasher.cs, backend/src/SafePath.Application/Common/Interfaces/IJwtTokenGenerator.cs, backend/src/SafePath.Application/Common/Interfaces/ICurrentUserService.cs, backend/src/SafePath.Application/Common/Models/AuthResult.cs, backend/src/SafePath.Application/Auth/RegisterCommand.cs, backend/src/SafePath.Application/Auth/LoginCommand.cs, backend/src/SafePath.Application/Auth/RefreshTokenCommand.cs, backend/src/SafePath.Application/Auth/LogoutCommand.cs, backend/src/SafePath.Application/DependencyInjection.cs, backend/src/SafePath.Infrastructure/SafePath.Infrastructure.csproj, backend/src/SafePath.Infrastructure/Persistence/ApplicationDbContext.cs, backend/src/SafePath.Infrastructure/Persistence/EntityConfigurations/UserConfiguration.cs, backend/src/SafePath.Infrastructure/Persistence/EntityConfigurations/RefreshTokenConfiguration.cs, backend/src/SafePath.Infrastructure/Identity/BCryptPasswordHasher.cs, backend/src/SafePath.Infrastructure/Identity/JwtTokenGenerator.cs, backend/src/SafePath.Infrastructure/Identity/CurrentUserService.cs, backend/src/SafePath.Infrastructure/DependencyInjection.cs, backend/src/SafePath.Api/SafePath.Api.csproj, backend/src/SafePath.Api/Controllers/AuthController.cs, backend/src/SafePath.Api/Program.cs, backend/src/SafePath.Api/appsettings.json</files>
  <read_first>
    - .planning/phases/01-backend-auth-foundation/01-RESEARCH.md (Architecture Patterns §1 lines 236-265 for JwtTokenGenerator; §2 lines 268-298 for the RefreshTokenCommand reuse-detection body; Code Examples lines 391-398 for BCryptPasswordHasher; Recommended Project Structure lines 197-227 for folder layout; Standard Stack lines 65-96 for package identities)
    - .planning/phases/01-backend-auth-foundation/01-PATTERNS.md (Pattern Assignments — the exact code excerpts to copy for JwtTokenGenerator, RefreshTokenCommand, BCryptPasswordHasher; and Shared Patterns: cryptographically-secure randomness, custom identity abstraction)
    - backend/tests/SafePath.Application.Tests/Auth/RegisterCommandTests.cs (the contracts these types must satisfy — written in Task 1)
  </read_first>
  <action>Scaffold the four `net9.0` source projects (`SafePath.Domain` classlib, `SafePath.Application` classlib referencing Domain, `SafePath.Infrastructure` classlib referencing Application, `SafePath.Api` webapi referencing Infrastructure) and add each to the solution with the correct project references (Api->Infrastructure->Application->Domain). Add packages at **9.0.x**: `Microsoft.EntityFrameworkCore` (+`.Design`), `Npgsql.EntityFrameworkCore.PostgreSQL`, `Microsoft.AspNetCore.Authentication.JwtBearer`; plus `BCrypt.Net-Next` 4.2.0, `FluentValidation` 12.1.1, and (Claude's discretion) `Media
  tor.SourceGenerator` 3.0.2 for command dispatch.

Domain: `User` (Guid Id, string Email unique, string PasswordHash, string FullName, Role Role, DateTime CreatedAt) as a plain POCO — no Identity base class (D6). `RefreshToken` (Guid Id, Guid UserId, string Token, DateTime ExpiresAt, bool IsRevoked, Guid? ReplacedFrom, DateTime CreatedAt). Enums: `Role { Guardian, Member, Caregiver, OrgAdmin }`, `PermissionLevel { ViewOnly, FullLocation, NotificationOnly }`, `InvitationStatus { Pending, Accepted, Declined, Expired, Revoked }` (the latter two enums are created now so the family migration in plan 05 does not require a Domain edit).

Application: interfaces `IApplicationDbContext` (exposes `DbSet<User> Users`, `DbSet<RefreshToken> RefreshTokens`, `SaveChangesAsync`), `IPasswordHasher` (`Hash`/`Verify`), `IJwtTokenGenerator` (`GenerateAccessToken(User)`, `(string,DateTime) GenerateRefreshToken()`), `ICurrentUserService` (`Guid? UserId`, `Role? Role`). `AuthResult` model (bool Succeeded, string? AccessToken, string? RefreshToken, static Success/Invalid factories). Commands+handlers: `RegisterCommand` (hash password via IPasswordHasher, reject duplicate email, persist User+role, return AuthResult with a fresh access+refresh pair; persist the refresh token row), `LoginCommand` (verify credentials, same failure result for unknown-email and wrong-password), `RefreshTokenCommand` (copy RESEARCH §2 reuse-detection body verbatim — single-use rotation, revoked-token-reuse wipes all active tokens for the user), `LogoutCommand` (mark the presented refresh token IsRevoked). FluentValidation validators for `RegisterCommand` (email format, password min length 8) and `ResetPasswordCommand` shape is out-of-scope here. `DependencyInjection.AddApplication()` registers Mediator/handlers + validators.

Infrastructure: `ApplicationDbContext : DbContext, IApplicationDbContext` with the two DbSets + `EntityConfigurations` (Email unique index; RefreshToken.Token indexed). `BCryptPasswordHasher` (work factor 12, verbatim from RESEARCH Code Examples). `JwtTokenGenerator` (verbatim from RESEARCH §1: `SecurityAlgorithms.HmacSha256`, 20-min access expiry, `RandomNumberGenerator.GetBytes(64)` refresh — never `Guid.NewGuid()`). `CurrentUserService` reads the `sub`/role claims from `IHttpContextAccessor`. `DependencyInjection.AddInfrastructure(config)` registers the DbContext with the Npgsql connection string `ConnectionStrings:DefaultConnection`, the hasher, the token generator, and `IApplicationDbContext`.

Api: `AuthController` with `POST /auth/register`, `/auth/login`, `/auth/refresh`, `/auth/logout` dispatching the commands and returning `AuthResult` (401 on `!Succeeded` for login/refresh). `Program.cs` wires `AddApplication()` + `AddInfrastructure(builder.Configuration)`, `AddHttpContextAccessor`, and `AddAuthentication().AddJwtBearer` with `TokenValidationParameters` that **pin `ValidAlgorithms` to `["HS256"]`**, validate issuer/audience/lifetime, and use the signing key from config (`Jwt:Key`/`Jwt:Issuer`/`Jwt:Audience`) — an `IssuerSigningKey`, never `alg:none`. `appsettings.json` holds Jwt issuer/audience + placeholder key (real key + connection string come from user-secrets, Task 3). Create the initial EF Core migration `dotnet ef migrations add InitialAuth` producing `Users` + `RefreshTokens` tables. All Application unit tests must now pass (GREEN) using the InMemory/SQLite provider; integration tests remain pending the DB in Task 3.</action>
  <verify>
    <automated>cd backend && dotnet build SafePath.sln && dotnet test tests/SafePath.Application.Tests --filter "FullyQualifiedName~RegisterCommandTests|FullyQualifiedName~LoginCommandTests|FullyQualifiedName~RefreshTokenCommandTests|FullyQualifiedName~LogoutCommandTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build backend/SafePath.sln` succeeds with 0 errors.
    - `backend/src/SafePath.Infrastructure/Identity/JwtTokenGenerator.cs` contains `SecurityAlgorithms.HmacSha256` and `RandomNumberGenerator.GetBytes(64)`; it does NOT use `Guid.NewGuid()` for the refresh token.
    - `backend/src/SafePath.Infrastructure/Identity/BCryptPasswordHasher.cs` contains `workFactor: 12`.
    - `Program.cs` JwtBearer config pins the accepted algorithm to HS256 (asserted by the presence of a `ValidAlgorithms` constraint) and reads `Jwt:Key` from configuration rather than a hardcoded literal.
    - All four Application auth test classes pass: `dotnet test tests/SafePath.Application.Tests` exits 0 for the filtered auth tests.
    - `backend/src/SafePath.Infrastructure/Persistence/Migrations/` contains an `InitialAuth` migration defining `Users` and `RefreshTokens`.
    - No package reference in any `.csproj` pins EF Core / Npgsql / JwtBearer to a 10.x version (D4).
  </acceptance_criteria>
  <done>Backend builds; Domain/Application/Infrastructure/Api populated per RESEARCH patterns; four auth commands implemented; initial migration generated; all Application auth unit tests green (GREEN).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The backend auth slice is code-complete and unit-green against an in-memory provider. It now needs a real Supabase Postgres database to apply the migration to and to run the integration tests against — the "one real DB write" of the Walking Skeleton.</what-built>
  <how-to-verify>
    1. In the Supabase Dashboard, create a new project (region nearest you) and wait for it to finish provisioning. Copy the **Direct connection** string (Project Settings -> Database -> Connection string, port 5432, URI form) and the database password.
    2. From `backend/src/SafePath.Api`, initialize user-secrets and set the connection string and a strong JWT signing key (do NOT commit either):
       `dotnet user-secrets init`
       `dotnet user-secrets set "ConnectionStrings:DefaultConnection" "<your Supabase direct connection string>"`
       `dotnet user-secrets set "Jwt:Key" "<a 32+ byte random base64 string>"`
    3. Reply "provisioned" here. Claude will then apply the migration (`dotnet ef database update`) and run the integration tests.
  </how-to-verify>
  <resume-signal>Type "provisioned" once the Supabase project exists and the connection string + JWT key are set in user-secrets, or describe any issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply migration to Supabase and prove the DB round-trip (integration GREEN)</name>
  <files>backend/src/SafePath.Api/appsettings.Development.json, backend/tests/SafePath.Api.IntegrationTests/AuthEndpointsTests.cs</files>
  <read_first>
    - backend/tests/SafePath.Api.IntegrationTests/AuthEndpointsTests.cs (the register/login integration assertions written in Task 1)
    - .planning/phases/01-backend-auth-foundation/01-RESEARCH.md (Alternatives Considered / STACK note lines 111 — direct connection port 5432 for the persistent ASP.NET Core process, not the transaction pooler)
  </read_first>
  <action>With the connection string now in user-secrets, run `dotnet ef database update --project backend/src/SafePath.Infrastructure --startup-project backend/src/SafePath.Api` to apply `InitialAuth` to Supabase (creates `Users` + `RefreshTokens`). If the integration test project uses a separate test database, point its `WebApplicationFactory` config at a test connection string via `appsettings.Development.json` or environment override rather than the production Supabase project (Claude's discretion — a dedicated Supabase "test" schema or a Testcontainers Postgres both satisfy this; document the choice in the SUMMARY). Run the integration suite: register a user via the real HTTP pipeline, confirm a row lands in `Users`, then log in with those credentials. Confirm the wrong-password path returns 401.</action>
  <verify>
    <automated>cd backend && dotnet test tests/SafePath.Api.IntegrationTests --filter "FullyQualifiedName~AuthEndpointsTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet ef database update` completes; the Supabase database now has `Users` and `RefreshTokens` tables (verifiable via the Supabase table editor).
    - `AuthEndpointsTests` passes: POST /auth/register returns 200 and creates exactly one `Users` row for a fresh email; POST /auth/login returns 200 for correct creds and 401 for a wrong password.
    - Neither the Supabase connection string nor `Jwt:Key` appears committed in any tracked file (they live in user-secrets / environment).
  </acceptance_criteria>
  <done>Migration applied to Supabase; register writes a real row; login round-trips; integration tests green. Walking-Skeleton backend DB write proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client -> /auth/* API | Untrusted email/password/refresh-token input crosses here over HTTPS |
| API -> Supabase Postgres | Backend connects with a trusted service-role connection string; DB trusts the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | POST /auth/login (credential stuffing / brute force) | high | mitigate | Rate-limit `/auth/login` per-IP and per-account via ASP.NET Core rate limiting middleware in Program.cs; generic 401 that never reveals which field was wrong (Task 2) |
| T-01-02 | Spoofing / Elevation of Privilege | Refresh token theft & reuse | high | mitigate | Single-use rotation + reuse-detection full-wipe in RefreshTokenCommand (RESEARCH §2), 64-byte CSPRNG tokens, DB-revocable RefreshTokens table (Task 2) |
| T-01-03 | Tampering | JWT algorithm-confusion / `alg:none` | high | mitigate | JwtBearer TokenValidationParameters pin accepted algorithm to HS256, validate issuer/audience/lifetime, signing key from config never hardcoded (Task 2) |
| T-01-04 | Information Disclosure | Password at rest | high | mitigate | BCrypt work factor 12; only the hash persisted, never plaintext; asserted by RegisterCommandTests (Tasks 1-2) |
| T-01-05 | Information Disclosure | JWT signing key / DB connection string in source | high | mitigate | Key + connection string live in `dotnet user-secrets` / environment, never committed (Task 3 acceptance) |
| T-01-SC | Tampering | NuGet package supply chain | medium | mitigate | All packages VERIFIED + Approved in RESEARCH.md Package Legitimacy Audit (no ASSUMED/SUS/SLOP); pin exact versions in .csproj |
</threat_model>

<verification>
- `dotnet build backend/SafePath.sln` → 0 errors.
- `dotnet test backend/SafePath.sln` → all Domain + Application + Api integration tests green.
- Supabase table editor shows `Users` and `RefreshTokens` tables with at least one registered user row after the integration run.
- Grep check: `JwtTokenGenerator.cs` contains `HmacSha256` and `GetBytes(64)`; `BCryptPasswordHasher.cs` contains `workFactor: 12`.
</verification>

<success_criteria>
- A user can register (with a role), log in, refresh, and log out through the real HTTP pipeline against Supabase.
- Refresh-token rotation + reuse detection provably revoke a stolen/reused token (RefreshTokenCommandTests green).
- Role is persisted and appears as a claim in the issued access token.
- No secret is committed to the repo.
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Solution / projects:** `backend/SafePath.sln`; projects `SafePath.Domain`, `SafePath.Application`, `SafePath.Infrastructure`, `SafePath.Api`, `SafePath.Domain.Tests`, `SafePath.Application.Tests`, `SafePath.Api.IntegrationTests`.
**Domain entities:** `User`, `RefreshToken`. **Enums:** `Role { Guardian, Member, Caregiver, OrgAdmin }`, `PermissionLevel { ViewOnly, FullLocation, NotificationOnly }`, `InvitationStatus { Pending, Accepted, Declined, Expired, Revoked }`.
**Application interfaces:** `IApplicationDbContext`, `IPasswordHasher`, `IJwtTokenGenerator`, `ICurrentUserService`. **Model:** `AuthResult`.
**Commands/handlers:** `RegisterCommand`, `LoginCommand`, `RefreshTokenCommand`, `LogoutCommand` (+ FluentValidation validators for register). **DI:** `AddApplication()`, `AddInfrastructure(IConfiguration)`.
**Infrastructure:** `ApplicationDbContext` (DbSets `Users`, `RefreshTokens`), `UserConfiguration`, `RefreshTokenConfiguration`, `BCryptPasswordHasher`, `JwtTokenGenerator`, `CurrentUserService`, `InitialAuth` EF migration.
**API:** `AuthController` endpoints `POST /auth/register`, `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`; `Program.cs` with JwtBearer (HS256-pinned) + login rate limiting.
**Config keys consumed:** `ConnectionStrings:DefaultConnection`, `Jwt:Key`, `Jwt:Issuer`, `Jwt:Audience`.
</artifacts>

<output>
Create `.planning/phases/01-backend-auth-foundation/01-01-SUMMARY.md` when done.
</output>
