---
phase: 04-geofencing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/SafePath.Domain/Entities/SafeZone.cs
  - backend/src/SafePath.Application/Geofencing/GeofenceTracer.cs
  - backend/src/SafePath.Application/Common/Interfaces/IApplicationDbContext.cs
  - backend/src/SafePath.Infrastructure/Persistence/ApplicationDbContext.cs
  - backend/src/SafePath.Application/DependencyInjection.cs
  - backend/src/SafePath.Api/Controllers/GeofenceCandidatesController.cs
  - backend/tests/SafePath.Application.Tests/Geofencing/GeofenceTracerTests.cs
  - backend/tests/SafePath.Api.IntegrationTests/GeofenceTracerEndpointTests.cs
autonomous: true
requirements: [GEO-01, GEO-02]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A Guardian can persist one production safe-zone configuration and its assigned member can fetch and acknowledge that registration (GEO-01, D-01-D-09)."
    - "The assigned member can submit one authenticated, idempotent native candidate and receive an Accepted/Duplicate result before any device checkpoint (GEO-02, D-05, D-06)."
  artifacts:
    - {path: "backend/src/SafePath.Application/Geofencing/GeofenceTracer.cs", provides: "Minimal zone/config/ack/candidate contracts and handlers"}
    - {path: "backend/src/SafePath.Api/Controllers/GeofenceCandidatesController.cs", provides: "Authenticated tracer endpoints"}
  key_links:
    - {from: "GeofenceCandidatesController", to: "IApplicationDbContext", via: "current-user-bound handlers", pattern: "ICurrentUserService"}
---

<objective>
Prove one real backend geofence path before native work: Guardian zone creation -> assigned-member registration fetch/ack -> authenticated candidate ingestion.

Purpose: Make the Android tracer executable against a production-shaped endpoint while implementing PR-01 and preserving D-05/D-06 candidate semantics.
Output: Minimal durable contracts, handlers, endpoints, and integration coverage.
</objective>

<execution_context>
@C:/Users/DELL/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/DELL/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-geofencing/04-CONTEXT.md
@.planning/phases/04-geofencing/04-RESEARCH.md
@backend/src/SafePath.Infrastructure/Identity/FamilyAuthorizationService.cs
@backend/src/SafePath.Api/Controllers/LocationController.cs
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Wire one authenticated zone/config/candidate path end to end</name>
  <files>backend/src/SafePath.Domain/Entities/SafeZone.cs, backend/src/SafePath.Application/Geofencing/GeofenceTracer.cs, backend/src/SafePath.Application/Common/Interfaces/IApplicationDbContext.cs, backend/src/SafePath.Infrastructure/Persistence/ApplicationDbContext.cs, backend/src/SafePath.Application/DependencyInjection.cs, backend/src/SafePath.Api/Controllers/GeofenceCandidatesController.cs, backend/tests/SafePath.Api.IntegrationTests/GeofenceTracerEndpointTests.cs</files>
  <behavior>
    - Guardian creates one fully specified circle zone; assigned member fetches its generation and acknowledges it.
    - Assigned member submits a unique native candidate and receives Accepted; replay receives Duplicate without a second row.
    - Unauthenticated, non-Guardian, wrong-member, inactive-member, and cross-family calls are rejected without disclosure.
  </behavior>
  <action>Implement the production happy path using PR-01 normalized persistence. Derive caller identity from ICurrentUserService and re-check family role/membership. Accept D-01-D-09 zone fields, but expose only create, assigned-member registration fetch/ack, and candidate submit in this tracer; full list/update/delete/error expansion belongs to 04-06. A native callback is only a candidate per D-05/D-06 and creates no activity or notification yet. Candidate request accepts event id, zone registration generation, transition, occurrence time, coordinates, and accuracy; it accepts no family/member/recipient authority fields.</action>
  <verify><automated>dotnet test backend/tests/SafePath.Api.IntegrationTests --filter "FullyQualifiedName~GeofenceTracerEndpointTests"</automated></verify>
  <done>The production endpoints complete one authenticated zone-to-candidate round trip against the test host.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Lock caller binding, replay, and inactive-registration behavior</name>
  <files>backend/tests/SafePath.Application.Tests/Geofencing/GeofenceTracerTests.cs, backend/tests/SafePath.Api.IntegrationTests/GeofenceTracerEndpointTests.cs</files>
  <behavior>
    - Registration acknowledgement must match member, zone, and generation.
    - Candidate replay is side-effect-free and stale/unknown generations are refused.
    - No tracer handler references push, AlertHub, or SOS services per D-11.
  </behavior>
  <action>Add focused unit and integration abuse cases, including structural SOS-isolation assertions. Keep accepted candidate state durable for the Android tracer and leave transition confirmation to 04-07.</action>
  <verify><automated>dotnet test backend/tests/SafePath.Application.Tests --filter "FullyQualifiedName~GeofenceTracerTests"</automated></verify>
  <done>The tracer has deterministic auth, replay, generation, and SOS-isolation behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Client -> API | Zone and candidate input crosses JWT and family authorization |
| API -> database | Idempotency and server-owned relationships become durable |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Spoofing/Elevation | tracer endpoints | high | mitigate | Current-user binding, Guardian/member checks, IDOR tests |
| T-04-02 | Tampering/Repudiation | candidate replay | high | mitigate | Generation binding, immutable event id, unique constraint |
</threat_model>

<verification>Run both focused suites and `dotnet test backend/SafePath.sln`.</verification>
<success_criteria>The authenticated backend path required by the native tracer exists before any physical-device checkpoint.</success_criteria>
<output>Create `.planning/phases/04-geofencing/04-01-SUMMARY.md` when done.</output>
