---
phase: 04-geofencing
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [GEO-01, GEO-02]
user_setup:
  - service: android-device
    why: "A physical Android 10+ Google Play Services device is required for process-death registration and authenticated upload proof."
estimate: {tokens: 7000, raw_tokens: 7000, tasks: 1, confidence: low}
must_haves:
  truths:
    - "The minimal backend, native registration, permission capability, process-death callback, restored-session upload, and replay behavior are physically proven before all later expansion."
    - "A concurrent SOS remains immediate and independent per D-11."
  artifacts: []
  key_links: []
---
<objective>Block Phase 4 expansion until the actual Android tracer works end to end.</objective>
<execution_context>@C:/Users/DELL/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/DELL/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-geofencing/04-01-SUMMARY.md
@.planning/phases/04-geofencing/04-04-SUMMARY.md
@.planning/phases/04-geofencing/04-CONTEXT.md</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prove Android process-death registration and authenticated upload</name>
  <what-built>Production minimal zone/config/candidate backend plus Android registration, durable callback outbox, headless Supabase-authenticated uploader, and cold-relaunch fallback.</what-built>
  <how-to-verify>
    1. Run the migrated backend on a device-reachable HTTPS URL; sign in as Guardian and assigned Member on a physical Android 10+ Google Play Services device.
    2. Use the production POST endpoint with the Guardian token to create one 100 m zone; grant background location through OS settings for this tracer and confirm the Member fetches/acks the exact generation.
    3. Background the app, kill its process with `adb shell am kill com.safepath.mobile` (do not force-stop), cross or simulate the zone boundary, and confirm one native event is durably written before any Flutter UI exists.
    4. Confirm WorkManager starts the headless entry point, restores the Member's Supabase session, posts the event to `/geofences/candidates`, and acknowledges it only after Accepted/Duplicate. Logs must contain no token.
    5. Repeat with network disabled: the row must remain; re-enable network and cold-launch the app; the authenticated bootstrap must drain it exactly once.
    6. Leave one candidate pending and trigger the existing SOS flow; it must open/respond immediately with existing channel/service behavior unchanged per D-11.
  </how-to-verify>
  <resume-signal>Type "approved" with device/API, event id, and observed headless/cold-relaunch results, or describe the failing lifecycle step.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Physical evidence -> expansion authorization | Device results permit later implementation work |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-09 | Repudiation | tracer approval | high | mitigate | Record OS/device/build/event id and both lifecycle results in SUMMARY |
</threat_model>
<verification>The blocking checklist is approved with concrete device evidence.</verification>
<success_criteria>No CRUD/UI/activity/push expansion begins until the Android tracer and SOS isolation pass.</success_criteria>
<output>Create `.planning/phases/04-geofencing/04-05-SUMMARY.md` when done.</output>
