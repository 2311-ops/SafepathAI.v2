---
phase: 04-geofencing
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - mobile/android/app/build.gradle.kts
  - mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBroadcastReceiver.kt
  - mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceUploadWorker.kt
  - mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBackgroundEngine.kt
  - mobile/lib/features/geofencing/data/geofence_candidate_uploader.dart
  - mobile/lib/features/geofencing/data/geofence_background_entrypoint.dart
  - mobile/lib/main.dart
  - mobile/test/features/geofencing/geofence_candidate_uploader_test.dart
autonomous: true
requirements: [GEO-02]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "PR-02 uploads a process-death candidate through restored Supabase auth without copying tokens into native storage."
    - "Headless auth/network failure retains the candidate and authenticated cold relaunch drains it; acknowledgement follows accepted/duplicate success only."
  artifacts:
    - {path: "mobile/lib/features/geofencing/data/geofence_background_entrypoint.dart", provides: "Headless Supabase/session/Dio drain entry point"}
    - {path: "mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceUploadWorker.kt", provides: "Unique retryable WorkManager bridge"}
  key_links:
    - {from: "GeofenceUploadWorker.kt", to: "geofence_background_entrypoint.dart", via: "headless Flutter engine", pattern: "geofenceBackgroundMain"}
---
<objective>Complete the concrete authenticated Android process-death/relaunch upload bridge from PR-02.</objective>
<execution_context>@C:/Users/DELL/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/DELL/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-geofencing/04-CONTEXT.md
@.planning/phases/04-geofencing/04-01-SUMMARY.md
@.planning/phases/04-geofencing/04-03-SUMMARY.md
@mobile/lib/main.dart
@mobile/lib/core/network/dio_client.dart</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enqueue one unique headless upload after native persistence</name>
  <files>mobile/android/app/build.gradle.kts, mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBroadcastReceiver.kt, mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceUploadWorker.kt, mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBackgroundEngine.kt</files>
  <behavior>
    - Receiver enqueues unique work only after synchronous persistence.
    - Worker starts one cached/headless engine and maps retry/success without deleting native data itself.
    - Concurrent callbacks coalesce bounded work but preserve distinct outbox rows.
  </behavior>
  <action>Use official AndroidX WorkManager with `ExistingWorkPolicy.KEEP`. Start the named `geofenceBackgroundMain` Dart entry point through a headless Flutter engine; close it after a bounded result/timeout. Never pass tokens through WorkManager input, preferences, MethodChannel, or logs.</action>
  <verify><automated>cd mobile/android &amp;&amp; ./gradlew testDebugUnitTest --tests "*GeofenceUploadWorkerTest"</automated></verify>
  <done>Process-death callbacks schedule bounded, retryable Dart work without a credential in Kotlin.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Restore Supabase auth, upload, acknowledge, and cold-relaunch fallback</name>
  <files>mobile/lib/features/geofencing/data/geofence_candidate_uploader.dart, mobile/lib/features/geofencing/data/geofence_background_entrypoint.dart, mobile/lib/main.dart, mobile/test/features/geofencing/geofence_candidate_uploader_test.dart</files>
  <behavior>
    - Headless entry initializes Supabase, restores/refreshes persisted session, builds `buildDio`, uploads, then acknowledges accepted/duplicate rows.
    - Missing/invalid session or network failure retains every row and reports retry.
    - Normal main bootstrap performs the same drain after auth restoration and is idempotent with headless completion.
  </behavior>
  <action>Factor one candidate drain coordinator shared by headless and normal bootstrap. Use the existing Supabase configuration, SupabaseAuthApi/AuthInterceptor, and Dio base URL. Wait for session restoration, attempt refresh through the existing AuthApi, and never sign out on transient failure. Acknowledge only HTTP accepted/duplicate. Register the cold-relaunch drain after Supabase.initialize and auth bootstrap, before presenting a zone as active.</action>
  <verify><automated>cd mobile &amp;&amp; flutter test test/features/geofencing/geofence_candidate_uploader_test.dart</automated></verify>
  <done>The exact PR-02 bridge is automated and ready for real-device proof.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Native worker -> Dart auth | Background execution attempts session restoration |
| Dart -> candidate API | Sensitive evidence crosses authenticated HTTPS |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07 | Information disclosure | background credential | critical | mitigate | No native token; existing Supabase persistence and AuthInterceptor only |
| T-04-08 | Denial of service | retry loop | high | mitigate | Unique bounded work, retained outbox, WorkManager backoff |
</threat_model>
<verification>Focused native/Dart tests, analyze, and Android debug build pass.</verification>
<success_criteria>A killed-process candidate has an authenticated headless upload path and a deterministic cold-relaunch fallback.</success_criteria>
<output>Create `.planning/phases/04-geofencing/04-04-SUMMARY.md` when done.</output>
