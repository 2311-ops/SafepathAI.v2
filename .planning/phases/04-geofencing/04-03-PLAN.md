---
phase: 04-geofencing
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - mobile/android/app/build.gradle.kts
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/main/kotlin/com/safepath/mobile/MainActivity.kt
  - mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBroadcastReceiver.kt
  - mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceNativeStore.kt
  - mobile/lib/features/geofencing/data/native_geofence_gateway.dart
  - mobile/lib/features/geofencing/application/geofence_registration_controller.dart
  - mobile/test/features/geofencing/native_geofence_gateway_test.dart
  - mobile/test/features/geofencing/geofence_registration_controller_test.dart
autonomous: true
requirements: [GEO-02]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Android registers the canonical server set only with background-capable permission and acknowledges the exact generation after native success."
    - "A dead Flutter process is not required to persist one OS candidate; duplicate callbacks remain one durable row (D-05, D-06, PR-02)."
  artifacts:
    - {path: "mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBroadcastReceiver.kt", provides: "Non-exported callback receiver"}
    - {path: "mobile/lib/features/geofencing/application/geofence_registration_controller.dart", provides: "Authenticated canonical registration lifecycle"}
  key_links:
    - {from: "geofence_registration_controller.dart", to: "GeofenceCandidatesController", via: "registration fetch and generation ack", pattern: "registration"}
---
<objective>Implement the Android registration, permission-capability, receiver, and native-outbox half of the physical tracer.</objective>
<execution_context>@C:/Users/DELL/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/DELL/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-geofencing/04-CONTEXT.md
@.planning/phases/04-geofencing/04-02-SUMMARY.md
@mobile/lib/features/location/application/permission_controller.dart
@mobile/android/app/src/main/AndroidManifest.xml</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist Android callbacks before Dart and deduplicate them</name>
  <files>mobile/android/app/build.gradle.kts, mobile/android/app/src/main/AndroidManifest.xml, mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceBroadcastReceiver.kt, mobile/android/app/src/main/kotlin/com/safepath/mobile/GeofenceNativeStore.kt</files>
  <behavior>
    - Receiver writes versioned event id, request id, transition, occurrence time, and error state synchronously before returning.
    - Replayed event ids do not duplicate; acknowledged rows disappear; reconstruction retains pending rows.
    - Receiver is non-exported and never starts the SOS foreground service.
  </behavior>
  <action>Add the official Google Play Services location dependency and non-exported PendingIntent receiver. Persist app-private versioned JSON with a synchronous commit, bounded row count, immutable ids, and no auth token. Treat callbacks as routine candidates only. Preserve SOS manifest/service declarations verbatim per D-11.</action>
  <verify><automated>cd mobile/android &amp;&amp; ./gradlew testDebugUnitTest --tests "*GeofenceNativeStoreTest"</automated></verify>
  <done>A callback survives Flutter-process death and remains safely drainable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Register canonical zones through a capability-aware Flutter gateway</name>
  <files>mobile/android/app/src/main/kotlin/com/safepath/mobile/MainActivity.kt, mobile/lib/features/geofencing/data/native_geofence_gateway.dart, mobile/lib/features/geofencing/application/geofence_registration_controller.dart, mobile/test/features/geofencing/native_geofence_gateway_test.dart, mobile/test/features/geofencing/geofence_registration_controller_test.dart</files>
  <behavior>
    - Authenticated member fetches, atomically replaces, then acknowledges only the matching generation.
    - Foreground-only, denied, restricted, and unavailable states never acknowledge and expose NeedsLocationPermission/NeedsSync.
    - Logout removes monitored zones without touching SOS state.
  </behavior>
  <action>Expose typed `replaceMonitoredZones`, `getCapability`, `drainPendingCandidates`, and `acknowledgeCandidate` over `safepath/geofencing`. The registration controller follows auth/family state, never prompts during build, and exposes an explicit save-triggered permission coordinator for 04-12. Enforce the 20-zone cross-platform budget.</action>
  <verify><automated>cd mobile &amp;&amp; flutter test test/features/geofencing/native_geofence_gateway_test.dart test/features/geofencing/geofence_registration_controller_test.dart</automated></verify>
  <done>One server configuration becomes an honestly acknowledged Android registration.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Android OS -> native outbox | Replayed/untrusted callback enters app storage |
| Server config -> OS registration | Authenticated configuration controls monitoring |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-05 | Spoofing/Repudiation | receiver/outbox | high | mitigate | Non-exported receiver, immutable ids, server caller binding |
| T-04-06 | Denial of service | permission/quota | medium | mitigate | Explicit capability states and 20-zone cap |
</threat_model>
<verification>Focused Kotlin/Flutter tests and `flutter analyze` pass.</verification>
<success_criteria>Android can register and durably capture one candidate without relying on a live Flutter UI.</success_criteria>
<output>Create `.planning/phases/04-geofencing/04-03-SUMMARY.md` when done.</output>
