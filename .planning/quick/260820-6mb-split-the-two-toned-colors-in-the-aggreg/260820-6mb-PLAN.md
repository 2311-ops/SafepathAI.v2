---
phase: quick-260820-6mb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/location/presentation/live_map_screen.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "The compact status pill on the Live Map screen shows the online count in green (AppColors.safe) and the offline count in the dedicated offline slate tone (AppColors.offline), as two independently-colored spans within one text run."
    - "The pill's background, border, icon color, and every sibling widget in the file are visually unchanged."
  artifacts:
    - "mobile/lib/features/location/presentation/live_map_screen.dart — _CompactStatusSummary.build() renders Text.rich(TextSpan) instead of a single Text"
  key_links:
    - "_CompactStatusSummary constructor still takes onlineCount/offlineCount ints and both are interpolated into their respective TextSpans exactly as before ('$onlineCount on' / '$offlineCount off'), just split into two spans."
---

<objective>
Split the single-color `Text('$onlineCount on  $offlineCount off', ...)` inside `_CompactStatusSummary` (Live Map screen's online/offline counter pill) into a `Text.rich(TextSpan(...))` with the "on" segment colored `AppColors.safe` (unchanged) and the "off" segment colored `AppColors.offline` (added in the prior quick task 260820-5xp).

Purpose: Direct visual follow-up to 260820-5xp — the offline token now exists but the pill's text was never updated to actually use it; currently the whole string (including the offline count) renders in the green "safe" color, which is misleading.
Output: `_CompactStatusSummary` renders two-toned text; no other widget in the file changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current implementation (mobile/lib/features/location/presentation/live_map_screen.dart, lines 654-694):
- `_CompactStatusSummary` is a `StatelessWidget` taking `onlineCount`/`offlineCount` (both `int`).
- `build()` returns a `DecoratedBox` (`color: AppColors.safeBg`, `border: Border.all(color: AppColors.safeBgBorder)`, `borderRadius: BorderRadius.circular(999)`) wrapping a `Padding` wrapping a `Row` (`mainAxisSize: MainAxisSize.min`) with:
  1. `Icon(Icons.wifi_tethering, size: 12, color: AppColors.safe)` — unchanged by this task.
  2. `SizedBox(width: 4)`.
  3. `Flexible(child: Text('$onlineCount on  $offlineCount off', maxLines: 1, overflow: TextOverflow.ellipsis, style: AppTypography.caption.copyWith(color: AppColors.safe, fontWeight: FontWeight.w800, letterSpacing: 0, fontSize: 10.5)))` — this is the only node touched.
- `AppColors.offline = Color(0xFF8B96A3)` already exists in `mobile/lib/core/theme/app_colors.dart` (line 65, added by 260820-5xp) — reference it, do not redefine.
- No existing test in `mobile/test/features/location/live_map_screen_test.dart` (or elsewhere) asserts on this pill's text or colors — confirmed via grep for `CompactStatusSummary`/`onlineCount`/`offlineCount`/`wifi_tethering` inside that test file: no matches.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split the online/offline pill text into two colored spans</name>
  <files>mobile/lib/features/location/presentation/live_map_screen.dart</files>
  <action>
In `_CompactStatusSummary.build()`, replace the `Flexible(child: Text('$onlineCount on  $offlineCount off', ...))` node (lines 677-689) with a `Flexible(child: Text.rich(TextSpan(...), maxLines: 1, overflow: TextOverflow.ellipsis))`.

Move `maxLines: 1` and `overflow: TextOverflow.ellipsis` onto the `Text.rich` widget itself (same as they were on `Text` today — these are `Text`/`Text.rich` constructor params, not `TextSpan` params).

Set `Text.rich`'s `style:` to the shared non-color base: `AppTypography.caption.copyWith(fontWeight: FontWeight.w800, letterSpacing: 0, fontSize: 10.5)` (everything from the current `.copyWith(...)` chain except `color`).

Build the root `TextSpan`'s `children` as three `TextSpan`s:
1. `TextSpan(text: '$onlineCount on', style: const TextStyle(color: AppColors.safe))`.
2. `TextSpan(text: '  ')` (two-space separator, exactly matching today's `'  '` gap between "on" and the offline count) — no explicit color (inherits from the base style, or use a neutral tone if that reads visibly better; this is a small judgment call, keep it simple by omitting `style` entirely so it inherits).
3. `TextSpan(text: '$offlineCount off', style: const TextStyle(color: AppColors.offline))`.

Do not add a `style:` to the outer `TextSpan` itself — only the inner base `Text.rich(style: ...)` and the two colored child spans need styling.

Do not touch: the `DecoratedBox`'s `color`/`border`/`borderRadius`, the `Icon(Icons.wifi_tethering, ...)`, the `SizedBox(width: 4)`, `_PresenceDot`, `_InlinePresence`, `_StatusBadge`, or any other class/widget in this file. Do not introduce `AppColors.sosRed`/`#DE3B40` anywhere in this change.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/location/presentation/live_map_screen.dart</automated>
  </verify>
  <done>`_CompactStatusSummary` renders a `Text.rich` with three `TextSpan`s: "$onlineCount on" colored `AppColors.safe`, a plain "  " separator, and "$offlineCount off" colored `AppColors.offline`; `maxLines: 1` and `TextOverflow.ellipsis` are preserved on the rich text; the pill's background/border and the wifi icon color are byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions in the touched file's test coverage</name>
  <files>mobile/lib/features/location/presentation/live_map_screen.dart</files>
  <action>
Run `flutter analyze` across the whole `mobile` package to confirm the edit introduces no lint/type errors anywhere else. Then run `mobile/test/features/location/live_map_screen_test.dart` (the test file for this screen) to confirm existing tests still pass — this file has no assertions on `_CompactStatusSummary`'s text/colors specifically (confirmed during planning), so this run is a regression check only, not new coverage. Do not write a new test for this widget since none exists to extend and the task instructions say not to invent one.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze &amp;&amp; flutter test test/features/location/live_map_screen_test.dart</automated>
  </verify>
  <done>`flutter analyze` reports no new issues in `mobile/`; `live_map_screen_test.dart` passes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure client-side presentational change (text color split) with no new data flow, trust boundary, or external input — no STRIDE-applicable threat surface introduced. |

## STRIDE Threat Register

No new threats — this plan touches only static widget styling (`TextSpan` colors) inside an existing, already-reviewed screen. No new inputs, no new permissions, no new network/storage access.
</threat_model>

<verification>
1. `flutter analyze` on `mobile/` is clean (no new errors/warnings from this change).
2. `flutter test test/features/location/live_map_screen_test.dart` passes.
3. Manual/visual: online count segment reads in `AppColors.safe` green, offline count segment reads in `AppColors.offline` slate-grey, both within the same pill, pill background/border/icon unchanged.
</verification>

<success_criteria>
- `_CompactStatusSummary` displays the online count and offline count in two distinct colors within a single text run.
- No other widget, color token, or file is modified.
- `flutter analyze` and the existing Live Map test suite pass.
</success_criteria>

<output>
Create `.planning/quick/260820-6mb-split-the-two-toned-colors-in-the-aggreg/260820-6mb-SUMMARY.md` when done
</output>
