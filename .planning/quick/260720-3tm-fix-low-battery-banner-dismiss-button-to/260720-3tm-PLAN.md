---
phase: quick-260720-3tm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/location/presentation/low_battery_banner.dart
  - mobile/test/features/location/low_battery_banner_test.dart
autonomous: true
requirements: []
must_haves:
  truths:
    - The low-battery banner dismiss button meets the >=44x44 logical-px minimum tap target.
  artifacts:
    - mobile/lib/features/location/presentation/low_battery_banner.dart
    - mobile/test/features/location/low_battery_banner_test.dart
  key_links:
    - Dismiss IconButton onPressed still fires onDismissed after the density change.
---

<objective>
Restore the low-battery banner's dismiss button to a full-size tap target by removing the `VisualDensity.compact` override that shrinks it below the 44x44px minimum touch-target guideline.

Purpose: `VisualDensity.compact` (visual density (-2, -2)) shrinks the `IconButton`'s interactive size below Material's default 48x48 and below the 44x44px accessibility minimum, making the dismiss control hard to tap reliably.
Output: A one-line style removal plus a lightweight tap-target-size assertion in the existing widget test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@mobile/lib/features/location/presentation/low_battery_banner.dart
@mobile/test/features/location/low_battery_banner_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove compact visual density from the dismiss IconButton</name>
  <files>mobile/lib/features/location/presentation/low_battery_banner.dart, mobile/test/features/location/low_battery_banner_test.dart</files>
  <action>In `low_battery_banner.dart`, delete the single line `visualDensity: VisualDensity.compact,` from the dismiss `IconButton` (currently lines 62-67). Leave `tooltip`, `onPressed`, and `icon` (and every other property, the surrounding `Row`/`Padding`/`Column` layout, colors, and copy) exactly as-is so the button falls back to the platform-default density and its full 48x48 tap target is restored.

  In `low_battery_banner_test.dart`, extend the existing `testWidgets('shows exact low-battery copy and dismisses', ...)` test: after the copy assertions and before/around the tap, add a tap-target assertion using `tester.getSize(find.byType(IconButton))` and assert both `size.width` and `size.height` are `>= 44` (e.g. `greaterThanOrEqualTo(44.0)`). Keep the existing tap-and-dismiss assertion intact — do not add a new test file.</action>
  <verify>
    <automated>cd mobile && flutter test test/features/location/low_battery_banner_test.dart</automated>
  </verify>
  <done>`visualDensity: VisualDensity.compact,` no longer appears in `low_battery_banner.dart`; the widget test passes with the new `>= 44x44` tap-target assertion and the existing copy + dismiss assertions.</done>
</task>

</tasks>

<verification>
- `flutter test test/features/location/low_battery_banner_test.dart` passes.
- Grep confirms `VisualDensity.compact` is absent from `low_battery_banner.dart`.
</verification>

<success_criteria>
The dismiss `IconButton` renders at >= 44x44 logical px (platform default), still fires `onDismissed` on tap, and no other property or layout of the banner changed.
</success_criteria>

<output>
Create `.planning/quick/260720-3tm-fix-low-battery-banner-dismiss-button-to/260720-3tm-SUMMARY.md` when done
</output>
