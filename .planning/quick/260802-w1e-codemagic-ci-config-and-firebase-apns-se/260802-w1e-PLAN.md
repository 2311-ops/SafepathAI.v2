---
phase: quick-260802-w1e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - codemagic.yaml
  - docs/EXTERNAL-SETUP.md
  - docs/CONFIGURATION.md
autonomous: true
requirements: [SOS-02, NOTIF-03]
user_setup:
  - service: apple-developer-program
    why: "TestFlight distribution, APNs auth keys, and iOS push all require a paid Apple Developer Program membership. codemagic.yaml is inert without it. This plan produces the config and the runbook; the enrolment itself is human-only."
    dashboard_config:
      - task: "Enrol in the Apple Developer Program (paid, annual) — prerequisite for every iOS step in docs/EXTERNAL-SETUP.md"
        location: "developer.apple.com/programs"
  - service: codemagic
    why: "Developer is on Windows with no macOS host; Codemagic's macOS M2 runners are the build environment codemagic.yaml targets."
    dashboard_config:
      - task: "Create a Codemagic account, connect this repository, register the App Store Connect API key as an integration, and create the environment variable groups codemagic.yaml references"
        location: "codemagic.io"
  - service: firebase
    why: "FCM push (03-06) has never been exercised against a real project; both Android and iOS app registrations plus an APNs key are required to unblock 03-06 Task 3."
    env_vars:
      - name: Firebase__ProjectId
        source: "Firebase Console -> Project settings -> General -> Project ID"
      - name: Firebase__CredentialsPath
        source: "Firebase Console -> Project settings -> Service accounts -> Generate new private key; save the JSON outside the repo and set this to its absolute path"

must_haves:
  truths:
    - "A developer with no Mac can produce a signed iOS build and upload it to TestFlight by running the committed Codemagic workflow"
    - "No credential value appears in any committed file — every secret is referenced by environment-variable group name or by Codemagic integration name"
    - "One discoverable doc lists every external provisioning step needed to unblock 03-06 Task 3, using the exact package/bundle id and the exact backend configuration keys the shipped code actually binds"
    - "That doc states the paid Apple Developer Program prerequisite and the Android-only fallback before any iOS step, so the developer does not discover the blocker mid-setup"
    - "That doc carries a running list of the other one-time external provisioning this project still needs"
  artifacts:
    - path: "codemagic.yaml"
      provides: "Flutter iOS build with automatic code signing + TestFlight upload, plus an optional Android APK workflow"
      contains: "app_store_connect"
    - path: "docs/EXTERNAL-SETUP.md"
      provides: "Human runbook for Firebase, APNs, App Store Connect, and Codemagic provisioning"
      contains: "com.safepath.mobile"
  key_links:
    - from: "codemagic.yaml"
      to: "mobile/"
      via: "workflow-level working_directory pointing at the Flutter project root, which is not the repository root"
      pattern: "working_directory"
    - from: "codemagic.yaml"
      to: "Codemagic App Store Connect API key integration"
      via: "integrations.app_store_connect referencing the key by its UI name, with publishing auth set to integration"
      pattern: "integrations"
    - from: "docs/EXTERNAL-SETUP.md"
      to: "backend/src/SafePath.Infrastructure/DependencyInjection.cs"
      via: "documents the Firebase:ProjectId / Firebase:CredentialsPath keys the DI container actually reads"
      pattern: "Firebase__ProjectId"
---

<objective>
Unblock the pending 03-06 Task 3 verification todo by producing the two artifacts the developer cannot obtain from inside this repo today: a Codemagic CI configuration that builds and ships the Flutter iOS app to TestFlight from a cloud Mac, and a single human runbook covering the Firebase / APNs / App Store Connect / Codemagic provisioning those builds depend on.

Purpose: 03-06's FCM code is complete and committed, but no real push has ever been sent or received. The blocking gate is entirely external provisioning, and the developer is on Windows with no Mac or Xcode — iOS push cannot be verified locally at all. Codemagic's free tier (500 build minutes/month on macOS M2 runners, no card required to start) is the chosen path, not GitHub Actions.

Output: `codemagic.yaml` at the repository root and `docs/EXTERNAL-SETUP.md`, plus a single cross-reference line in `docs/CONFIGURATION.md` so the runbook is discoverable from the doc a developer already reads for configuration.

Explicit non-goals: creating any external account, project, or key (all human-only, documented not performed); any application code change; any change to `mobile/ios/Runner/Runner.entitlements` or `mobile/ios/Runner/Info.plist` — the Push Notifications entitlement and the remote-notification background mode are already committed and correct, so no Xcode GUI action is needed for those two capabilities.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/03-sos-fast-path/03-06-PLAN.md
@.planning/todos/pending/2026-08-02-provision-firebase-apns-and-verify-fcm-push-deep-link.md
@docs/CONFIGURATION.md
@.planning/phases/02-real-time-location-history-privacy/02-01-USER-SETUP.md
</context>

<facts_verified_this_session>
These were read from the repository during planning. Treat them as ground truth; do not re-derive or contradict them.

| Fact | Source |
|------|--------|
| Flutter project root is `mobile/`, not the repository root | `mobile/pubspec.yaml` |
| Android `applicationId` and `namespace` are both `com.safepath.mobile` | `mobile/android/app/build.gradle.kts` |
| iOS `PRODUCT_BUNDLE_IDENTIFIER` for the Runner target is `com.safepath.mobile` | `mobile/ios/Runner.xcodeproj/project.pbxproj` |
| iOS `IPHONEOS_DEPLOYMENT_TARGET` is `13.0` | `mobile/ios/Runner.xcodeproj/project.pbxproj` |
| `mobile/ios/Runner/Runner.entitlements` exists and sets `aps-environment` to `development` | file contents |
| `mobile/ios/Podfile` does **not** exist — CocoaPods has never run for this project | directory listing |
| The Google Services Gradle plugin is already declared at `4.4.4` (root, `apply false`) and applied in the app module | `mobile/android/build.gradle.kts`, `mobile/android/app/build.gradle.kts` |
| `firebase_core ^4.12.1`, `firebase_messaging ^16.4.3`, `flutter_local_notifications ^22.2.0` | `mobile/pubspec.yaml` |
| `mobile/env.json`, `mobile/android/app/google-services.json`, and `mobile/ios/Runner/GoogleService-Info.plist` are all gitignored | `mobile/.gitignore` lines 37, 44, 45 |
| The backend binds `Firebase:ProjectId` and `Firebase:CredentialsPath` — **not** `FIREBASE_PROJECT_ID` / `GOOGLE_APPLICATION_CREDENTIALS` as 03-06's `user_setup` block claimed | `backend/src/SafePath.Infrastructure/DependencyInjection.cs`, `backend/src/SafePath.Infrastructure/Push/FirebaseOptions.cs` |
| Twilio binds `Twilio:AccountSid`, `Twilio:AuthToken`, `Twilio:FromNumber`, `Twilio:StatusCallbackUrl` | `backend/src/SafePath.Infrastructure/DependencyInjection.cs` |
| Both push and SMS fall back to a logging implementation when unconfigured, and log which sender is active at startup | same file |
| `docs/` already holds `ARCHITECTURE.md`, `CONFIGURATION.md`, `DEVELOPMENT.md`, `GETTING-STARTED.md`, `TESTING.md` — SCREAMING-KEBAB naming, no `SETUP.md` and no existing external-provisioning doc | directory listing |
| The only existing user-setup artifact is `.planning/phases/02-real-time-location-history-privacy/02-01-USER-SETUP.md`, which is a phase-scoped planning artifact, not a living repo doc | file contents |
| Mobile builds are run as `flutter run/build --dart-define-from-file=env.json --dart-define=API_BASE_URL=...` | `docs/CONFIGURATION.md`, `docs/DEVELOPMENT.md` |
| Python 3.13 with PyYAML is available on this machine for YAML validation | verified by invocation |

**Naming decision:** the runbook goes to `docs/EXTERNAL-SETUP.md`. `docs/` is the established location for living project docs and uses SCREAMING-KEBAB filenames; the content is external third-party service provisioning, which is a different concern from `docs/CONFIGURATION.md` (local env files and their keys). No pre-existing `SETUP.md` convention exists to follow, and `02-01-USER-SETUP.md` is a phase artifact under `.planning/`, not a repo doc, so it is referenced rather than imitated.
</facts_verified_this_session>

<tasks>

<task type="auto">
  <name>Task 1: Codemagic workflow for iOS TestFlight (primary) and Android APK (optional)</name>
  <files>codemagic.yaml</files>
  <read_first>
    - mobile/pubspec.yaml (project name `mobile`, version `1.0.0+1`, Dart SDK `^3.12.2`, the firebase_core/firebase_messaging/flutter_local_notifications entries)
    - mobile/android/app/build.gradle.kts (applicationId, Java 17 compile options, and the already-applied Google Services plugin)
    - mobile/.gitignore (confirm `env.json`, `android/app/google-services.json`, `ios/Runner/GoogleService-Info.plist` are excluded — every one of these must be materialised by the workflow before any build step)
    - docs/CONFIGURATION.md (the `--dart-define-from-file=env.json --dart-define=API_BASE_URL=...` invocation the workflow must reproduce)
  </read_first>
  <action>
Create `codemagic.yaml` at the repository root. Codemagic discovers this file in the repository root by default; the Flutter project lives one level down, so every workflow sets the workflow-level `working_directory` key to `mobile` and all script paths are then relative to `mobile/`.

Open the file with a short comment header stating: what it builds, that the Flutter root is `mobile/`, that every credential is supplied through Codemagic environment-variable groups or the App Store Connect integration and never committed, and that `docs/EXTERNAL-SETUP.md` is the provisioning runbook for the variables and integrations named below.

Top-level structure is a single `workflows` mapping with two workflow ids: `ios-testflight` and `android-apk`.

**Workflow `ios-testflight`** — the primary deliverable.

Set `name` to a human label naming iOS TestFlight, `instance_type` to `mac_mini_m2`, `max_build_duration` to 60, and `working_directory` to `mobile`.

Deliberately omit any `triggering` block. Both workflows are manual-start only: the free tier grants 500 build minutes per month, and an automatic push trigger on a repository where most commits touch backend or planning files would burn that budget on builds nobody asked for. Add a comment saying exactly this, and noting that a `triggering` block with `events` and `branch_patterns` can be added later once the pipeline is proven.

Under `environment`:
- `flutter` set to `stable`, `xcode` set to `latest`, `cocoapods` set to `default`.
- `ios_signing` with `distribution_type` set to `app_store` and `bundle_identifier` set to `com.safepath.mobile`. This is Codemagic's automatic code signing: combined with the App Store Connect integration below, Codemagic fetches or creates the signing certificate and provisioning profile itself. No certificate, key, or profile file is committed or referenced by path.
- `groups` listing two environment-variable group names: one for the mobile runtime configuration and one for the Firebase config payloads. Name them descriptively and use the same names verbatim in the runbook of Task 2 so the UI setup and the yaml cannot drift.
- `vars` declaring `APP_STORE_APPLE_ID` with a clearly-marked placeholder value and an inline comment that it is the numeric Apple ID of the app record in App Store Connect, obtained only after the app record exists, and that it may alternatively be moved into a secure group.

Add an `integrations` block with `app_store_connect` set to the name given to the App Store Connect API key in the Codemagic UI. Use a stable, obvious name and reuse that exact string in Task 2's runbook. This is a reference to a key stored in Codemagic, never key material.

Under `scripts`, in this order, each with a `name`:
1. Materialise the gitignored configuration files from secure environment variables. Write the mobile dart-define file to `env.json` from a variable holding its JSON contents, and decode a base64 variable into `ios/Runner/GoogleService-Info.plist`. Guard each with a check that the variable is non-empty and fail the build with an explicit message naming the missing variable and pointing at `docs/EXTERNAL-SETUP.md` if it is not set — a silent empty plist produces an iOS build that installs fine and then never receives a push, which is the exact failure this whole exercise exists to eliminate.
2. `flutter pub get`.
3. `flutter precache --ios` followed by a CocoaPods install in the `ios` directory. Add a comment noting that `mobile/ios/Podfile` is not committed and is generated by the Flutter tool on first iOS build, so this step both creates and consumes it.
4. Apply the fetched signing profiles to the Xcode project by running `xcode-project use-profiles`. Note in a comment that this step writes the export options plist that the build step consumes.
5. Build the IPA. Run `flutter build ipa --release`, passing `--dart-define-from-file=env.json`, `--dart-define=API_BASE_URL=$API_BASE_URL`, `--build-name=1.0.0`, a `--build-number` computed as one greater than the value returned by `app-store-connect get-latest-testflight-build-number "$APP_STORE_APPLE_ID"`, and `--export-options-plist` pointing at the export options plist that `xcode-project use-profiles` produced in the builder's home directory. Comment that TestFlight rejects a build whose number is not strictly greater than every previous upload, which is why the number is derived from App Store Connect rather than from `pubspec.yaml`.

Add `artifacts` collecting the built IPA, the Xcode build logs, and the Flutter build directory's dSYM output so a crash on a tester's device is symbolicatable.

Add `publishing` with an `app_store_connect` block: `auth` set to `integration`, `submit_to_testflight` set to `true`, and a commented-out `beta_groups` list showing how to target a named internal tester group once one exists. Add an `email` sub-block with a `recipients` list containing a placeholder address and `notify` configured to report on both success and failure.

**Workflow `android-apk`** — secondary and optional.

Set `name` to a human label marking it as optional, `instance_type` to `linux_x2`, `max_build_duration` to 30, and `working_directory` to `mobile`. Omit `triggering` here too.

Add a comment stating plainly that this workflow exists only so one pipeline covers both platforms after a single Firebase provisioning pass, that Android needs no cloud Mac and is faster to verify in a local emulator, and that it should be left unrun unless a clean-machine reproduction is actually wanted. Add a second comment noting that if the account's plan restricts instance types, switching `instance_type` to `mac_mini_m2` is the fallback.

Under `environment`: `flutter` set to `stable`, `java` set to `17` (matching the app module's Java 17 source and target compatibility), and `groups` listing the same mobile-configuration group plus the Firebase group.

Under `scripts`: the same guarded materialisation step, writing `env.json` and decoding a base64 variable into `android/app/google-services.json`; then `flutter pub get`; then `flutter build apk --debug` with `--dart-define-from-file=env.json` and `--dart-define=API_BASE_URL=$API_BASE_URL`. Use the debug variant deliberately and comment why: the release build type is still signed with the debug keystore (see the app module's `buildTypes` block), so a release APK here would carry no more trust than a debug one and would only mask that gap.

Add `artifacts` collecting the APK output and the Gradle build log.

Constraints that apply to the whole file: no credential, key, token, issuer id, team id, or account identifier may appear as a literal value — every one is either an environment-variable reference, a group name, or an integration name. Do not reference any `.p8`, `.p12`, or keystore file by path. Do not add, remove, or modify any file other than `codemagic.yaml`.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('codemagic.yaml',encoding='utf-8')); w=d['workflows']; assert set(w)=={'ios-testflight','android-apk'}, w.keys(); assert all(v['working_directory']=='mobile' for v in w.values()); i=w['ios-testflight']; assert i['environment']['ios_signing']['bundle_identifier']=='com.safepath.mobile'; assert i['integrations']['app_store_connect']; assert i['publishing']['app_store_connect']['submit_to_testflight'] is True; assert i['publishing']['app_store_connect']['auth']=='integration'; assert 'triggering' not in i and 'triggering' not in w['android-apk']; print('codemagic.yaml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated command above prints `codemagic.yaml OK` and exits 0.
    - `grep -c 'working_directory: mobile' codemagic.yaml` returns 2.
    - `grep -c 'com.safepath.mobile' codemagic.yaml` returns at least 1.
    - `grep -c 'dart-define-from-file=env.json' codemagic.yaml` returns 2.
    - `grep -c 'get-latest-testflight-build-number' codemagic.yaml` returns at least 1.
    - `grep -c 'GoogleService-Info.plist' codemagic.yaml` returns at least 1 and `grep -c 'google-services.json' codemagic.yaml` returns at least 1.
    - `grep -c 'EXTERNAL-SETUP.md' codemagic.yaml` returns at least 1.
    - `grep -c 'BEGIN PRIVATE KEY' codemagic.yaml` returns 0 — no key material is embedded.
    - `git status --porcelain` shows `codemagic.yaml` as the only added or modified file for this task.
  </acceptance_criteria>
  <done>`codemagic.yaml` parses as valid YAML, defines exactly the two workflows both rooted at `mobile/`, builds and publishes an `app_store`-signed IPA to TestFlight through the App Store Connect integration with an auto-incremented build number, materialises every gitignored config file from secure variables with an explicit failure message when one is missing, and contains no literal credential value.</done>
</task>

<task type="auto">
  <name>Task 2: External provisioning runbook and its cross-reference</name>
  <files>docs/EXTERNAL-SETUP.md, docs/CONFIGURATION.md</files>
  <read_first>
    - docs/CONFIGURATION.md (read in full: heading depth, the two-column `| Key | Purpose |` table style, the fenced `text` blocks used for file paths, and the PowerShell-flavoured command examples — match this voice exactly)
    - docs/GETTING-STARTED.md (the prerequisite-then-steps ordering this doc should mirror)
    - .planning/phases/03-sos-fast-path/03-06-PLAN.md (Task 3's ten-step verification script, and the `user_setup` block — note its env-var names are superseded by the verified names in this plan's facts table)
    - .planning/phases/02-real-time-location-history-privacy/02-01-USER-SETUP.md (the OpenStreetMap production tile-provider item, which belongs in the still-outstanding list)
    - The facts table in this plan — every id, key name, and version stated there is verified; do not restate anything not verified there without checking the file first
  </read_first>
  <action>
Create `docs/EXTERNAL-SETUP.md`. Match `docs/CONFIGURATION.md`'s register precisely: a single H1, H2 sections, two-column tables, fenced `text` blocks for bare file paths, fenced `powershell` blocks for commands, no emoji, no exclamation marks, plain declarative sentences.

Open with a one-paragraph statement of scope: this doc covers one-time provisioning of third-party services that live outside the repository. Local configuration file contents and their keys stay in `docs/CONFIGURATION.md`; link to it. State that nothing here is required to build, test, or demo the project — the backend falls back to logging implementations for both push and SMS when unconfigured — and that it is required only to exercise real push delivery and to ship an iOS build.

Section: **Prerequisites and costs.** Put this first, before any step. State plainly that iOS push and TestFlight both require a paid Apple Developer Program membership (annual fee, enrolment at `developer.apple.com/programs`), that there is no free path to an APNs authentication key or a TestFlight build, and that Codemagic's free tier grants 500 build minutes per month on macOS M2 runners with no payment card required to start. Add an explicit fallback paragraph: Firebase Cloud Messaging on Android is entirely free and needs no Apple account, so the 03-06 verification can be completed on Android alone and iOS push verification deferred — say which of the later verification steps are Android-only in that case. Finish with a short table of what each item costs and what it unlocks.

Section: **Firebase project and app registration.** Numbered steps to create or reuse a Firebase project, register the Android app using the package name `com.safepath.mobile`, download `google-services.json` to `mobile/android/app/`, register the iOS app using the bundle identifier `com.safepath.mobile`, and download `GoogleService-Info.plist` to `mobile/ios/Runner/`. State that both files are gitignored by design and must never be committed, and that the Google Services Gradle plugin is already wired at version 4.4.4 so no Gradle edit is needed once the JSON is in place. Give the console path for each step in the `Firebase Console -> ... -> ...` arrow style that `.planning` artifacts in this repo already use.

Section: **APNs authentication key.** Steps to generate an APNs key in the Apple Developer portal under Certificates, Identifiers and Profiles, download the private key file exactly once (it cannot be re-downloaded), record the Key ID and the Team ID from Membership details, and upload the key with those two ids in the Firebase console under Project settings, Cloud Messaging, Apple app configuration. State explicitly that iOS push silently never arrives without this and that no error surfaces anywhere to explain it — this is the single most common FCM-on-iOS failure. Note that the Push Notifications capability and the remote-notification background mode are already committed in `mobile/ios/Runner/Runner.entitlements` and `mobile/ios/Runner/Info.plist`, so no Xcode GUI action is required for either, superseding the corresponding line in 03-06's `user_setup` block.

Section: **Backend configuration keys.** A two-column table matching `docs/CONFIGURATION.md`'s style listing `Firebase__ProjectId` and `Firebase__CredentialsPath` with their sources, alongside the already-live `Twilio__AccountSid`, `Twilio__AuthToken`, `Twilio__FromNumber`, and `Twilio__StatusCallbackUrl`. Explain the double-underscore to colon mapping the way `docs/CONFIGURATION.md` already does. Add a correction note stating that 03-06's `user_setup` block named `FIREBASE_PROJECT_ID` and `GOOGLE_APPLICATION_CREDENTIALS`, but the shipped `FirebaseOptions` binding reads `Firebase:ProjectId` and `Firebase:CredentialsPath`, and the shipped names are authoritative. State that the service-account JSON must be saved outside the repository and referenced by absolute path. State the observable success signal: on restart the API logs that the Firebase push sender is active rather than the logging push sender, and say the same for the Twilio versus logging SMS gateway.

Section: **App Store Connect API key.** Steps to create the app record (recording its numeric Apple ID, which `codemagic.yaml` needs as `APP_STORE_APPLE_ID`), then generate a team API key under Users and Access, Integrations, App Store Connect API, with an access role sufficient to upload builds. Record the Issuer ID and Key ID and download the private key file once.

Section: **Codemagic setup.** Steps to create the account, connect this repository, and add the application. Then: register the App Store Connect API key as an integration under the team or personal account integrations, naming it with the exact string `codemagic.yaml` references — quote that string. Then a table of every environment variable the two workflows read: variable name, which group it belongs to, whether it is secure, what its value is, and how to produce that value. Cover the `env.json` contents variable, the API base URL, and the two base64-encoded Firebase config payloads. Because the developer is on Windows, give the base64 encoding command as a PowerShell one-liner using `[Convert]::ToBase64String([IO.File]::ReadAllBytes(...))`, and note that the API base URL must be a host the build's testers can actually reach, not a loopback or emulator address. Finish with how to start a manual build from the Codemagic UI and where the IPA and logs land, plus a note that both workflows are manual-start by design to protect the monthly minute budget.

Section: **Verifying FCM end to end.** Condense 03-06 Task 3's ten steps into a numbered checklist, preserving the parts that carry the actual signal: two devices, guardian signed in on device B, device B fully backgrounded and then separately fully terminated, SOS held for three seconds on device A, heads-up notification naming the sender, tap opens the SOS Responder screen for that exact session rather than the Live Map or home shell, and device A's delivery chip for device B moving from Queued to Delivered. Link to `.planning/phases/03-sos-fast-path/03-06-PLAN.md` as the authoritative source. Add a short symptom-to-cause troubleshooting table covering at least: nothing arrives on iOS, nothing arrives on Android, notification arrives but the tap lands on the wrong screen, and the delivery chip stays Queued forever.

Section: **Known gotchas.** Each as a short subsection with the symptom, the cause, and the fix. Include at minimum:
- The committed `aps-environment` entitlement value is `development`. A TestFlight or App Store build signed for distribution registers against the APNs sandbox with that value, so production pushes are never delivered. State the fix as a per-configuration entitlements setup so Release carries the production value, flag it as required before any TestFlight push test is trusted, and state that it is intentionally not changed by this work.
- `mobile/ios/Podfile` is not committed and is generated on the first iOS build. A first Codemagic run therefore does more work than later runs, and a `pod install` failure on that first run is expected to be about deployment target or plugin resolution rather than about the workflow itself.
- The iOS deployment target is currently 13.0 while the Firebase iOS SDK pulled in by `firebase_core 4.x` and `firebase_messaging 16.x` may require a higher minimum. If CocoaPods fails on the first build with a deployment-target conflict, the fix is to raise the target in the Xcode project and in the generated Podfile to the version CocoaPods names in its error. Do not assert a specific required version — record it in this doc once the first build reports it.
- Every gitignored config file must exist on the runner before the build, or the build produces an app that installs and then silently never receives a push.

Section: **Still outstanding.** A living checklist table with columns for item, why it is needed, when it becomes blocking, and status. Seed it with, at minimum: an OpenStreetMap production tile-hosting provider before any real-user traffic (referencing `.planning/phases/02-real-time-location-history-privacy/02-01-USER-SETUP.md`, and noting no key is needed for development); an Android release signing keystore, since the release build type is still signed with the debug keystore; the Release-configuration APNs production entitlement from the gotchas section; Twilio account and phone number provisioning for real SMS, marked as code-complete since 03-05 but unprovisioned; the Supabase Storage `avatar` bucket, marked done in 02-13; and the Six Labors ImageSharp license required at backend build time, also from 02-13. Mark each row done or outstanding accurately based on the facts table and the linked artifacts. State at the top of the section that this list is appended to as new external dependencies appear, and that it is the reason this doc lives in `docs/` rather than under a phase directory.

Finally, edit `docs/CONFIGURATION.md`: add exactly one cross-reference line pointing to `docs/EXTERNAL-SETUP.md` for third-party service provisioning, placed where a reader looking for configuration would naturally hit it. One line only — do not restructure, reword, or otherwise revise that file.
  </action>
  <verify>
    <automated>python -c "import io; t=io.open('docs/EXTERNAL-SETUP.md',encoding='utf-8').read(); req=['Prerequisites','Firebase','APNs','App Store Connect','Codemagic','Still outstanding','com.safepath.mobile','Firebase__ProjectId','Firebase__CredentialsPath','Twilio__AccountSid','aps-environment','Podfile']; miss=[r for r in req if r not in t]; assert not miss, miss; assert t.count('com.safepath.mobile')>=2; assert len(t.splitlines())>80; c=io.open('docs/CONFIGURATION.md',encoding='utf-8').read(); assert 'EXTERNAL-SETUP.md' in c; print('EXTERNAL-SETUP.md OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated command above prints `EXTERNAL-SETUP.md OK` and exits 0.
    - `grep -c 'Firebase__ProjectId' docs/EXTERNAL-SETUP.md` returns at least 1 and `grep -c 'GOOGLE_APPLICATION_CREDENTIALS' docs/EXTERNAL-SETUP.md` returns at least 1 — the superseded name is named only inside the correction note, so a reader who arrives from 03-06's `user_setup` block finds the correction instead of a contradiction.
    - `grep -c 'developer.apple.com/programs' docs/EXTERNAL-SETUP.md` returns at least 1 — the paid prerequisite is stated with its source.
    - `grep -ci 'android' docs/EXTERNAL-SETUP.md` returns at least 3 — the Android-only fallback path is genuinely described, not mentioned once.
    - Every environment variable name and every group name referenced in `codemagic.yaml` appears verbatim in `docs/EXTERNAL-SETUP.md`. Verify by extracting them from the yaml and grepping the doc for each; there must be no unexplained variable.
    - `git diff --stat docs/CONFIGURATION.md` shows exactly one line added and zero lines removed.
    - `git status --porcelain` shows only `docs/EXTERNAL-SETUP.md` and `docs/CONFIGURATION.md` changed for this task.
  </acceptance_criteria>
  <done>`docs/EXTERNAL-SETUP.md` exists in the repo's established docs location and voice, leads with the paid-Apple-Developer prerequisite and the free Android-only fallback, gives correct console paths for Firebase app registration and APNs key upload against `com.safepath.mobile`, documents the backend configuration keys the shipped code actually binds while explicitly correcting 03-06's superseded names, explains every Codemagic variable and integration `codemagic.yaml` references, condenses the 03-06 verification script with a troubleshooting table, records the `aps-environment` and Podfile gotchas without changing either file, and carries a seeded running list of outstanding external provisioning. `docs/CONFIGURATION.md` gains one cross-reference line and nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → Codemagic runner | A committed config file instructs a third-party cloud macOS host to fetch signing material and produce a distributable build. |
| Codemagic runner → App Store Connect | Build upload authenticated by an App Store Connect API key held in Codemagic. |
| documentation → operator | A runbook instructs a human where to place private key material and service-account credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-W1E-01 | Information Disclosure | `codemagic.yaml` | high | mitigate | No credential value is written as a literal. Signing material is fetched by Codemagic's automatic `ios_signing` from the App Store Connect integration, referenced by name only; all other values are environment-variable references resolved from secure groups. Enforced by this plan's negative grep for embedded key material and by the review of the yaml diff. |
| T-W1E-02 | Information Disclosure | Firebase config payloads in CI | medium | mitigate | `google-services.json` and `GoogleService-Info.plist` stay gitignored and are supplied as secure, base64-encoded Codemagic variables materialised at build time, so neither is ever committed nor printed. |
| T-W1E-03 | Information Disclosure | `docs/EXTERNAL-SETUP.md` | medium | mitigate | The runbook records only where credentials come from and where they go, never a value. The Firebase service-account JSON is documented as living outside the repository and referenced by absolute path; the APNs and App Store Connect private key files are documented as download-once artifacts stored outside the repository. |
| T-W1E-04 | Tampering | build artifact provenance | medium | accept | Builds run on a third-party host with access to app-store signing material. Accepted: this is inherent to any hosted-Mac CI, it is the only path available without a macOS host, and the blast radius is a graduation-project app record. Revocable at any time by deleting the App Store Connect API key. |
| T-W1E-05 | Repudiation | false verification confidence | high | mitigate | The `aps-environment: development` gotcha is documented as blocking, so a TestFlight build that receives no production push is diagnosed rather than misread as a code defect; the runbook states the observable startup-log signal that distinguishes the real Firebase sender from the logging fallback, so nobody concludes push works while the no-op sender is active. |
</threat_model>

<verification>
1. `python -c "import yaml; yaml.safe_load(open('codemagic.yaml',encoding='utf-8'))"` parses without error.
2. Both task-level automated checks pass.
3. `git diff --stat` shows exactly three paths touched: `codemagic.yaml`, `docs/EXTERNAL-SETUP.md`, `docs/CONFIGURATION.md`. No file under `mobile/ios/`, `mobile/android/`, `backend/`, or `mobile/lib/` is modified.
4. Cross-check by hand: every environment-variable name, group name, and integration name in `codemagic.yaml` is explained in `docs/EXTERNAL-SETUP.md`, and every name the doc tells the developer to create appears in `codemagic.yaml`. A mismatch here is the single most likely way this pair fails in practice.
</verification>

<success_criteria>
- A developer on Windows can follow `docs/EXTERNAL-SETUP.md` end to end and reach a TestFlight build without needing to infer any step or guess any name.
- The runbook states the paid Apple Developer Program prerequisite and the free Android-only fallback before asking for any iOS work.
- The backend configuration keys documented are the ones the shipped `FirebaseOptions` binding actually reads, with 03-06's superseded names explicitly corrected rather than silently contradicted.
- `codemagic.yaml` is valid YAML defining a manual-start iOS TestFlight workflow and an optional Android workflow, both rooted at `mobile/`, with zero literal credentials.
- The pending todo `2026-08-02-provision-firebase-apns-and-verify-fcm-push-deep-link.md` becomes actionable by a human with no further investigation.
- No application code, no iOS entitlements or Info.plist, and no external account are touched.
</success_criteria>

<output>
Create `.planning/quick/260802-w1e-codemagic-ci-config-and-firebase-apns-se/260802-w1e-SUMMARY.md` when done.
</output>