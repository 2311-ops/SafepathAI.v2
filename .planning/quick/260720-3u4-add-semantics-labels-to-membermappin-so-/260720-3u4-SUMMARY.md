---
phase: quick-260720-3u4
plan: 01
subsystem: accessibility
tags: [flutter, semantics, widget-test]

requires:
  - phase: 02-real-time-location-history-privacy
    provides: MemberMapPin self-summary pin on the Live Map
provides:
  - MemberMapPin exposes one clean screen-reader label combining the member label and status phrase
  - Fresh pins announce "<label>, current location"
  - Stale pins announce "<label>, Last seen X min ago"
affects: [live-map, accessibility]

tech-stack:
  added: []
  patterns:
    - "Use Semantics(container: true, excludeSemantics: true) to collapse decorative child text into one intentional accessibility label for non-interactive status widgets"

key-files:
  created:
    - mobile/test/shared_widgets/member_map_pin_semantics_test.dart
  modified:
    - mobile/lib/shared_widgets/member_map_pin.dart

key-decisions:
  - "MemberMapPin is a status/identity widget, not a button; no button role was added."
  - "The label is derived from widget.label plus band.badgeText when stale, otherwise the fallback phrase 'current location'."
  - "LiveMemberMarker was left untouched because it already owns its separate interactive semantics contract."

requirements-completed: ["260720-3u4"]

coverage:
  - id: D1
    description: "Fresh MemberMapPin announces the label plus current-location status as one node"
    requirement: "260720-3u4"
    verification:
      - kind: unit
        ref: "mobile/test/shared_widgets/member_map_pin_semantics_test.dart#announces the label and current-location status as one node"
        status: not_run
        note: "Flutter SDK was not available on PATH in this shell."
    human_judgment: false
  - id: D2
    description: "Stale MemberMapPin announces the label plus staleness badge text as one node"
    requirement: "260720-3u4"
    verification:
      - kind: unit
        ref: "mobile/test/shared_widgets/member_map_pin_semantics_test.dart#announces the label and stale status as one node"
        status: not_run
        note: "Flutter SDK was not available on PATH in this shell."
    human_judgment: false

duration: ~15min
completed: 2026-07-24
status: complete
---

# Quick Task 260720-3u4: MemberMapPin Semantics Summary

**MemberMapPin now exposes a single screen-reader label that combines the visible member label with either `current location` or the existing staleness badge text.**

## Accomplishments

- Wrapped the existing `MemberMapPin` visual column in `Semantics(container: true, excludeSemantics: true, label: ...)`.
- Derived the label from `widget.label` and `band.badgeText ?? 'current location'`.
- Kept the widget non-interactive for assistive tech by not setting a button role.
- Added focused widget tests for fresh and stale accessibility labels.
- Left `LiveMemberMarker`, constructor parameters, dependencies, and existing pulse logic unchanged.

## Task Commit

1. **Task 1: Add MemberMapPin semantics label and tests** - `7246356` (feat)

## Files Created/Modified

- `mobile/lib/shared_widgets/member_map_pin.dart` - Adds the computed `Semantics` label around the existing visual pin.
- `mobile/test/shared_widgets/member_map_pin_semantics_test.dart` - Adds fresh and stale semantics-label coverage.

## Verification

Attempted but blocked in this shell:

- `dart format lib/shared_widgets/member_map_pin.dart test/shared_widgets/member_map_pin_semantics_test.dart` - blocked: `dart` not found on PATH.
- `flutter test test/shared_widgets/member_map_pin_semantics_test.dart` - blocked: `flutter` not found on PATH.

## Deviations from Plan

- The plan referenced `mobile/test/shared_widgets/member_map_pin_pulse_test.dart`, but that file is not present on this branch. The new test follows the existing `avatar_cache_key_test.dart` conventions instead.
- The plan noted a prior nullable pulse-controller refactor, but this branch's `MemberMapPin` still has the older always-on pulse controller. The task did not alter that logic.

## User Setup Required

- Run the focused Flutter test after opening a terminal where Flutter is available on PATH:

```powershell
cd mobile
flutter test test/shared_widgets/member_map_pin_semantics_test.dart
```

---
*Plan: quick-260720-3u4*
*Completed: 2026-07-24*
