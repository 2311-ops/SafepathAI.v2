---
phase: quick-260820-53n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/auth/presentation/welcome_screen.dart
  - mobile/lib/features/sos/presentation/emergency_contacts_screen.dart
  - mobile/lib/features/geofencing/presentation/zone_activity_screen.dart
autonomous: true
requirements: [QUICK-SVG-WIRE-REMAINING]

must_haves:
  truths:
    - "flutter_svg remains a resolved pubspec dependency and assets/illustrations/ remains registered under flutter: assets: (both added in prior quick task 260820-3tj) so the three already-on-disk SVG files (welcome-background.svg, emergency-contacts.svg, zone-activity-empty.svg) load at runtime without a MissingAssetError."
    - "WelcomeScreen renders welcome-background.svg as a full-bleed backdrop behind its existing SafeArea content (including the untouched hero-safety.svg from the prior quick task); auth_flow_navigation_test.dart's Welcome-screen entry points still pass unmodified."
    - "EmergencyContactsScreen's empty state (_EmptyContactsCard) renders emergency-contacts.svg above its existing two Text widgets; emergency_contacts_screen_test.dart's empty-state test still passes unmodified."
    - "ZoneActivityScreen's empty state (_EmptyState) renders zone-activity-empty.svg in place of the stock Icons.timeline_outlined; zone_activity_screen_test.dart's empty/error-state test still passes unmodified."
    - "flutter analyze is clean (zero issues) across the mobile package; no SVG asset file's content is altered under any circumstances."
    - "git diff --name-only shows only the 3 target Dart files changed (plus pubspec.yaml/pubspec.lock only if Task 1 finds something actually missing, which is not expected)."
  artifacts:
    - file: mobile/lib/features/auth/presentation/welcome_screen.dart
      contains: "welcome-background.svg"
    - file: mobile/lib/features/sos/presentation/emergency_contacts_screen.dart
      contains: "emergency-contacts.svg"
    - file: mobile/lib/features/geofencing/presentation/zone_activity_screen.dart
      contains: "zone-activity-empty.svg"
  key_links:
    - from: "WelcomeScreen's gradient Container's child (currently a bare SafeArea)"
      to: "assets/illustrations/welcome-background.svg"
      via: "Stack wrapping the Container's child: Positioned.fill(child: SvgPicture.asset(..., fit: BoxFit.cover)) as the first Stack child, the existing SafeArea subtree unchanged as the second"
      pattern: "welcome-background.svg"
    - from: "_EmptyContactsCard's Column (emergency_contacts_screen.dart)"
      to: "assets/illustrations/emergency-contacts.svg"
      via: "SvgPicture.asset(..., width: 130) inserted as the new first Column child, followed by SizedBox(height: AppSpacing.md), before the two existing Text children"
      pattern: "emergency-contacts.svg"
    - from: "_EmptyState's Column (zone_activity_screen.dart)"
      to: "assets/illustrations/zone-activity-empty.svg"
      via: "direct replacement of Icon(Icons.timeline_outlined, size: 48) with SvgPicture.asset(..., width: 110)"
      pattern: "zone-activity-empty.svg"
---

<objective>
Wire the three already-created, already-on-disk SVG illustrations (`welcome-background.svg`,
`emergency-contacts.svg`, `zone-activity-empty.svg`) into their designated spots in the SafePath AI
Flutter app: a full-bleed backdrop on the Welcome screen, the Emergency Contacts empty state, and the
Zone Activity empty state.

Purpose: This is a follow-up to quick task 260820-3tj, which wired 3 other illustrations
(`hero-safety`, `family-connect`, `safe-zone-empty`) and already added `flutter_svg` +
`assets/illustrations/` to `mobile/pubspec.yaml`. The orchestrator has already written these 3 new
SVG files verbatim to their final destination on disk, before this planning step started — they are
final, pre-approved, read-only content. This plan is pure wiring: reference the existing files from
Dart widget code, with zero regeneration, rewriting, or "improvement" of their artistic content.

Output: Three presentation files (`welcome_screen.dart`, `emergency_contacts_screen.dart`,
`zone_activity_screen.dart`) rendering the three illustrations at their identified spots, with every
other line of each file — and every other screen in the app — left untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@mobile/pubspec.yaml
@mobile/lib/features/auth/presentation/welcome_screen.dart
@mobile/lib/features/sos/presentation/emergency_contacts_screen.dart
@mobile/lib/features/geofencing/presentation/zone_activity_screen.dart
</context>

<interface_contracts>
Verified by reading the source during planning — reuse verbatim, do not reinvent:

- The 3 target SVG files already exist on disk with the expected viewBox dimensions (confirmed via
  directory listing, content not opened per the read-only mandate):
  `mobile/assets/illustrations/welcome-background.svg` (viewBox 0 0 390 844),
  `mobile/assets/illustrations/emergency-contacts.svg` (viewBox 0 0 280 220),
  `mobile/assets/illustrations/zone-activity-empty.svg` (viewBox 0 0 260 200).
- `mobile/pubspec.yaml` already has `flutter_svg: ^2.3.0` under `dependencies:` and
  `assets: - assets/illustrations/` under the `flutter:` section (both added in quick task 260820-3tj)
  — confirmed present during planning. No pubspec change is expected.
- `WelcomeScreen` (`StatelessWidget`) has `body: Container(..., decoration: const BoxDecoration(...),
  child: SafeArea(child: TweenAnimationBuilder<double>(...)))`. The `SafeArea` subtree already contains
  a `SvgPicture.asset('assets/illustrations/hero-safety.svg', width: 160)` call (from 260820-3tj,
  inside the scrollable `Column`, between the tagline and the `PrimaryButton`) — this existing call
  must not be touched. `flutter_svg` is already imported in this file.
- `EmergencyContactsScreen`'s private `_EmptyContactsCard` (`StatelessWidget`) renders a `SafePathCard`
  whose `child` is a `Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text(...),
  SizedBox(height: AppSpacing.xs), Text(...)])`. This file does **not** yet import `flutter_svg`.
- `ZoneActivityScreen`'s private `_EmptyState` (`StatelessWidget`) has a single-expression `build()`
  returning `const Center(child: Padding(padding: EdgeInsets.all(24), child: Column(mainAxisSize:
  MainAxisSize.min, children: [Icon(Icons.timeline_outlined, size: 48), SizedBox(height: 16),
  Text('No zone activity yet'), SizedBox(height: 8), Text('Confirmed arrivals...')])))` — the whole
  tree is one `const` expression. Replacing the `Icon` with a non-const `SvgPicture.asset(...)` call
  breaks `const`-ness for `Center`/`Padding`/`Column`; re-add explicit `const` on the individually
  still-constant leaves (`EdgeInsets.all(24)`, both `SizedBox`s, both `Text`s) so `flutter analyze`
  stays clean under `flutter_lints`' `prefer_const_constructors`. This file does **not** yet import
  `flutter_svg`.
</interface_contracts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify prerequisites (read-only check, fix only if actually missing)</name>
  <files>mobile/pubspec.yaml</files>
  <action>
    Confirm the 3 SVG files exist at these exact paths — check existence only, do not open, read, or
    alter their content in any way:
    - mobile/assets/illustrations/welcome-background.svg
    - mobile/assets/illustrations/emergency-contacts.svg
    - mobile/assets/illustrations/zone-activity-empty.svg

    Confirm `mobile/pubspec.yaml` still lists `flutter_svg` under `dependencies:` and still lists
    `assets/illustrations/` under the `flutter: assets:` block (both were added by the prior quick
    task 260820-3tj and are expected to already be correct).

    If both checks pass (expected outcome), make no changes to `pubspec.yaml` — do not touch it. Only
    if something is verifiably missing, add the minimal fix (the `flutter_svg: ^2.3.0` dependency line
    and/or the `assets/illustrations/` entry) and run `flutter pub get` inside `mobile/`; note this
    deviation explicitly in the SUMMARY.
  </action>
  <verify>
    <automated>cd mobile && test -f assets/illustrations/welcome-background.svg && test -f assets/illustrations/emergency-contacts.svg && test -f assets/illustrations/zone-activity-empty.svg && grep -c 'flutter_svg' pubspec.yaml && grep -c 'assets/illustrations/' pubspec.yaml</automated>
  </verify>
  <done>
    All 3 SVG files confirmed to exist on disk (content untouched); `flutter_svg` and
    `assets/illustrations/` confirmed present in `pubspec.yaml`; no changes made unless something was
    genuinely missing.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire welcome-background.svg as a full-bleed Welcome-screen backdrop</name>
  <files>mobile/lib/features/auth/presentation/welcome_screen.dart</files>
  <action>
    In `welcome_screen.dart`, wrap the gradient `Container`'s `child:` in a `Stack`. Currently the
    `Container`'s `child` is directly `SafeArea(child: TweenAnimationBuilder<double>(...))`. Change it
    so `child:` is a `Stack` with exactly two children, in this order:
    1. `Positioned.fill(child: SvgPicture.asset('assets/illustrations/welcome-background.svg', fit:
       BoxFit.cover))`.
    2. The existing `SafeArea(child: TweenAnimationBuilder<double>(...))` subtree, completely
       unchanged — same widget, same content, just now the second child of the `Stack` instead of the
       direct `Container` child.

    `flutter_svg` is already imported in this file (used by the existing `hero-safety.svg` call inside
    the scrollable content) — do not add a duplicate import. Do not touch the gradient colors, the
    `BoxDecoration`, the existing `hero-safety.svg` `SvgPicture.asset` call inside the `Column`, any
    text, or any button. Do not add a `semanticsLabel` to the new backdrop `SvgPicture` — it is purely
    decorative and should stay out of the accessibility tree.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/auth/presentation/welcome_screen.dart && flutter test test/core/router/auth_flow_navigation_test.dart</automated>
  </verify>
  <done>
    `WelcomeScreen`'s gradient `Container` renders `welcome-background.svg` as a `Positioned.fill`
    backdrop behind the unchanged `SafeArea` content; `flutter analyze` is clean for this file;
    `auth_flow_navigation_test.dart` passes unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire emergency-contacts.svg into EmergencyContactsScreen's empty state</name>
  <files>mobile/lib/features/sos/presentation/emergency_contacts_screen.dart</files>
  <action>
    Add `import 'package:flutter_svg/flutter_svg.dart';` to `emergency_contacts_screen.dart`.

    In the private `_EmptyContactsCard` widget's `build()`, add
    `SvgPicture.asset('assets/illustrations/emergency-contacts.svg', width: 130)` as the new first
    child of the `Column`, followed by `const SizedBox(height: AppSpacing.md)`, before the existing
    `Text('No emergency contacts yet.', ...)` and its sibling `SizedBox`/`Text`. Do not change any
    text, the `SafePathCard` wrapper, `crossAxisAlignment`, or any other part of this file (the
    add/edit/remove-contact form logic, `_ContactRow`, `_AddContactCard`, `_PhoneInputGroup`, etc. stay
    untouched).
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/sos/presentation/emergency_contacts_screen.dart && flutter test test/features/sos/emergency_contacts_screen_test.dart</automated>
  </verify>
  <done>
    `_EmptyContactsCard` renders `emergency-contacts.svg` above its existing two `Text` widgets;
    `flutter analyze` is clean for this file; `emergency_contacts_screen_test.dart` passes unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire zone-activity-empty.svg into ZoneActivityScreen's empty state</name>
  <files>mobile/lib/features/geofencing/presentation/zone_activity_screen.dart</files>
  <action>
    Add `import 'package:flutter_svg/flutter_svg.dart';` to `zone_activity_screen.dart`.

    In the private `_EmptyState` widget's `build()`, replace only `Icon(Icons.timeline_outlined, size:
    48)` with `SvgPicture.asset('assets/illustrations/zone-activity-empty.svg', width: 110)`. Because
    the current `build()` is a single `const Center(...)` expression and `SvgPicture.asset(...)` is not
    const-constructible, remove the outer `const` from `Center(` and from `Padding(` and `Column(` (so
    the expression compiles), then re-add explicit `const` to every child that is still independently
    constant: `const EdgeInsets.all(24)` for the padding value, `const SizedBox(height: 16)`, `const
    Text('No zone activity yet')`, `const SizedBox(height: 8)`, and `const Text('Confirmed arrivals and
    departures from the last 7 days will appear here.')`. Leave the loading skeleton
    (`_ActivitySkeleton`), the error state (`_ErrorState`), the activity rows (`_ActivityRow`), and
    everything else in the file untouched.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/geofencing/presentation/zone_activity_screen.dart && flutter test test/features/geofencing/zone_activity_screen_test.dart</automated>
  </verify>
  <done>
    `_EmptyState` renders `zone-activity-empty.svg` in place of the stock timeline icon, with all
    still-constant siblings marked `const`; `flutter analyze` is clean for this file;
    `zone_activity_screen_test.dart` passes unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Full-package verification gate and change-scope check</name>
  <files>mobile/</files>
  <action>
    Run `flutter analyze` across the whole `mobile/` package and confirm zero issues.

    Run the three tests exercising the touched screens together in one invocation:
    `test/core/router/auth_flow_navigation_test.dart`, `test/features/sos/emergency_contacts_screen_test.dart`,
    and `test/features/geofencing/zone_activity_screen_test.dart`. All must pass. Do not write, edit, or
    delete any test file.

    Finally, run `git diff --name-only` (and check untracked files via `git status --short` — none are
    expected from this plan) and confirm the change set is exactly the 3 target Dart files declared in
    this plan's `files_modified` frontmatter, plus `mobile/pubspec.yaml`/`mobile/pubspec.lock` only if
    Task 1 found and fixed a genuine gap (not expected). If any other file appears in the diff, scope
    drifted and must be investigated before considering this plan complete — in particular this
    repository currently has unrelated uncommitted changes in the geofencing feature area (see `git
    status` at session start) that must not be conflated with or accidentally re-touched by this plan.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze && flutter test test/core/router/auth_flow_navigation_test.dart test/features/sos/emergency_contacts_screen_test.dart test/features/geofencing/zone_activity_screen_test.dart</automated>
  </verify>
  <done>
    `flutter analyze` reports zero issues across the mobile package; all three targeted test files
    pass; `git diff --name-only` lists exactly the files declared in this plan's `files_modified`
    frontmatter (plus pubspec files only if Task 1 genuinely needed to fix something) and nothing else.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled asset -> `flutter_svg` renderer | Three static, pre-authored SVG files (already written to disk before this plan started) parsed at runtime by the already-resolved `flutter_svg` dependency; no network fetch, no user-supplied SVG. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53N-01 | Tampering | `assets/illustrations/*.svg` content | low | accept | Files are static, pre-approved, and explicitly read-only for this plan — never fetched at runtime, never user-suppliable, no `<script>`/executable content in inline SVG. |
| T-53N-02 | Denial of Service | `SvgPicture.asset` parse/render cost | low | accept | Each file is a few KB of simple shape markup rendered on three non-critical screens (Welcome backdrop, Emergency Contacts empty state, Zone Activity empty state); none sit on the SOS pipeline, location tracking, or geofence-registration hot paths. |
| T-53N-03 | Information Disclosure | Illustration content | low | accept | All three illustrations are generic, non-personalized iconography (constellation pattern, shield/contact icon, dashed journey path) with no user, location, or family data embedded. |
</threat_model>

<verification>
1. `cd mobile && flutter analyze` — zero issues across the whole package.
2. `cd mobile && flutter test test/core/router/auth_flow_navigation_test.dart
   test/features/sos/emergency_contacts_screen_test.dart
   test/features/geofencing/zone_activity_screen_test.dart` — all pass.
3. Read each of the three touched presentation files: each contains an `SvgPicture.asset(...)` call
   referencing its designated asset path at the identified spot.
4. Confirm (via `git status`/directory listing) the 3 SVG files under `mobile/assets/illustrations/`
   are byte-identical to their state at the start of this plan.
5. `git diff --name-only` — matches this plan's `files_modified` frontmatter exactly (no pubspec
   change unless Task 1 found a genuine gap).
</verification>

<success_criteria>
- `welcome-background.svg`, `emergency-contacts.svg`, and `zone-activity-empty.svg` each render at
  their designated spot: Welcome-screen full-bleed backdrop, Emergency Contacts empty state, and Zone
  Activity empty state respectively.
- No SVG asset file's content is altered, regenerated, or "improved" — they are treated as read-only,
  pre-approved final content throughout.
- No other screen, color, token, or layout is touched beyond what's needed to make room for each
  illustration.
- `flutter analyze` is clean and every existing test exercising these three screens still passes
  without any test file being edited.
</success_criteria>

<output>
Create `.planning/quick/260820-53n-wire-3-already-created-svg-illustration-/260820-53n-SUMMARY.md` when done
</output>
