---
phase: quick-260820-73v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart
  - mobile/test/features/geofencing/safe_zone_router_flow_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 'ZONE TYPE AND NAME' section in the Safe Zone editor renders each SafeZoneCategory as an icon-led, category-colored option (not a plain-text ChoiceChip), inside a card matching _LocationPickerPanel's visual style (white surface, 18px radius, hairline border, same shadow, icon+title+subtitle header row)."
    - "Selecting a category still calls controller.selectCategory(category) and then syncs _nameController.text to controller.draft.name, exactly as the current ChoiceChip.onSelected wiring does today."
    - "The zone name field is the shared SafePathTextField widget (not a raw TextField), showing the selected category's icon as its prefixIcon and editor.validation.name as its errorText."
    - "No AppColors.sosRed / SOS-red token appears anywhere in the new section."
    - "The pre-existing uncommitted one-line Sensitivity fix (Text(sensitivity.label) instead of Text(sensitivity.wireValue)) is untouched by this plan's diff."
    - "No other section of edit_safe_zone_screen.dart (_LocationPickerPanel, map picker dialog, Radius, Sensitivity, Notifications) changes."
  artifacts:
    - "mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart — new private _CategoryAndNamePanel widget (card), _CategoryOption widget (per-category chip replacement), and top-level _categoryColor/_categoryIcon helper functions"
  key_links:
    - "_CategoryOption.onTap -> onCategorySelected(category) -> controller.selectCategory(category); _nameController.text = controller.draft.name (identical behavior to today, relocated off ChoiceChip.onSelected)"
    - "SafePathTextField.onChanged -> controller.setName (unchanged wiring, just moved off the raw TextField)"
    - "SafePathTextField.prefixIcon -> _categoryIcon(draft.category) (re-evaluates on every rebuild as the selected category changes)"
---

<objective>
Replace the plain `Text('ZONE TYPE AND NAME', ...)` + `Wrap` of `ChoiceChip`s + raw `TextField` in `SafeZoneEditorScreen` with a colorful, icon-led card that mirrors `_LocationPickerPanel`'s exact visual language (surface card, header icon/title/subtitle row) and the same category color/icon mapping already used on `safe_zones_screen.dart`.

Purpose: The "ZONE TYPE AND NAME" section currently reads as a bare caption label over generic Material `ChoiceChip`s and a raw `TextField` — visually inconsistent with the rest of the editor screen, which uses card-wrapped, icon-led sections throughout. The redesign was scoped and approved in chat (ui-ux-pro-max + flutter-ui guidance); this plan encodes that approved design, it does not re-derive it.
Output: `edit_safe_zone_screen.dart` gains a `_CategoryAndNamePanel` card (mirroring `_LocationPickerPanel`), a `_CategoryOption` widget per `SafeZoneCategory` value, and private `_categoryColor`/`_categoryIcon` helpers matching `safe_zones_screen.dart`'s mapping. Behavior (category selection, name entry, validation) is unchanged — only presentation changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

**CRITICAL — pre-existing uncommitted change to preserve:** `mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart` currently has ONE unrelated uncommitted line in its working tree, inside the "Sensitivity" `RadioGroup` section: `title: Text(sensitivity.label)` (changed from `sensitivity.wireValue`). This belongs to a separate, still-in-progress debug session and is OUT OF SCOPE for this plan. Do not touch the Sensitivity section. Before committing, run `git diff -- mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart` and confirm that line is still present, unmodified, and is the ONLY line outside your own edit region that differs from HEAD.

**Current structure of `edit_safe_zone_screen.dart`** (verify exact line numbers with a fresh Read before editing — they may have shifted):
- Imports (top of file) already include `app_colors.dart`, `app_spacing.dart`, `app_typography.dart`, `family_models.dart`, `location_controller.dart`, `map_geometry.dart`, `vector_map.dart`, `geofence_controller.dart`, `geofence_models.dart`. `shared_widgets/safepath_text_field.dart` is NOT yet imported — add it as `import '../../../shared_widgets/safepath_text_field.dart';` (same `../../../` depth pattern already used by the `core/theme/*` imports from this file's location).
- In `_SafeZoneEditorScreenState.build()`, immediately after the `_LocationPickerPanel(...)` widget and its trailing `const SizedBox(height: AppSpacing.lg)`, the section to replace is: `Text('ZONE TYPE AND NAME', style: AppTypography.caption)`, followed by a `Wrap(spacing: 8, runSpacing: 8, children: [for (final category in SafeZoneCategory.values) ChoiceChip(label: Text(category.wireValue), selected: draft.category == category, onSelected: (_) { controller.selectCategory(category); _nameController.text = controller.draft.name; })])`, followed by a raw `TextField(controller: _nameController, decoration: InputDecoration(labelText: 'ZONE NAME', errorText: editor.validation.name), onChanged: controller.setName)`. Immediately after this block is `const SizedBox(height: 24)`, then the "Family member" `DropdownButtonFormField`. Preserve the `SizedBox(height: AppSpacing.lg)` before the block and the `SizedBox(height: 24)` after it — replace only the `Text('ZONE TYPE AND NAME', ...)` through the closing `TextField(...)` in between.
- `_LocationPickerPanel` (separate class, further down the file) is the exact visual pattern to mirror: `DecoratedBox` with `decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.circular(18), border: Border.all(color: AppColors.hairline), boxShadow: const [BoxShadow(color: Color(0x120C3A3F), blurRadius: 14, offset: Offset(0, 6))])`, `Padding(padding: const EdgeInsets.all(AppSpacing.md))`, then a `Column` starting with a header `Row` of `Icon` + `SizedBox(width: AppSpacing.sm)` + `Expanded(Column(title via AppTypography.title, SizedBox(height: 2), subtitle via AppTypography.bodySecondary))`.
- `_formatCenter` is an existing top-level private helper function near the bottom of the file (after `_SafeZoneEditorScreenState` closes, before `_LocationPickerPanel`) — add the new `_categoryColor`/`_categoryIcon` top-level functions alongside it.
- The current Flutter/Dart opacity API in active use in this exact file is `.withValues(alpha: ...)` (confirmed at the map-picker's surface tint and `_MapInstructionChip`) — use that, not the deprecated `.withOpacity`.

**Category color/icon mapping to mirror** (source of truth: `mobile/lib/features/geofencing/presentation/safe_zones_screen.dart`'s private `_categoryColor`/`_categoryIcon` functions — copy the mapping as new private helpers local to `edit_safe_zone_screen.dart`, since the originals are private to the other file and cannot be imported):
- `SafeZoneCategory.home` -> `AppColors.safe`, `Icons.home_outlined`
- `SafeZoneCategory.school` -> `AppColors.primaryTeal`, `Icons.school_outlined`
- `SafeZoneCategory.university` -> `AppColors.primaryTeal`, `Icons.account_balance_outlined`
- `SafeZoneCategory.workplace` -> `AppColors.memberViolet`, `Icons.work_outline`
- `SafeZoneCategory.custom` -> `AppColors.bodySecondary`, `Icons.place_outlined`

**`SafePathTextField` API** (`mobile/lib/shared_widgets/safepath_text_field.dart`): constructor params `label` (String, required — rendered uppercased as the caption above the field, so `label: 'Zone name'` displays as "ZONE NAME", matching today's `labelText: 'ZONE NAME'`), `controller`, `errorText`, `onChanged`, `prefixIcon` (`IconData?`). It internally builds one `TextFormField` (which Flutter renders as a `TextField` under the hood) — do not modify this shared widget.

**Test surface already checked during planning** (no other test files reference this section):
- `mobile/test/features/geofencing/safe_zone_editor_test.dart` line ~169 does `await tester.tap(find.text('Custom'));` after scrolling — this taps by the literal category label text, which still renders (as `category.wireValue`) inside the new `_CategoryOption`, so it should keep passing unchanged.
- `mobile/test/features/geofencing/safe_zone_router_flow_test.dart` line ~366 does `final nameField = tester.widget<TextField>(find.byType(TextField)); expect(nameField.controller?.text, zone.name);` — since `TextFormField` (used inside `SafePathTextField`) builds a `TextField` internally and forwards the explicit controller through, this finder is expected to keep resolving to exactly one `TextField` with the same `_nameController`. Confirm this by running the test; only touch the finder if it actually fails (see Task 2).
- `mobile/test/features/geofencing/safe_zone_wire_contract_test.dart` tests `SafeZoneCategory`/JSON wire values directly (no widget tree) — unaffected by this change, no need to run it as part of verification but no harm if included.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the zone type &amp; name section as a colorful, icon-led card</name>
  <files>mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart</files>
  <action>
Add the import for `SafePathTextField`: `import '../../../shared_widgets/safepath_text_field.dart';` near the other relative imports at the top of the file.

In `_SafeZoneEditorScreenState.build()`, replace the `Text('ZONE TYPE AND NAME', ...)` + `Wrap` of `ChoiceChip`s + raw `TextField` block (described in context above) with a single `_CategoryAndNamePanel(...)` invocation, passing: `selectedCategory: draft.category`, `nameController: _nameController`, `nameError: editor.validation.name`, `onCategorySelected:` a closure that calls `controller.selectCategory(category)` then sets `_nameController.text = controller.draft.name` (identical to today's `ChoiceChip.onSelected` body, just relocated), and `onNameChanged: controller.setName`. Leave the surrounding `SizedBox(height: AppSpacing.lg)` before and `SizedBox(height: 24)` after this block exactly as they are today.

Add a new private `_CategoryAndNamePanel` `StatelessWidget` (place it near `_LocationPickerPanel`, following the same structural pattern) with fields `selectedCategory` (`SafeZoneCategory`), `nameController` (`TextEditingController`), `nameError` (`String?`), `onCategorySelected` (`ValueChanged&lt;SafeZoneCategory&gt;`), `onNameChanged` (`ValueChanged&lt;String&gt;`). Its `build()` returns a `DecoratedBox` with the identical `BoxDecoration` used by `_LocationPickerPanel` (surface color, 18px radius, hairline border, same single `BoxShadow`), wrapping a `Padding(EdgeInsets.all(AppSpacing.md))` wrapping a `Column` (`crossAxisAlignment: CrossAxisAlignment.start`) containing, in order: (1) the header `Row` mirroring `_LocationPickerPanel`'s pattern — `Icon(Icons.category_outlined)`, `SizedBox(width: AppSpacing.sm)`, `Expanded(Column(Text('Zone type &amp; name', style: AppTypography.title), SizedBox(height: 2), Text('Choose what this place is, and give it a name your family will recognize.', style: AppTypography.bodySecondary)))`; (2) `SizedBox(height: AppSpacing.md)`; (3) a `Wrap(spacing: 8, runSpacing: 8)` containing one `_CategoryOption` per `SafeZoneCategory.values`, passing `category: category`, `selected: selectedCategory == category`, `onTap: () => onCategorySelected(category)`; (4) `SizedBox(height: AppSpacing.md)`; (5) a `SafePathTextField` with `label: 'Zone name'`, `controller: nameController`, `errorText: nameError`, `onChanged: onNameChanged`, `prefixIcon: _categoryIcon(selectedCategory)`.

Add a new private `_CategoryOption` `StatelessWidget` with fields `category` (`SafeZoneCategory`), `selected` (`bool`), `onTap` (`VoidCallback`). Its `build()` computes `final color = _categoryColor(category);` then returns `Material(color: Colors.transparent, child: InkWell(onTap: onTap, borderRadius: BorderRadius.circular(14), child: AnimatedContainer(duration: const Duration(milliseconds: 180), curve: Curves.easeInOut, constraints: const BoxConstraints(minHeight: 44, minWidth: 44), padding: const EdgeInsets.symmetric(horizontal: AppSpacing.xsMd, vertical: AppSpacing.sm), decoration: BoxDecoration(color: selected ? color : color.withValues(alpha: 0.12), borderRadius: BorderRadius.circular(14), border: selected ? null : Border.all(color: color), boxShadow: selected ? [BoxShadow(color: color.withValues(alpha: 0.35), blurRadius: 8, offset: const Offset(0, 3))] : null), child: Row(mainAxisSize: MainAxisSize.min, children: [Icon(_categoryIcon(category), size: 18, color: selected ? Colors.white : color), const SizedBox(width: AppSpacing.xs), Text(category.wireValue, style: AppTypography.body.copyWith(color: selected ? Colors.white : color, fontWeight: FontWeight.w600)), if (selected) ...[const SizedBox(width: AppSpacing.xs), const Icon(Icons.check, size: 16, color: Colors.white)]]))))`. Unselected: ~12% opacity tint background with a category-colored 1px border, category-colored icon and label text. Selected: solid category-color background, no border, subtle category-tinted shadow, white icon, white bold label, small trailing white check icon.

Add two new top-level private functions near `_formatCenter`: `Color _categoryColor(SafeZoneCategory category)` and `IconData _categoryIcon(SafeZoneCategory category)`, each a `switch (category)` expression returning the exact mapping listed in the context section above (copied from `safe_zones_screen.dart`'s private functions of the same names — this file's copies are separate top-level functions, not imports, since the originals are private to the other file).

Do not modify: `_LocationPickerPanel`, `_SafeZoneMapPickerScreen`, the "Radius" section, the "Sensitivity" `RadioGroup` section (including the pre-existing uncommitted `sensitivity.label` line), the "Notifications" section, the "Family member" `DropdownButtonFormField`, `SafePathTextField` itself, or any other file. Do not introduce `AppColors.sosRed` or the literal hex `#DE3B40` anywhere in this change.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter analyze lib/features/geofencing/presentation/edit_safe_zone_screen.dart</automated>
  </verify>
  <done>`edit_safe_zone_screen.dart` renders the zone-type-and-name section as a `_CategoryAndNamePanel` card containing five icon-led, category-colored `_CategoryOption` chips and a `SafePathTextField` with a category-matched `prefixIcon`; category selection and name entry still drive `controller.selectCategory`/`controller.setName` exactly as before; `flutter analyze` on the file is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions, adapt test finders if needed, confirm Sensitivity line untouched</name>
  <files>mobile/test/features/geofencing/safe_zone_router_flow_test.dart</files>
  <action>
Run `flutter analyze` across the whole `mobile` package to confirm no lint/type errors anywhere else in the codebase from this change.

Run the two widget test files that exercise `SafeZoneEditorScreen`: `mobile/test/features/geofencing/safe_zone_editor_test.dart` and `mobile/test/features/geofencing/safe_zone_router_flow_test.dart`. Both are expected to pass unchanged per the planning-time analysis in this plan's context section (the "Custom" text tap still hits the new `_CategoryOption`'s `InkWell`; `find.byType(TextField)` still resolves through `SafePathTextField`'s internal `TextFormField`).

If `safe_zone_router_flow_test.dart`'s `tester.widget&lt;TextField&gt;(find.byType(TextField))` assertion (in the "edit journey" test, checking the name field is prefilled with `zone.name`) actually fails at runtime, update ONLY that finder to `find.byType(TextFormField)` (reading `.controller?.text` the same way — `TextFormField` exposes the same `controller` field, so the assertion strength is unchanged, only the concrete widget type looked up changes) — do not weaken the assertion, and do not touch any other line in that test file. If it passes as-is, make no changes to the test file at all.

If `safe_zone_editor_test.dart`'s `find.text('Custom')` tap fails, add a `ValueKey('category-option-custom')`-style key per `_CategoryOption` (keyed by `category.name`) and update the finder to use `find.byKey(const ValueKey('category-option-custom'))` instead — again, adapt the finder only, do not weaken what the test proves (that tapping Custom updates the draft).

Finally, run `git diff -- mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart` and confirm the pre-existing uncommitted line inside the Sensitivity section (`title: Text(sensitivity.label)`, not `sensitivity.wireValue`) is still present in the diff and that no line inside the Sensitivity `RadioGroup` block was altered by this plan's changes.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter analyze &amp;&amp; flutter test test/features/geofencing/safe_zone_editor_test.dart test/features/geofencing/safe_zone_router_flow_test.dart</automated>
  </verify>
  <done>`flutter analyze` is clean across `mobile/`; both geofencing editor test files pass (with any finder adaptation documented in the summary if one was needed); `git diff` confirms the pre-existing Sensitivity-section fix is present and untouched, and that no other section of the file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure client-side presentational change (category chip styling + shared text-field component swap) inside an already-authenticated, already-reviewed editor screen — no new data flow, input source, or trust boundary is introduced. |

## STRIDE Threat Register

No new threats — this plan only restyles existing, already-wired controls (`SafeZoneCategory` selection, zone name text entry) using the same `GeofenceController` calls (`selectCategory`, `setName`) and validation (`editor.validation.name`) that were already in place. No new network calls, permissions, or persisted data.
</threat_model>

<verification>
1. `flutter analyze` on `mobile/` is clean (no new errors/warnings from this change).
2. `flutter test test/features/geofencing/safe_zone_editor_test.dart test/features/geofencing/safe_zone_router_flow_test.dart` passes.
3. Manual/visual: the "Zone type &amp; name" card shows five icon-led, category-colored options (home=safe green, school/university=teal, workplace=violet, custom=slate) with a white check on the selected one, followed by the `SafePathTextField`-styled name field whose prefix icon matches the selected category; the card's border/shadow/radius visually match `_LocationPickerPanel` above it.
4. `git diff -- mobile/lib/features/geofencing/presentation/edit_safe_zone_screen.dart` shows only this plan's intended region changed, plus the pre-existing untouched Sensitivity-section line.
</verification>

<success_criteria>
- The "ZONE TYPE AND NAME" section is replaced by a card-wrapped, icon-led, category-colored selector matching `_LocationPickerPanel`'s visual language.
- The zone name field uses the shared `SafePathTextField` widget with a category-matched prefix icon.
- Category selection and name entry behavior are unchanged (same controller calls, same validation).
- No SOS red token appears in the new section.
- `_LocationPickerPanel`, Radius, Sensitivity (including the pre-existing uncommitted fix), Notifications, and the Family member dropdown are byte-for-byte unchanged.
- `flutter analyze` and the two relevant geofencing test files pass.
</success_criteria>

<output>
Create `.planning/quick/260820-73v-redesign-the-zone-type-and-name-section-/260820-73v-SUMMARY.md` when done
</output>
