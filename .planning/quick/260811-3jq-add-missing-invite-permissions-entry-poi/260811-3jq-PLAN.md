---
phase: quick-260811-3jq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/privacy/presentation/privacy_center_screen.dart
  - mobile/test/features/privacy/privacy_center_screen_test.dart
autonomous: true
requirements: [QUICK-PRIVACY-INVITE-PERMISSIONS-ENTRY]

must_haves:
  truths:
    - "A Guardian who belongs to a family circle sees Invite and Permissions app-bar actions on the Privacy Center screen, restoring the entry points that used to live on the now-dead landing_stub_screen.dart."
    - "Tapping Invite pushes /circle/invite (InviteMemberScreen); tapping Permissions pushes /circle/permissions (ManagePermissionsScreen) — both pre-existing, already-registered GoRouter routes that today have zero live callers."
    - "A non-Guardian member, or any user with no family circle, never sees either action on the Privacy Center app bar."
    - "The no-family empty state's own AppBar (_PrivacyMessage, shown via the NoCircleCta path) is byte-for-byte unchanged — this task only touches the populated-state Scaffold's AppBar."
    - "flutter analyze reports no new issues from this change."
    - "The existing mobile/test/features/privacy/privacy_center_screen_test.dart widget-test suite still passes."
  artifacts:
    - path: "mobile/lib/features/privacy/presentation/privacy_center_screen.dart"
      provides: "Guardian-gated Invite/Permissions IconButtons in the main Scaffold's AppBar actions, before LogoutAction"
      contains: "Icons.person_add_alt_1"
  key_links:
    - "Gating logic (hasFamily && isGuardian, effectiveRole = currentMember?.role ?? profileState?.profile?.role, effectiveUserId = currentUserId ?? profileUserId) is copied verbatim from the retired landing_stub_screen.dart (lines 52-73), so behavior matches the dead-code original exactly — no new authorization semantics are introduced."
    - "IconButton onPressed handlers call context.push('/circle/invite') and context.push('/circle/permissions') — the exact same already-registered app_router.dart routes the dead stub used. app_router.dart itself is not touched by this task."
---

<objective>
Restore the Guardian-only "Invite" and "Permissions" entry points that were lost when the app's
home route moved from the Phase-1 `LandingStubScreen` to the bottom-nav `MainShell`. Add them to
the Privacy Center screen's app bar instead, reusing the exact Guardian-gating logic the dead stub
used to have.

Purpose: `InviteMemberScreen` (`/circle/invite`) and `ManagePermissionsScreen`
(`/circle/permissions`) are fully built, tested, and registered in the router, but nothing in the
live app links to them anymore — a Guardian currently has no way to invite a new family member or
manage member permissions from the running app.
Output: Two Guardian-gated `IconButton`s in `PrivacyCenterScreen`'s populated-state AppBar,
positioned before the existing `LogoutAction()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@mobile/lib/features/privacy/presentation/privacy_center_screen.dart
@mobile/lib/features/home/presentation/landing_stub_screen.dart
@mobile/lib/features/profile/application/profile_controller.dart
@mobile/lib/core/router/app_router.dart
@mobile/test/features/privacy/privacy_center_screen_test.dart
</context>

<background>
Verified during planning, not a guess:

- `/home` now routes to `LocationPermissionGate(child: MainShell())` (`app_router.dart:233-237`).
  `LandingStubScreen` is confirmed dead code — nothing imports or navigates to it.
- The dead stub's Guardian-gated actions (`landing_stub_screen.dart:82-93`) were: an
  `Icons.person_add_alt_1` `IconButton` with tooltip `'Invite'` pushing `/circle/invite`, and an
  `Icons.tune` `IconButton` with tooltip `'Permissions'` pushing `/circle/permissions`. Both routes
  are registered and working today (`app_router.dart:280-284` and `293-297`) — they simply have no
  live caller.
- The stub's gating (`landing_stub_screen.dart:52-73`), to be reproduced verbatim: watch
  `familyControllerProvider` and `profileControllerProvider`; take `currentUserId` from
  `authApiProvider().currentSession?.user.id`; fall back to `profileState?.profile?.userId` as
  `effectiveUserId`; find the matching `FamilyMemberView` in `familyState?.members` by
  `member.userId == effectiveUserId` as `currentMember`; take `effectiveRole` as
  `currentMember?.role ?? profileState?.profile?.role`; `isGuardian` is
  `effectiveRole == Role.guardian`; `hasFamily` is `familyState?.family != null`; each action is
  gated on `hasFamily && isGuardian`.
- Target file `privacy_center_screen.dart` already watches `familyControllerProvider` (local var
  `familyState`) and `authApiProvider` (local var `currentUserId`), and already derives
  `familyId = familyState?.family?.id`. It does NOT currently watch `profileControllerProvider` and
  does NOT currently import `Role` (from `auth/data/auth_models.dart`) or
  `profile_controller.dart` — both must be added. Its populated-state AppBar currently reads
  `actions: const [LogoutAction()]` (around line 191-194); this is the ONLY app bar this task
  touches. The screen also renders a separate `_PrivacyMessage` widget for the no-family state with
  its own `actions: const [LogoutAction()]` (around line 571-574) — that one is explicitly
  out of scope and must not change.
- Do NOT touch `landing_stub_screen.dart`, `app_router.dart`, `InviteMemberScreen`,
  `ManagePermissionsScreen`, or any backend code. Do NOT add a new bottom-nav tab or new route.
</background>

<tasks>

<task type="auto">
  <name>Task 1: Add Guardian-gated Invite/Permissions app-bar actions to Privacy Center</name>
  <files>mobile/lib/features/privacy/presentation/privacy_center_screen.dart, mobile/test/features/privacy/privacy_center_screen_test.dart</files>
  <action>
In `privacy_center_screen.dart`, add two imports in alphabetical position among the existing
`import` lines: `../../auth/data/auth_models.dart` (placed right after the existing
`../../auth/data/auth_api.dart` import, since "auth_api" sorts before "auth_models"), and
`../../profile/application/profile_controller.dart` (placed after `../../family/data/family_models.dart`
and before `../../sos/presentation/sos_reach_warning_card.dart`, since "profile" sorts after
"family" and before "sos").

In `PrivacyCenterScreen.build`, immediately after the existing line that assigns
`final now = ref.watch(privacyNowProvider)();`, add the gating computation reproduced verbatim
from `landing_stub_screen.dart:52-73` (see the `<background>` section above for the exact
variable derivation): watch `profileControllerProvider` and take its `.value` as `profileState`;
derive `profileUserId` from `profileState?.profile?.userId`; derive `effectiveUserId` as
`currentUserId ?? profileUserId` (reusing the `currentUserId` already computed above — do not
rename or duplicate it); derive `hasFamily` as `familyId != null` (reusing the existing `familyId`
local, do not recompute family-null differently); build a `members` list from
`familyState?.members ?? const &lt;FamilyMemberView&gt;[]` (note: this is deliberately a different
local than the existing `recipients` variable, which excludes the current user — the gating lookup
must search the FULL member list, including self); loop over `members` to find the
`FamilyMemberView` whose `userId == effectiveUserId` as `currentMember`; derive `effectiveRole` as
`currentMember?.role ?? profileState?.profile?.role`; derive `isGuardian` as
`effectiveRole == Role.guardian`. Do not rename any of the pre-existing local variables
(`familyState`, `currentUserId`, `familyId`, `recipients`, `now`) and do not change their
computation.

Then change ONLY the main populated-state `Scaffold`'s `AppBar.actions` (the one inside the
`return Scaffold(...)` that follows the `if (familyId == null)` early return — currently
`actions: const [LogoutAction()]`) to a non-const list containing, in order: an `IconButton`
with `icon: const Icon(Icons.person_add_alt_1)`, `tooltip: 'Invite'`, and
`onPressed: () =&gt; context.push('/circle/invite')`, included only `if (hasFamily &amp;&amp; isGuardian)`;
then an `IconButton` with `icon: const Icon(Icons.tune)`, `tooltip: 'Permissions'`, and
`onPressed: () =&gt; context.push('/circle/permissions')`, also gated `if (hasFamily &amp;&amp; isGuardian)`;
then `const LogoutAction()` unconditionally last. This mirrors the ordering and gating in
`landing_stub_screen.dart:81-105` exactly (minus that stub's separate account popup menu, which is
out of scope here).

Do NOT modify `_PrivacyMessage`'s AppBar (the no-family empty state, still
`actions: const [LogoutAction()]`), the `NoCircleCta` widget, `landing_stub_screen.dart`,
`app_router.dart`, or any other file. Do not add a new route or bottom-nav tab.

After the change, read through `mobile/test/features/privacy/privacy_center_screen_test.dart` and
run its suite. Based on this file's fixtures, the main-path helper `_app()` seeds a family via
`_SeededFamilyController` where the current user (`self-user`) IS itself a Guardian member of the
family, so the new buttons WILL render during those existing tests — confirm no existing assertion
in that file inspects AppBar action counts, icon types, or is otherwise broken by the two new
`IconButton`s appearing (a read of the file during planning found no such assertion, so the suite
is expected to pass unmodified). If, contrary to that expectation, a test genuinely breaks because
of the new buttons, fix that specific test minimally so it reflects the new, correct behavior — do
not weaken or delete an assertion just to make it pass, and do not add unrelated new test cases
beyond what's needed to keep the suite green.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter analyze</automated>
    <automated>cd mobile &amp;&amp; flutter test test/features/privacy/privacy_center_screen_test.dart</automated>
    <automated>cd mobile &amp;&amp; grep -c "Icons.person_add_alt_1" lib/features/privacy/presentation/privacy_center_screen.dart</automated>
    <automated>cd mobile &amp;&amp; grep -c "hasFamily &amp;&amp; isGuardian" lib/features/privacy/presentation/privacy_center_screen.dart</automated>
  </verify>
  <done>
`flutter analyze` is clean (no new issues). `privacy_center_screen_test.dart` passes. The main
Scaffold's AppBar renders the Guardian-gated Invite (`Icons.person_add_alt_1`) and Permissions
(`Icons.tune`) actions before `LogoutAction()`, gated on `hasFamily &amp;&amp; isGuardian` computed the
same way as the retired `landing_stub_screen.dart`. `_PrivacyMessage`'s AppBar, `app_router.dart`,
and `landing_stub_screen.dart` are unchanged in `git diff --name-only`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI visibility gate -> existing server-authorized routes | The new buttons only change whether a client-side navigation affordance is shown; they grant no new capability, since `/circle/invite` and `/circle/permissions` already exist, are already reachable by direct navigation, and already enforce their own (unmodified) authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3JQ-01 | Elevation of Privilege | `hasFamily && isGuardian` client-side gate on the two new IconButtons | low | accept | This is a UI convenience gate only, reproduced verbatim from the already-shipped (now dead) `landing_stub_screen.dart`. It introduces no new authorization surface: `InviteMemberScreen` and `ManagePermissionsScreen` were already reachable via direct route navigation before this task and enforce their own server-side authorization, which this task does not touch. |
| T-3JQ-02 | Information Disclosure | Invite/Permissions actions visible to non-Guardians | low | accept | Gated correctly behind `isGuardian`; a non-Guardian or user without a family never sees the actions, matching the pre-existing stub behavior. |
| T-3JQ-SC | Tampering | npm/pub/cargo installs | low | accept | No package is added, upgraded, or removed by this task — no legitimacy audit is triggered. |
</threat_model>

<verification>
- `cd mobile && flutter analyze` is clean.
- `cd mobile && flutter test test/features/privacy/privacy_center_screen_test.dart` passes.
- `git diff --name-only` shows only `privacy_center_screen.dart` (and the test file, only if a fix
  was genuinely required to keep the suite green).
- `landing_stub_screen.dart`, `app_router.dart`, `InviteMemberScreen`, and `ManagePermissionsScreen`
  are untouched.
</verification>

<success_criteria>
A Guardian who belongs to a family circle can reach Invite (`/circle/invite`) and Permissions
(`/circle/permissions`) directly from the Privacy Center app bar. Non-Guardians and users without a
family never see these actions. The no-family empty state is visually and behaviorally unchanged.
`flutter analyze` and the existing widget-test suite both pass.
</success_criteria>

<output>
Create `.planning/quick/260811-3jq-add-missing-invite-permissions-entry-poi/260811-3jq-SUMMARY.md` when done
</output>
