---
phase: quick-260720-544
plan: 01
subsystem: ui
tags: [flutter, riverpod, location, presence, live-map]

# Dependency graph
requires:
  - phase: 02-real-time-location-history-privacy
    provides: LocationController/LocationState presence pipeline (isMemberOnline, memberLastSeenAt), LiveMemberMarker, member_detail_sheet, staleness.dart opacity bands
provides:
  - kStaleThreshold (3 min) + isStaleAge() pure helper in staleness.dart
  - LocationState.isMemberStale(userId, {now}) derived read
  - Neutral navy/slate STALE presence state on the Live Map marker, status rail, and member detail sheet badge
  - 30s periodic wall-clock re-evaluation ticker on LiveMapScreen (cancelled on dispose)
affects: [location, live-map, sos-fast-path]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Derived boolean state (isMemberStale) computed from existing timestamps with an injectable {now} param for deterministic tests, following the existing memberLastSeenAt/lastSeenText convention"
    - "Low-frequency Timer.periodic + bare setState(() {}) as the standard way to re-render wall-clock-derived UI state in a ConsumerStatefulWidget, cancelled synchronously in dispose before other teardown"

key-files:
  created: []
  modified:
    - mobile/lib/features/location/application/staleness.dart
    - mobile/lib/features/location/application/location_controller.dart
    - mobile/lib/features/location/presentation/live_map_screen.dart
    - mobile/lib/features/location/presentation/member_detail_sheet.dart
    - mobile/test/features/location/staleness_test.dart
    - mobile/test/features/location/member_presence_test.dart
    - mobile/test/features/location/live_member_marker_test.dart

key-decisions:
  - "kStaleThreshold = 3 minutes, chosen to sit just above the existing 2-minute stalenessFor opacity-fade band and below the 5-minute movement-independent battery-refresh cadence, so the STALE label appears as a coherent next step after the marker begins fading"
  - "Stale styling uses AppColors.primaryNavy/navyTintBg/hairline — explicitly never sosRed/sosRedDeep and never caution/cautionBg/cautionText, since SOS red is reserved for emergencies and amber/caution is reserved for the low-battery alert"
  - "A stale member still counts toward the _LiveMapOverlay 'online' count chip (they are still connected) — a separate stale count was out of scope for this fix"
  - "Offline gates stale: an offline member is never reported as stale, since OFFLINE already communicates the data gap; a member online but with no last-seen evidence yet is treated as fresh, not stale"

patterns-established:
  - "Presence three-state UI (online/stale/offline) always renders a distinct text label, never relies on color alone, preserving the existing a11y property from quick task 260720-3u4"

requirements-completed: [QUICK-260720-544]

coverage:
  - id: D1
    description: "isStaleAge() and LocationState.isMemberStale() correctly classify online+fresh, online+stale (boundary and above), offline, and online-with-no-last-seen-data cases, clamping negative/clock-skew ages to fresh"
    requirement: "QUICK-260720-544"
    verification:
      - kind: unit
        ref: "mobile/test/features/location/staleness_test.dart#group isStaleAge"
        status: pass
      - kind: unit
        ref: "mobile/test/features/location/member_presence_test.dart#group LocationState.isMemberStale"
        status: pass
    human_judgment: false
  - id: D2
    description: "LiveMemberMarker and the member detail sheet render a distinct neutral STALE label/badge for an online-but-stale member (not ONLINE, not OFFLINE), and online/offline behavior is unchanged"
    requirement: "QUICK-260720-544"
    verification:
      - kind: unit
        ref: "mobile/test/features/location/live_member_marker_test.dart#renders the STALE label for an online-but-stale member"
        status: pass
      - kind: unit
        ref: "mobile/test/features/location/member_presence_test.dart#member detail sheet shows the STALE badge for online-but-stale members"
        status: pass
      - kind: unit
        ref: "mobile/test/features/location/live_map_screen_test.dart (full file, regression)"
        status: pass
    human_judgment: false
  - id: D3
    description: "A periodic 30s ticker on LiveMapScreen re-evaluates staleness on wall-clock time alone and is cancelled synchronously on dispose (no pending-timer test failures across the whole location test suite)"
    requirement: "QUICK-260720-544"
    verification:
      - kind: unit
        ref: "mobile/test/features/location/ (full suite, 81 tests, no Timer-still-pending failures)"
        status: pass
    human_judgment: true
    rationale: "Automated tests confirm the timer doesn't leak past disposal and doesn't break existing widget tests, but actually observing a marker visually flip from ONLINE to STALE purely from wall-clock time passing (no state change) needs either a fake-clock-driven widget test with tester.pump(30s+) or manual device verification per the plan's optional manual-sanity step — neither was exercised in this session."
  - id: D4
    description: "Threat register T-544-01 (information disclosure, accepted) and T-544-02 (DoS via periodic timer, mitigated by the 30s tick + synchronous dispose cancellation) are addressed by the implementation as specified"
    requirement: "QUICK-260720-544"
    verification: []
    human_judgment: true
    rationale: "Threat disposition review is a design/security judgment call, not something a unit test asserts directly — covered implicitly by D3's dispose-safety tests but the disposition itself needs human sign-off."

# Metrics
duration: 11min
completed: 2026-07-20
status: complete
---

# Phase quick-260720-544: Fix the online-but-stale gap in location Summary

**Added a client-side `isMemberStale` derived state plus a 30s periodic re-render ticker so a socket-connected-but-data-stale family member now shows a neutral navy "STALE" label distinct from green ONLINE and grey OFFLINE across the Live Map marker, status rail, and member detail sheet.**

## Performance

- **Duration:** 11 min (03:52:44 -> 03:59:23 UTC+3, excluding the earlier context-load/read time)
- **Started:** 2026-07-20T00:52:44.000Z
- **Completed:** 2026-07-20T00:59:23.000Z
- **Tasks:** 2
- **Files modified:** 8 (4 lib, 4 test)

## Accomplishments
- Added `kStaleThreshold` (3 min) and a pure `isStaleAge()` helper to `staleness.dart`, plus `LocationState.isMemberStale(userId, {now})` derived from existing `isMemberOnline`/`memberLastSeenAt` state — no new stored state.
- Threaded `isStale` through `LiveMemberMarker`, the member status rail (`_InlinePresence`/`_PresenceDot`), and the member detail sheet's `_StatusBadge`, each rendering a distinct neutral navy/slate "STALE" treatment (never SOS-red, never caution/amber).
- Added a 30-second `_stalenessTimer` on `LiveMapScreen` so a stationary member's marker flips from ONLINE to STALE purely from wall-clock time passing, with synchronous cancellation in `dispose()` before map-controller teardown.
- Updated Semantics labels on the marker and status card to announce "stale" alongside "online"/"offline", preserving the non-color-only a11y property from quick task 260720-3u4.

## Task Commits

Each task was committed atomically:

1. **Task 1: Derive isMemberStale from data freshness** - `47e21db` (feat)
2. **Task 2: Surface stale state on the Live Map with periodic re-evaluation** - `9db3dbd` (feat)

**Plan metadata:** (pending — orchestrator commits SUMMARY.md/STATE.md separately)

## Files Created/Modified
- `mobile/lib/features/location/application/staleness.dart` - Added `kStaleThreshold` and `isStaleAge()`
- `mobile/lib/features/location/application/location_controller.dart` - Added `LocationState.isMemberStale()`
- `mobile/lib/features/location/presentation/live_map_screen.dart` - Periodic ticker, `isStale` threading through marker/rail/`_VisibleMember`, three-state `_MarkerPresenceLabel`/`_InlinePresence`/`_PresenceDot`
- `mobile/lib/features/location/presentation/member_detail_sheet.dart` - `MemberDetail.isStale` field, three-state `_StatusBadge`
- `mobile/test/features/location/staleness_test.dart` - `isStaleAge` boundary/clock-skew tests
- `mobile/test/features/location/member_presence_test.dart` - `LocationState.isMemberStale` unit tests + stale detail-sheet widget test
- `mobile/test/features/location/live_member_marker_test.dart` - STALE label widget test

## Decisions Made
- `kStaleThreshold` set to 3 minutes to sit just above the existing 2-minute opacity-fade band in `stalenessFor` and below the 5-minute battery-refresh cadence, so the fade and the explicit label form one coherent progression.
- Stale styling uses `AppColors.primaryNavy`/`navyTintBg`/`hairline` exclusively — verified no `sosRed*`/`caution*` tokens are used anywhere in the new code paths.
- `_LiveMapOverlay`'s online/offline count chips are left unchanged per the plan: a stale member is still connected and continues to count as online; a separate stale count was explicitly out of scope.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The online-but-stale UI gap is closed; `LocationState.isMemberStale` is a general-purpose derived read other Live Map surfaces (e.g. a future stale count chip) can reuse without new plumbing.
- Manual device verification of the visual ONLINE -> STALE transition (app open, stationary >3 min) remains optional/unexercised in this session, per the plan's own "Manual sanity (optional)" verification note — not a blocker for this quick task's scope.

---
*Phase: quick-260720-544*
*Completed: 2026-07-20*

## Self-Check: PASSED

All 7 modified source/test files and the SUMMARY.md file confirmed present on disk; both task commits (`47e21db`, `9db3dbd`) confirmed present in git log.
