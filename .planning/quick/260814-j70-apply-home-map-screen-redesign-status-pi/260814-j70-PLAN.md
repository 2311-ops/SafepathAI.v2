---
phase: quick-260814-j70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/location/presentation/live_map_screen.dart
  - mobile/lib/features/home/presentation/main_shell.dart
  - mobile/test/features/location/live_map_screen_test.dart
  - mobile/test/features/location/live_member_marker_test.dart
  - mobile/test/features/location/location_permission_gate_test.dart
autonomous: true
requirements: [DESIGN-01]

must_haves:
  truths:
    - The Home Map top overlay is a single slim row — identity pin, one family status pill reading "N online · N offline" with a presence dot, profile action, logout action — with no title text, no action buttons, and no two-chip cluster.
    - Manage-safe-zones and Notifications are reachable only from a draggable bottom sheet that rests at 20% height and drags open to 50%.
    - Every family member map pin renders a presence-colored ring — safety green when online, slate grey when offline.
    - The self ("You") map pin renders an animated expanding pulse ring; other members' pins do not.
    - Existing Live Map behavior is unchanged - rail-card tap still recenters the camera at zoom 17, the header identity pin still live-reads selfPosition, the low-battery banner still renders, Manage-safe-zones is still Guardian-only, and the detail sheet still opens on marker tap.
    - MainShell's SOS control and four-tab bottom nav are byte-identical apart from one extracted layout constant; no SOS arm, hold, timing, trigger, or navigation code is touched.
    - flutter analyze reports no issues and the full mobile test suite passes.
  artifacts:
    - mobile/lib/features/location/presentation/live_map_screen.dart with a slim top bar widget, a family status pill widget, a DraggableScrollableSheet action sheet, and a stateful LiveMemberMarker with a presence ring plus self pulse ring.
    - mobile/lib/features/home/presentation/main_shell.dart exposing the bottom-bar height as a named public constant.
    - mobile/test/features/location/live_map_screen_test.dart with new coverage for the status pill and the draggable action sheet.
    - mobile/test/features/location/live_member_marker_test.dart with new coverage for the presence ring color and the self-only pulse ring.
  key_links:
    - MainShell bottom-bar height constant -> LiveMapScreen bottom-sheet inset (sheet content must clear the nav bar and the raised SOS disc).
    - LiveMemberMarker avatar-slot size -> OverlayMarker.height in live_map_screen.dart (a mismatch clips the pulse ring or overflows the marker box).
    - MemberMapPin single-instance invariant -> live_map_screen_test.dart UAT-72 assertion (map markers must keep using LiveMemberMarker, never MemberMapPin).
    - Uppercase ONLINE/OFFLINE marker labels -> existing findsOneWidget assertions (the new pill must use lowercase copy so it cannot collide).
    - location_permission_gate_test.dart map-not-mounted sentinel -> the removed "Your family, live" string (must move to a sentinel that still exists).
---

<objective>
Apply the user's Home Map redesign to `LiveMapScreen`: split today's single tall overlay card
into a slim top status bar plus a draggable bottom action sheet, and give the map pins the
specified presence ring and self pulse ring.

Purpose: The design-token rebrand (quick task 260814-amy) recolored the app but explicitly
deferred every screen-level structural change. This is the Home Map structural pass. Colors,
type, and radii are already correct and are not re-litigated here.

Output: Restructured Live Map presentation layer, extended widget-test coverage, green
analyzer and green full mobile test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@mobile/lib/features/location/presentation/live_map_screen.dart
@mobile/lib/features/home/presentation/main_shell.dart
@mobile/lib/core/theme/app_colors.dart
@mobile/lib/core/theme/app_spacing.dart
@mobile/test/features/location/live_map_screen_test.dart
@mobile/test/features/location/live_member_marker_test.dart
</context>

<investigation_findings>

Read before planning any work. These were verified against the real files; do not re-derive.

## Already spec-compliant — DO NOT CHANGE

| Spec item | Where it already lives | Evidence |
|-----------|------------------------|----------|
| SOS button: 72px, deep red, center-bottom, hold-to-activate, 3-second press ring | `mobile/lib/features/sos/presentation/sos_arm_button.dart`, mounted at `Positioned(top: 0)` inside `MainShell`'s bottom-center `Stack` | Outer `SizedBox(width: 72, height: 72)` and inner `Container(width: 72, height: 72)`; `color: AppColors.sosRed` (`#E53935`); `AnimationController(duration: Duration(milliseconds: 3000))`; ring drawn by `SosArmRingPainter` at `Size(72, 72)` |
| Bottom nav: Map, Activity, Insights, Privacy | `MainShell._tabs` | Exactly four `_ShellTab` entries in that order; index 2 is a non-navigable spacer holding the SOS disc |
| Full-screen map | `LiveMapScreen.build` | `Scaffold(body: Stack(...))` with `VectorMap` as the first child, and `VectorMap` renders its base map inside `Positioned.fill` |
| Muted tile style | `mobile/lib/features/location/presentation/vector_map.dart`, `kOpenFreeMapLibertyStyle` | OpenFreeMap Liberty is already the flat, low-saturation basemap; `maplibre_gl` exposes only a whole-style URL (`styleString`), so there is no per-property "muted" knob to tweak. Swapping providers/styles wholesale is out of scope. No change. |

The SOS button is confirmed correct at both the size and the interaction level. Nothing in
`sos_controller.dart`, `sos_session_state.dart`, `sos_arm_button.dart`, `sos_arm_ring_painter.dart`,
or `sos_countdown.dart` is edited by this plan.

## Missing / different from spec — the actual work

1. **No slim top bar.** `_LiveMapOverlay` is one tall card holding an identity pin, a
   `'Your family, live'` title, two separate `_StatusCountChip`s, `ManageSafeZonesButton`,
   `ViewNotificationsButton`, a profile `IconButton`, and `LogoutAction`.
2. **No bottom sheet.** The two action buttons live inside that top card.
3. **No presence ring on pins.** `LiveMemberMarker`'s avatar border is
   `Border.all(color: AppColors.surface, width: 3)` — white regardless of presence. Presence is
   conveyed only by the separate `_MarkerPresenceLabel` text pill.
4. **No pulse ring on the self pin.** `LiveMemberMarker` is a `StatelessWidget` with no
   animation. (`MemberMapPin` in `shared_widgets/` has a fading presence *dot*, not an expanding
   ring, and is the header identity pin — a different widget.)

## Constraints discovered in existing tests — violating any of these breaks a green suite

- `live_map_screen_test.dart` asserts exactly one `MemberMapPin` on the populated map, with
  `label == 'You'` and `userId`/`profileImageUrl`/`profileUpdatedAt` read from
  `LocationState.selfPosition` (UAT 72). Map markers must keep using `LiveMemberMarker`.
- `live_map_screen_test.dart` asserts `find.text('ONLINE')` and `find.text('OFFLINE')` each
  find exactly one widget (the two marker presence labels). New top-bar copy must not add
  either uppercase string.
- `live_map_screen_test.dart` asserts tapping `ValueKey('member-card-other-user')` writes a
  camera command at zoom 17 and does not open `MemberDetailSheet`. The member status rail and
  its keys must survive.
- `safe_zone_flow_test.dart` constructs `ManageSafeZonesButton` directly and asserts its
  rendered size is at least 48pt. Keep that public class and its `minimumSize` intact.
- `location_permission_gate_test.dart` uses `find.text('Your family, live')` three times as a
  "map is not mounted" sentinel. That string is being deleted, so the sentinel must move.
- `sos_button_press_hold_test.dart` mounts `MainShell` (which eagerly builds `LiveMapScreen`)
  and calls `pumpAndSettle`. It is safe today only because its fakes put the map on the
  "No circle yet" branch, so no marker mounts. Keep that true — never mount a repeating
  animation on the empty-state branches.
- `live_member_marker_test.dart` uses `tester.pump()` exclusively, never `pumpAndSettle`. New
  tests that mount a self marker must follow that, because a repeating animation never settles.

</investigation_findings>

<tasks>

<task type="auto">
  <name>Task 1: Split the map overlay into a slim top status bar and a draggable bottom action sheet</name>
  <files>
mobile/lib/features/home/presentation/main_shell.dart,
mobile/lib/features/location/presentation/live_map_screen.dart
  </files>
  <action>
In `main_shell.dart`, make the bottom bar's height reusable. Declare a top-level public
constant above the `MainShell` class — name it `kShellBottomBarHeight`, value `124`, doc-commented
as the height of the nav bar plus the raised SOS disc overhang, excluding the bottom safe-area
inset, exposed so a tab body drawing its own bottom-anchored surface can reserve the space. Replace
the existing hard-coded `124` in the `SizedBox` inside `bottomNavigationBar` with that constant.
That is the ONLY edit to this file: `SosArmButton`, `_onArmComplete`, `sosControllerProvider`,
`pushNamed('sos-session')`, `_tabs`, `_NavItem`, and the `IndexedStack` children stay untouched.

In `live_map_screen.dart`, restructure the `Stack` in `_LiveMapScreenState.build` into three
layers instead of two:

Layer 1 stays the existing `VectorMap` call, unmodified.

Layer 2 replaces the current `SafeArea` block. Use `SafeArea(bottom: false, ...)` wrapping the
existing `Padding(EdgeInsets.all(AppSpacing.md))` and `Column(mainAxisSize: MainAxisSize.min)`.
Its children become, in order: the new top bar widget; `SizedBox(height: AppSpacing.sm)`; the
existing `_MemberStatusRail` with its existing `onMemberTap` recenter callback verbatim; then the
existing conditional `LowBatteryBanner` block verbatim. Setting `bottom: false` matters — it stops
the top cluster from reserving bottom inset that the sheet layer needs.

Layer 3 is new: `Positioned.fill(bottom: kShellBottomBarHeight, child: DraggableScrollableSheet(...))`,
imported from `main_shell.dart`. Reserving that inset is what keeps the sheet's content clear of
the nav bar and the raised SOS disc, since `MainShell` sets `extendBody: true` and the map draws
behind the bar. Configure the sheet with `initialChildSize: 0.2`, `minChildSize: 0.2`,
`maxChildSize: 0.5`, `snap: true`, `snapSizes: const [0.2, 0.5]`. Its builder returns a
`DecoratedBox` styled with `AppColors.surface`, a top-only
`BorderRadius.vertical(top: Radius.circular(AppRadius.bottomSheet))` (`AppRadius` already comes
from the imported `app_spacing.dart`), an `AppColors.hairline` border, and the same upward soft
shadow idiom the nav bar uses. Inside it put a `ListView` wired to the builder's `ScrollController`
— that wiring is what makes the sheet draggable at all — padded
`EdgeInsets.fromLTRB(AppSpacing.md, AppSpacing.sm, AppSpacing.md, AppSpacing.md)`, containing: a
centered grab handle (40x4, fully rounded, `AppColors.hairline`); `SizedBox(height: AppSpacing.md)`;
the existing `ManageSafeZonesButton` wrapped in `SizedBox(width: double.infinity)` and rendered only
when the Guardian-gated callback is non-null; `SizedBox(height: AppSpacing.sm)`; the existing
`ViewNotificationsButton`, also full-width. Both buttons keep their current `context.push` targets
(`/safe-zones`, `/notifications`) and the `showSafeZones` Guardian gate is unchanged.

Add a private `_MapTopBar` widget replacing `_LiveMapOverlay`. Keep `_LiveMapOverlay`'s existing
260ms `TweenAnimationBuilder` fade-and-rise entrance so the surface keeps its current polish. Its
card decoration keeps the same surface/hairline/shadow treatment but drops to
`BorderRadius.circular(999)` and `EdgeInsets.symmetric(horizontal: AppSpacing.sm, vertical: 6)`
padding so it reads as a slim pill bar rather than a panel. Its child is a single `Row` with
`mainAxisSize: MainAxisSize.min` containing, in order: the existing `MemberMapPin` call with
`label: 'You'`, `identityColor: AppColors.primaryTeal`, `isSelf: true`, `size: 36` (down from 40),
and `userId`/`profileImageUrl`/`profileUpdatedAt` still sourced from `state?.selfPosition` — this
call must stay a live-reading, non-const `MemberMapPin` or the UAT-72 test fails;
`SizedBox(width: AppSpacing.sm)`; an `Expanded` holding the new status pill; the existing profile
`IconButton.filledTonal`; `SizedBox(width: AppSpacing.xs)`; the existing `const LogoutAction()`.
Delete the `'Your family, live'` `Text`, the `Wrap`, and both action buttons from this widget.

Add a private `_FamilyStatusPill` widget taking `onlineCount` and `offlineCount`. It renders a
rounded-999 `DecoratedBox` with a presence dot followed by `'$onlineCount online · $offlineCount offline'`
in `AppTypography.caption` with `letterSpacing: 0`. The copy is deliberately lowercase so it can
never collide with the marker labels the existing tests count. Dot and text foreground are
`AppColors.safe` when `onlineCount > 0`, otherwise `AppColors.bodySecondary`; background/border
follow the same `safeBg`/`safeBgBorder` versus `hairlineSoft`/`hairline` pairing
`_StatusCountChip` uses today. Wrap it in a `Semantics` node whose label spells out the same
counts as words, and set the pill text to `maxLines: 1` with ellipsis overflow.

Delete `_LiveMapOverlay` and `_StatusCountChip` once nothing references them. Keep the public
`ManageSafeZonesButton` and `ViewNotificationsButton` classes exactly as they are, including their
`minimumSize: Size(48, 48)` — an external test constructs and measures the first one.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter analyze --no-fatal-infos 2>&amp;1 | tail -5 &amp;&amp; grep -c "SosArmButton(onArmComplete: _onArmComplete)" lib/features/home/presentation/main_shell.dart &amp;&amp; grep -c "sosControllerProvider.notifier).arm()" lib/features/home/presentation/main_shell.dart &amp;&amp; grep -c "kShellBottomBarHeight" lib/features/home/presentation/main_shell.dart lib/features/location/presentation/live_map_screen.dart &amp;&amp; grep -c "DraggableScrollableSheet" lib/features/location/presentation/live_map_screen.dart</automated>
  </verify>
  <done>
`flutter analyze` reports no issues. `main_shell.dart` still contains exactly one
`SosArmButton(onArmComplete: _onArmComplete)` and one `sosControllerProvider.notifier).arm()`
occurrence. `kShellBottomBarHeight` appears at least twice in `main_shell.dart` (declaration plus
the `SizedBox`) and at least once in `live_map_screen.dart`. `live_map_screen.dart` contains a
`DraggableScrollableSheet` and no longer contains `_LiveMapOverlay`, `_StatusCountChip`, or the
string `Your family, live`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give map pins a presence ring and the self pin an animated pulse ring</name>
  <files>mobile/lib/features/location/presentation/live_map_screen.dart</files>
  <action>
Convert `LiveMemberMarker` from `StatelessWidget` to `StatefulWidget` with
`SingleTickerProviderStateMixin`. Keep the class public and the constructor signature
(`location`, `name`, `isOnline`, `isSelf`, `color`, `onTap`) byte-identical — existing tests
construct it directly and find it by type. Keep the existing `Semantics` label, the existing
`stalenessFor` opacity behavior, the `_hasAvatar` getter, the `_initials()` fallback, and the
`_MarkerNameLabel` / `_MarkerPresenceLabel` / `BatteryIndicator` column children exactly as they
are. The presence text pill stays: the ring is an addition, and removing the text would make
presence color-only.

Presence ring: change the 36x36 avatar `Container`'s border from `AppColors.surface` to
`isOnline ? AppColors.safe : AppColors.bodySecondary`, keeping `width: 3`. Because that border was
also what separated the pin from the basemap, prepend a zero-blur spread shadow to the existing
`boxShadow` list — `BoxShadow(color: AppColors.surface, blurRadius: 0, spreadRadius: 1.5)` — which
paints a crisp white halo just outside the ring at zero layout cost, ahead of the existing soft
drop shadow. Keep the existing soft shadow as the second entry.

Pulse ring: wrap the avatar `Container` in a fixed `SizedBox(width: 56, height: 56)` holding a
`Stack(alignment: Alignment.center)`. Apply that same 56x56 slot to every marker, self or not, so
the pin's anchor point relative to its geographic coordinate stays identical across members —
`VectorMap` centres each marker box on its coordinate, so a per-member height would visibly offset
some pins. The self pin adds one extra `Stack` child behind the avatar: an `IgnorePointer` wrapping
a `RepaintBoundary` wrapping an `AnimatedBuilder` on the pulse controller, driving a
`CustomPaint(size: Size(56, 56))` keyed `ValueKey('self-pulse-ring')`. Add a private
`_SelfPulseRingPainter` taking `progress`, stroking a circle whose radius eases from 20 to 28 and
whose `AppColors.primaryTeal` stroke fades from opacity 0.5 to 0 as progress runs 0 to 1, with a
2px stroke width, and a `shouldRepaint` comparing `progress`. Do not reuse `SosPulseRing` from
`sos_countdown.dart` — that widget is SOS-scoped emergency visual language and must not appear on
a routine surface.

Drive it with an `AnimationController(vsync: this, duration: Duration(milliseconds: 1800))` created
in `initState` but NOT started there. Start and stop it in `didChangeDependencies`: call `repeat()`
only when `widget.isSelf` is true and `MediaQuery.of(context).disableAnimations` is false —
matching `sos_arm_button.dart`'s existing reduce-motion convention — and call `stop()` otherwise.
Guard `repeat()` behind an `isAnimating` check so repeated dependency changes do not restart it.
Dispose the controller in `dispose()`. Because `isSelf` can change across a rebuild, also
re-evaluate the same start/stop decision in `didUpdateWidget`.

Finally, in the marker-building loop in `_LiveMapScreenState.build`, raise the `OverlayMarker`
`height` from `108` to `128` to absorb the avatar slot growing 36 to 56, and update the adjacent
comment to record why. Leave `width: 104` alone.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter analyze --no-fatal-infos 2>&amp;1 | tail -5 &amp;&amp; flutter test test/features/location/live_member_marker_test.dart test/features/location/live_map_screen_test.dart</automated>
  </verify>
  <done>
`flutter analyze` reports no issues. Both existing location widget-test files pass with no
edits yet — the conversion to `StatefulWidget`, the ring recolor, and the marker height bump are
all backward compatible with the current assertions. No `RenderFlex overflowed` exception appears
in the test output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend widget coverage for the new sheet, pill, and pin rings, and run the full gates</name>
  <files>
mobile/test/features/location/live_map_screen_test.dart,
mobile/test/features/location/live_member_marker_test.dart,
mobile/test/features/location/location_permission_gate_test.dart
  </files>
  <action>
In `location_permission_gate_test.dart`, the three `expect(find.text('Your family, live'), findsNothing)`
sentinels now assert against a deleted string, which would silently pass forever. Import
`package:mobile/features/location/presentation/live_map_screen.dart` and replace each with
`expect(find.byType(LiveMapScreen), findsNothing)`. Leave every other assertion in that file alone.

In `live_map_screen_test.dart`, keep all seven existing tests and their fakes unchanged, then add
three. First: with `_PopulatedFamilyController` plus `_PopulatedLocationController` plus a Guardian
profile and the fake platform-view builder, pump a fixed `Duration(milliseconds: 100)` (never
`pumpAndSettle` — the self pin now animates forever) and assert `find.text('1 online · 1 offline')`
finds one widget, since that fixture has one online self and one offline member. Second: from the
same fixture, assert a `DraggableScrollableSheet` is present, read it via `tester.widget` and
assert its `initialChildSize` is `0.2` and `maxChildSize` is `0.5`, and assert one
`ManageSafeZonesButton` and one `ViewNotificationsButton` render inside it. Third: repeat the
second with a `Role.member` profile and assert `ManageSafeZonesButton` finds nothing while
`ViewNotificationsButton` still finds one — pinning the Guardian gate to its new home in the sheet.

In `live_member_marker_test.dart`, keep all seven existing tests unchanged and add three, following
the file's existing `tester.pump()`-only convention and its existing single-marker pump harness.
First: mount an `isOnline: true, isSelf: false` marker, locate the 36x36 avatar `Container` by
walking its `BoxDecoration`, and assert its border's top side color is `AppColors.safe`. Second:
mount the same marker with `isOnline: false` and assert that color is `AppColors.bodySecondary`.
Third: mount an `isSelf: true` marker and assert `find.byKey(const ValueKey('self-pulse-ring'))`
finds one widget, then mount an `isSelf: false` marker and assert it finds nothing.

Then run the full gates below. `flutter analyze` must be clean and every test in the mobile
package must pass, including the SOS, geofencing, and privacy suites that transitively mount
`MainShell` or `LiveMapScreen`.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; flutter analyze 2>&amp;1 | tail -5 &amp;&amp; flutter test</automated>
  </verify>
  <done>
`flutter analyze` prints "No issues found!". `flutter test` reports all tests passed with zero
failures and zero timeouts — in particular `sos_button_press_hold_test.dart`,
`safe_zone_flow_test.dart`, `safe_zone_router_flow_test.dart`, and
`location_permission_gate_test.dart` remain green, proving no repeating animation leaked into a
`pumpAndSettle` path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → SOS arm path | The only safety-critical boundary in reach; this plan adds a new bottom-anchored surface near the SOS disc |
| server → map render | Presence and location values already crossed this boundary upstream; this plan only re-renders them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-J70-01 | Denial of Service | `SosArmButton` reachability | critical | mitigate | The new `DraggableScrollableSheet` is inset by `kShellBottomBarHeight`, so its hit area can never overlap the 72px SOS disc or the nav bar. Task 3's full-suite gate includes `sos_button_press_hold_test.dart`, which drives a real press-and-hold through `MainShell`. |
| T-J70-02 | Denial of Service | Repeating pulse animation | medium | mitigate | The pulse controller starts only for `isSelf` and only when reduce-motion is off, is `RepaintBoundary`-isolated, and is disposed with the state. It never mounts on the empty-state branches that `pumpAndSettle`-based tests rely on. |
| T-J70-03 | Information Disclosure | Family status pill | low | accept | The pill exposes only aggregate online/offline counts already rendered on this screen today by `_StatusCountChip`; no new data crosses a boundary. |
| T-J70-04 | Tampering | Dependencies | low | accept | No package is added, removed, or version-changed, so no package-legitimacy gate applies. |
</threat_model>

<verification>
1. `cd mobile && flutter analyze` → "No issues found!"
2. `cd mobile && flutter test` → all tests pass, no timeouts
3. `grep -c "SosArmButton(onArmComplete: _onArmComplete)" mobile/lib/features/home/presentation/main_shell.dart` → `1`
4. `git diff --stat mobile/lib/features/sos/` → empty (no SOS source file touched)
5. `git diff --stat mobile/lib/features/home/presentation/main_shell.dart` → a 2-3 line change only (constant declaration plus its single use)
</verification>

<success_criteria>
- Home Map top overlay is one slim row: identity pin, "N online · N offline" pill with presence dot, profile action, logout action.
- Manage-safe-zones and Notifications live in a `DraggableScrollableSheet` resting at 20% and opening to 50%, still Guardian-gated for safe zones.
- Member pins ring green when online and grey when offline; the "You" pin pulses.
- Member status rail, low-battery banner, rail-tap recenter, marker-tap detail sheet, and the live-reading header identity pin all behave exactly as before.
- SOS button and bottom nav are functionally untouched and already met spec before this task.
- Analyzer clean, full mobile suite green.
</success_criteria>

<output>
Create `.planning/quick/260814-j70-apply-home-map-screen-redesign-status-pi/260814-j70-SUMMARY.md` when done.
Record explicitly in the summary that the SOS button (72px, deep red, 3-second hold-to-activate
with press ring) and the four-tab bottom nav were already spec-compliant and were not modified,
and that the OpenFreeMap Liberty basemap was accepted as already-muted with no style change.
</output>
</content>
</invoke>
