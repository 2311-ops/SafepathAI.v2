---
phase: quick-260720-1ou
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/features/location/presentation/osm_attribution.dart
  - mobile/test/features/location/osm_attribution_test.dart
  - mobile/lib/features/location/presentation/live_map_screen.dart
  - mobile/lib/features/location/presentation/route_stats_sheet.dart
autonomous: true
requirements:
  - OSM-ATTRIB-MINIMAL
must_haves:
  truths:
    - "The OpenStreetMap credit text stays visible and legible on both the Live Map and the route-history map (never removed, never hidden behind a tap)."
    - "The credit renders as small, muted, low-opacity text — visually minimal versus the previous default chip."
    - "Both maps use the identical attribution treatment via one shared widget."
  artifacts:
    - mobile/lib/features/location/presentation/osm_attribution.dart
    - mobile/test/features/location/osm_attribution_test.dart
  key_links:
    - "Both FlutterMap children lists render OsmAttribution() in place of the inline SimpleAttributionWidget."
    - "OsmAttribution derives its text style from AppTypography.bodySecondary + AppColors tokens (no hardcoded colors/fonts)."
---

<objective>
Restyle the OpenStreetMap attribution on the Live Map and the route-history map to be as visually unobtrusive as possible while staying ODbL-compliant: the "OpenStreetMap contributors" credit must remain visible and legible on screen, just small, muted, and low-opacity instead of the default attribution chip.

Purpose: The default `SimpleAttributionWidget` chip is heavier than the design system warrants; a minimal always-visible credit keeps the maps clean without violating OSM's attribution requirement.

Output: A shared `OsmAttribution` widget (with a Nyquist widget test) reused by both map screens, replacing the two duplicated inline `SimpleAttributionWidget(...)` usages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Design tokens to reuse (do not invent new colors/fonts)
@mobile/lib/core/theme/app_colors.dart
@mobile/lib/core/theme/app_typography.dart

# The two maps that currently duplicate the inline attribution
@mobile/lib/features/location/presentation/live_map_screen.dart
@mobile/lib/features/location/presentation/route_stats_sheet.dart

# Small-widget test convention to match (GoogleFonts runtime-fetch disabled in setUpAll)
@mobile/test/features/location/battery_indicator_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared minimal OsmAttribution widget + Nyquist test</name>
  <files>mobile/lib/features/location/presentation/osm_attribution.dart, mobile/test/features/location/osm_attribution_test.dart</files>
  <action>
Create `osm_attribution.dart` exporting a `const`-constructible `StatelessWidget` named `OsmAttribution`. Its `build` returns flutter_map's `SimpleAttributionWidget` (already used today — no new dependency, no RichAttributionWidget) configured to be minimal but always-visible:
- `source:` a `Text('OpenStreetMap contributors', ...)` styled by reusing the design tokens — base it on `AppTypography.bodySecondary` via `.copyWith(fontSize: 9, height: 1.0, color: AppColors.bodySecondary.withValues(alpha: 0.55))`. This reuses the Manrope family and the muted secondary-text token rather than hardcoding a new color/font.
- `backgroundColor:` `AppColors.surface.withValues(alpha: 0.45)` — a faint translucent chip so the credit stays legible over varied map tiles without adding visual weight.
- Leave alignment at the widget default (bottom-right).
Use `withValues(alpha: ...)` (not the deprecated `withOpacity`) — the pinned Flutter 3.44.x stack supports it. Add a short doc comment stating the credit is kept always-visible on purpose to satisfy OSM's ODbL attribution requirement (it must not be removed or hidden behind a tap). Import only `package:flutter/material.dart`, `package:flutter_map/flutter_map.dart`, and the two theme token files via relative paths (`../../../core/theme/app_colors.dart`, `../../../core/theme/app_typography.dart`).

Create `osm_attribution_test.dart` matching the `battery_indicator_test.dart` convention: `setUpAll` sets `GoogleFonts.config.allowRuntimeFetching = false`; a `wrap` helper mounts the child in `MaterialApp(home: Scaffold(body: Stack(children: [child])))`; one `testWidgets` case pumps `const OsmAttribution()` and asserts `find.text('OpenStreetMap contributors')` findsOneWidget (proves the ODbL credit still renders).
  </action>
  <verify>
    <automated>cd mobile && flutter test test/features/location/osm_attribution_test.dart</automated>
  </verify>
  <done>osm_attribution.dart defines a const OsmAttribution widget that renders the OSM credit as small (9px), muted, low-opacity text from AppTypography/AppColors tokens; the widget test passes, confirming the credit text still renders.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both maps to OsmAttribution</name>
  <files>mobile/lib/features/location/presentation/live_map_screen.dart, mobile/lib/features/location/presentation/route_stats_sheet.dart</files>
  <action>
In `live_map_screen.dart` (the inline attribution is the last child of the `FlutterMap` children list, ~line 184) replace the inline `SimpleAttributionWidget(source: Text('OpenStreetMap contributors'))` with `const OsmAttribution()`. Do the same in `route_stats_sheet.dart` (the equivalent inline attribution is the last child of the route-history map's `FlutterMap` children list, ~line 109). Add the relative import `import 'osm_attribution.dart';` to each file (both files sit in the same `presentation/` directory, so it is a bare-filename relative import). Keep every other map layer (TileLayer, CircleLayer/PolylineLayer, MarkerLayer) untouched and in the same order. Do not change `flutter_map` imports — `FlutterMap`/`TileLayer` still need them.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/location/presentation/live_map_screen.dart lib/features/location/presentation/route_stats_sheet.dart lib/features/location/presentation/osm_attribution.dart && grep -rc 'OsmAttribution(' lib/features/location/presentation/live_map_screen.dart lib/features/location/presentation/route_stats_sheet.dart && flutter test test/features/location/live_map_screen_test.dart test/features/location/route_stats_sheet_test.dart</automated>
  </verify>
  <done>Both map screens render `const OsmAttribution()` as their final FlutterMap child (grep shows one match each), `flutter analyze` reports no issues on the three touched lib files, and the existing live-map and route-stats screen tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This is a client-side presentational restyle of a static credit label; no new input is parsed, no network/storage/auth surface changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Information (compliance) | OSM attribution label | low | mitigate | Keep the "OpenStreetMap contributors" credit always-visible and legible (small/muted, not removed or hidden behind a tap); widget test asserts the text still renders. |
</threat_model>

<verification>
- `cd mobile && flutter test test/features/location/osm_attribution_test.dart` passes (credit text renders).
- `cd mobile && flutter analyze lib/features/location/presentation/` reports no issues.
- `cd mobile && flutter test test/features/location/live_map_screen_test.dart test/features/location/route_stats_sheet_test.dart` still pass.
- Manual smoke (optional): the OSM credit appears small/muted at bottom-right on both the Live Map and the route-history sheet.
</verification>

<success_criteria>
- OSM attribution on both maps is visibly smaller, muted, and low-opacity versus the previous default chip, yet the credit text remains readable and always on screen.
- A single shared `OsmAttribution` widget is the sole source of the treatment (no duplicated inline styling).
- Styling reuses `AppColors` / `AppTypography` tokens — no new hardcoded colors or fonts, no new dependency.
</success_criteria>

<output>
Create `.planning/quick/260720-1ou-restyle-osm-attribution-text-to-be-as-vi/260720-1ou-SUMMARY.md` when done.
</output>
