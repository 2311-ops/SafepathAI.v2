---
phase: quick-260820-3tj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/pubspec.yaml
  - mobile/assets/illustrations/hero-safety.svg
  - mobile/assets/illustrations/family-connect.svg
  - mobile/assets/illustrations/safe-zone-empty.svg
  - mobile/lib/features/auth/presentation/welcome_screen.dart
  - mobile/lib/features/family/presentation/create_circle_screen.dart
  - mobile/lib/features/family/presentation/invite_member_screen.dart
  - mobile/lib/features/geofencing/presentation/safe_zones_screen.dart
autonomous: true
requirements: [QUICK-SVG-ILLUSTRATIONS]

must_haves:
  truths:
    - "flutter_svg is a resolved pubspec dependency and assets/illustrations/ is registered under flutter: assets: so the three bundled SVG files load at runtime without a MissingAssetError."
    - "WelcomeScreen renders hero-safety.svg between the tagline text and the CTA buttons, and the existing Welcome-screen navigation tests (tapping 'Create your circle' / 'I already have an account' without scrolling) still pass."
    - "CreateCircleScreen renders family-connect.svg in place of its 64x64 diversity_3 icon tile; every other element on the screen (heading, subtitle, form field, error banner, button) is unchanged."
    - "InviteMemberScreen's 'Create a circle first' empty state renders family-connect.svg in place of Icons.diversity_3, at the same ~100px scale as CreateCircleScreen; every other branch of the screen (QR/code card, pending list, error recovery) is unchanged."
    - "AcceptInviteScreen is left byte-for-byte unmodified because it has no icon/header-tile spot equivalent to CreateCircleScreen's diversity_3 tile, and forcing one in would require a layout restructure that is explicitly out of scope."
    - "SafeZonesScreen's empty-zones state ('No safe zones yet') renders safe-zone-empty.svg; its loading state (spinner) and error state (Icons.cloud_off_outlined) are pixel-for-pixel unchanged."
    - "flutter analyze is clean across the touched files and the mobile package as a whole; every existing widget test that exercises these five screens still passes with no test file edited."
    - "No other screen, design token, color, or spacing value is touched, and no photo-upload or user-photo functionality is added — the three assets are static bundled decorative SVGs only."
  artifacts:
    - file: mobile/assets/illustrations/hero-safety.svg
      contains: "<svg viewBox=\"0 0 320 260\""
    - file: mobile/assets/illustrations/family-connect.svg
      contains: "<svg viewBox=\"0 0 320 320\""
    - file: mobile/assets/illustrations/safe-zone-empty.svg
      contains: "<svg viewBox=\"0 0 280 280\""
    - file: mobile/pubspec.yaml
      contains: "flutter_svg"
    - file: mobile/lib/features/geofencing/presentation/safe_zones_screen.dart
      contains: "illustration"
  key_links:
    - from: "WelcomeScreen Column (between the tagline Text and the PrimaryButton)"
      to: "assets/illustrations/hero-safety.svg"
      via: "SvgPicture.asset(...) inserted where the second _AdaptiveWelcomeGap(multiplier: 4) currently sits"
      pattern: "hero-safety.svg"
    - from: "CreateCircleScreen's 64x64 Container/Icon(Icons.diversity_3)"
      to: "assets/illustrations/family-connect.svg"
      via: "direct SvgPicture.asset(...) replacement, same position in the Column"
      pattern: "family-connect.svg"
    - from: "InviteMemberScreen 'Create a circle first' SafePathCard's Icon(Icons.diversity_3, size: 48)"
      to: "assets/illustrations/family-connect.svg"
      via: "direct SvgPicture.asset(...) replacement inside the family == null branch only"
      pattern: "family-connect.svg"
    - from: "SafeZonesScreen._body()'s zones.isEmpty branch"
      to: "assets/illustrations/safe-zone-empty.svg"
      via: "a new optional _StateMessage.illustration widget slot, passed only by the empty-zones call site"
      pattern: "safe-zone-empty.svg"
---

<objective>
Wire the three already-approved, already-designed SVG illustrations (`hero-safety`, `family-connect`,
`safe-zone-empty`) into the SafePath AI Flutter app, replacing stock Material icons on the Welcome
screen, the family-circle creation/invite flow, and the Safe Zones empty state.

Purpose: The design for these three illustrations was already brainstormed and approved outside this
task. This plan is pure implementation — add the `flutter_svg` renderer, bundle the three exact SVGs
supplied, and wire each into its already-identified spot with no redesign, no layout rework beyond
making room for the artwork, and no touching of any other screen.

Output: `flutter_svg` resolved in `mobile/pubspec.yaml`, three new files under
`mobile/assets/illustrations/`, and four presentation files (`welcome_screen.dart`,
`create_circle_screen.dart`, `invite_member_screen.dart`, `safe_zones_screen.dart`) rendering the
illustrations in place of their prior stock icons — with `accept_invite_screen.dart` deliberately left
untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@mobile/pubspec.yaml
@mobile/lib/features/auth/presentation/welcome_screen.dart
@mobile/lib/features/family/presentation/create_circle_screen.dart
@mobile/lib/features/family/presentation/invite_member_screen.dart
@mobile/lib/features/family/presentation/accept_invite_screen.dart
@mobile/lib/features/geofencing/presentation/safe_zones_screen.dart
@mobile/lib/core/theme/app_spacing.dart
</context>

<interface_contracts>
Verified by reading the source — reuse verbatim, do not reinvent:

- `flutter_svg` current stable is **2.3.0** (pub.dev, verified 2026-08-20, `flutter.dev`-verified
  publisher). Add as `flutter_svg: ^2.3.0`. API surface used here is just
  `SvgPicture.asset(String assetPath, {double? width, double? height})` — no `SvgTheme`, no network
  loader, no color filters needed for any of the three illustrations (all colors are baked into the
  SVG markup itself).
- `WelcomeScreen` (`StatelessWidget`) currently has, in order inside its `Column`: a logo `Hero`, the
  "SafePath AI" `display`-style headline, the tagline `Text`, then
  `const _AdaptiveWelcomeGap(multiplier: 4)`, then the `PrimaryButton`. `_AdaptiveWelcomeGap` is a
  private widget in the same file (`unit = isLandscape ? AppSpacing.md : AppSpacing.lg`) — do not
  change its implementation, only how many of them surround the new illustration.
- `CreateCircleScreen` (`ConsumerStatefulWidget`) currently renders a `Container(width: 64, height: 64,
  decoration: BoxDecoration(color: AppColors.primaryTeal, borderRadius: BorderRadius.circular(18)),
  child: Icon(Icons.diversity_3, ...))` as the first child of its form `Column`. `AppColors` stays
  imported/used elsewhere in this file (the `_ErrorBanner`), so do not remove that import.
- `InviteMemberScreen` (`ConsumerStatefulWidget`) has multiple `SafePathCard` branches inside its
  `build()`. Only the `family == null` branch ("Create a circle first") has a diversity_3 icon
  (`Icon(Icons.diversity_3, size: 48)`). The sibling `invite == null && error != null` branch uses
  `Icon(Icons.cloud_off, size: 48)` — a different icon for a different (error) meaning; leave it
  untouched.
- `AcceptInviteScreen` (`ConsumerStatefulWidget`) has been read in full during planning: it contains
  only heading `Text`s, an invite-code `SafePathTextField` or `_LinkInviteNotice` banner, and
  Accept/Decline actions. There is no icon, tile, or header-graphic spot anywhere on this screen — do
  not add one.
- `SafeZonesScreen._StateMessage` (private widget, bottom of the file) currently takes
  `required this.icon` (`IconData`) and renders `Icon(icon, size: 48, color: AppColors.primaryTeal)`
  unconditionally. It has exactly two call sites in `_body()`: the `errorMessage != null` branch
  (`icon: Icons.cloud_off_outlined`) and the `zones.isEmpty` branch
  (`icon: Icons.add_location_alt_outlined`). The `isLoading` branch renders a bare
  `CircularProgressIndicator` directly and never touches `_StateMessage` at all.
</interface_contracts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add flutter_svg, bundle the three SVG assets, register them in pubspec</name>
  <files>
    mobile/pubspec.yaml,
    mobile/assets/illustrations/hero-safety.svg,
    mobile/assets/illustrations/family-connect.svg,
    mobile/assets/illustrations/safe-zone-empty.svg
  </files>
  <action>
    In `mobile/pubspec.yaml`, add `flutter_svg: ^2.3.0` to the `dependencies:` block (near the other
    UI-rendering packages such as `qr_flutter`/`cached_network_image` is fine — exact position within
    the block does not matter). Do not touch any existing dependency's version pin.

    Create the new directory `mobile/assets/illustrations/` with exactly three files, each containing
    the exact SVG markup supplied for this task, byte-for-byte (do not reformat, re-indent, or
    "clean up" the markup — these are the approved, designed assets):
    - `hero-safety.svg`
    - `family-connect.svg`
    - `safe-zone-empty.svg`

    In the `flutter:` section of `pubspec.yaml`, add an active `assets:` list (the current file only
    has this commented out as an example) registering the whole directory:
    ```
    assets:
      - assets/illustrations/
    ```
    Place it directly under `uses-material-design: true`, above the existing commented-out example
    block (leave those comments as-is).

    Run `flutter pub get` inside `mobile/` so `flutter_svg` resolves into `pubspec.lock`.
  </action>
  <verify>
    <automated>cd mobile && flutter pub get && test -f assets/illustrations/hero-safety.svg && test -f assets/illustrations/family-connect.svg && test -f assets/illustrations/safe-zone-empty.svg</automated>
  </verify>
  <done>
    `mobile/pubspec.yaml` lists `flutter_svg` as a dependency and `assets/illustrations/` under
    `flutter: assets:`; all three SVG files exist under `mobile/assets/illustrations/` with exactly
    the specified markup; `flutter pub get` resolves with no errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire hero-safety.svg into the Welcome screen</name>
  <files>mobile/lib/features/auth/presentation/welcome_screen.dart</files>
  <action>
    Add `import 'package:flutter_svg/flutter_svg.dart';` to `welcome_screen.dart`.

    Replace the single `const _AdaptiveWelcomeGap(multiplier: 4)` that currently sits between the
    tagline `Text` ("Family safety, privacy, and calm coordination.") and the `PrimaryButton` with
    three widgets in sequence: `const _AdaptiveWelcomeGap(multiplier: 1)`, then
    `SvgPicture.asset('assets/illustrations/hero-safety.svg', width: 160)`, then another
    `const _AdaptiveWelcomeGap(multiplier: 1)`. Do not add a `semanticsLabel` — this illustration is
    purely decorative and should stay out of the accessibility tree, matching `SvgPicture`'s default
    behavior when no label is supplied.

    Leave every other line in this file untouched: the outer gradient `Container`, the `SafeArea`,
    the `TweenAnimationBuilder`, the `LayoutBuilder`/`SingleChildScrollView`/`ConstrainedBox` scroll
    scaffolding, the logo `Hero`, the headline/tagline `Text`s, the `PrimaryButton`,
    `GoogleSignInButton`, the "I already have an account" `TextButton`, and the private
    `_AdaptiveWelcomeGap` class definition itself.

    This screen is exercised by `test/core/router/auth_flow_navigation_test.dart`'s "Welcome screen
    entry points" group, which taps `'Create your circle'` and `'I already have an account'` directly
    (no `ensureVisible`) against the default flutter_test viewport — so those two controls must remain
    hit-testable without scrolling. Run the verify command below after making the change; if either
    tap-based test fails because a control is pushed out of the default viewport, reduce the
    `SvgPicture` `width` and/or lower one or both `_AdaptiveWelcomeGap` multipliers further (down to a
    minimum of `multiplier: 0`, i.e. no extra gap) until both tests pass again. Do not modify the test
    file (no adding `ensureVisible`, no relaxing its assertions) to work around this — the fix belongs
    entirely in the illustration's sizing on this screen.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/auth/presentation/welcome_screen.dart && flutter test test/core/router/auth_flow_navigation_test.dart</automated>
  </verify>
  <done>
    `WelcomeScreen` renders the hero-safety SVG between the tagline and the CTA buttons; `flutter
    analyze` is clean for this file; `auth_flow_navigation_test.dart`'s "Welcome screen entry points"
    group (and every other test in that file) passes unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire family-connect.svg into CreateCircleScreen and InviteMemberScreen</name>
  <files>
    mobile/lib/features/family/presentation/create_circle_screen.dart,
    mobile/lib/features/family/presentation/invite_member_screen.dart
  </files>
  <action>
    In `create_circle_screen.dart`, add `import 'package:flutter_svg/flutter_svg.dart';` and replace
    the whole `Container(width: 64, height: 64, decoration: ..., child: const Icon(Icons.diversity_3,
    ...))` block with `SvgPicture.asset('assets/illustrations/family-connect.svg', width: 100, height:
    100)`. Keep the `AppColors` import (still used by `_ErrorBanner`). Leave the heading, subtitle,
    `SafePathTextField`, error banner, and `PrimaryButton` untouched.

    In `invite_member_screen.dart`, add the same `flutter_svg` import. Inside the `family == null`
    branch of `build()` (the `SafePathCard` headed "Create a circle first"), replace `const
    Icon(Icons.diversity_3, size: 48)` with `SvgPicture.asset('assets/illustrations/family-connect.svg',
    width: 100, height: 100)` — the same 100px scale as `CreateCircleScreen`, for visual consistency
    between the two "no circle yet" moments. Do not touch the sibling `Icon(Icons.cloud_off, size: 48)`
    in the `invite == null && error != null` branch, and do not touch the QR/code card, the pending-
    invites list, or any other part of this screen.

    Read `mobile/lib/features/family/presentation/accept_invite_screen.dart` to confirm (this was
    already verified during planning) that it has no icon/header-tile spot comparable to
    `CreateCircleScreen`'s diversity_3 tile — it renders only heading text, an invite-code field or
    link-invite banner, and Accept/Decline actions. Make **no changes** to this file; do not
    restructure its layout to force an illustration in.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/family/presentation/create_circle_screen.dart lib/features/family/presentation/invite_member_screen.dart && flutter test test/features/family/invite_member_screen_test.dart test/features/home/landing_role_flow_test.dart</automated>
  </verify>
  <done>
    `CreateCircleScreen` and `InviteMemberScreen`'s "Create a circle first" state both render
    `family-connect.svg` at a consistent ~100px scale in place of their `diversity_3` icon/tile;
    `AcceptInviteScreen` is byte-for-byte unchanged; `flutter analyze` is clean; both listed test
    files pass unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire safe-zone-empty.svg into SafeZonesScreen's empty state only</name>
  <files>mobile/lib/features/geofencing/presentation/safe_zones_screen.dart</files>
  <action>
    Add `import 'package:flutter_svg/flutter_svg.dart';` to `safe_zones_screen.dart`.

    Widen the private `_StateMessage` widget to support an optional illustration in place of an
    `IconData`, since it is the shared widget behind both the error state and the empty-zones state
    and only the latter should change:

    - Change `final IconData icon;` to `final IconData? icon;` and drop `required` from the
      constructor's `this.icon` parameter (default it to `null`).
    - Add a new field `final Widget? illustration;` and a matching optional constructor parameter.
    - Add an `assert(icon != null || illustration != null)` to the constructor so a future call site
      can never render neither.
    - In `build()`, replace the unconditional `Icon(icon, size: 48, color: AppColors.primaryTeal)`
      line with `illustration ?? Icon(icon!, size: 48, color: AppColors.primaryTeal)` as the first
      child of the `Column`. Everything else in `_StateMessage.build()` (padding, title/body `Text`s,
      the optional `action`) stays exactly as it is.

    Then, in `_body()`, update only the `zones.isEmpty` call site: remove its
    `icon: Icons.add_location_alt_outlined` argument and add
    `illustration: SvgPicture.asset('assets/illustrations/safe-zone-empty.svg', width: 96, height:
    96)` in its place. Leave the `errorMessage != null` call site's `icon: Icons.cloud_off_outlined`
    argument completely unchanged (do not pass `illustration` there), and leave the `isLoading`
    branch (a bare `CircularProgressIndicator`, no `_StateMessage` involved) untouched.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze lib/features/geofencing/presentation/safe_zones_screen.dart && flutter test test/features/geofencing/safe_zone_flow_test.dart test/features/geofencing/safe_zone_router_flow_test.dart</automated>
  </verify>
  <done>
    `SafeZonesScreen`'s "No safe zones yet" state renders `safe-zone-empty.svg` via the new
    `_StateMessage.illustration` slot; its error state still renders `Icons.cloud_off_outlined`
    through the unchanged `icon` path; `flutter analyze` is clean; both listed test files pass
    unmodified.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Full-package verification gate</name>
  <files>mobile/</files>
  <action>
    Run `flutter analyze` across the whole `mobile/` package and confirm it reports zero issues.

    Run every test file touched by this plan's verification steps together in one invocation:
    `test/core/router/auth_flow_navigation_test.dart`, `test/features/family/invite_member_screen_test.dart`,
    `test/features/home/landing_role_flow_test.dart`, `test/features/geofencing/safe_zone_flow_test.dart`,
    and `test/features/geofencing/safe_zone_router_flow_test.dart`. All must pass.

    Note for context: the mobile suite has one pre-existing, unrelated failure in
    `test/features/location/live_member_marker_test.dart` (documented in
    `.planning/debug/add-zone-not-working.md`, confirmed present before this plan's changes, about
    battery-percent rendering on live map markers — nothing to do with illustrations). If that
    specific test fails and nothing else newly regresses, this is not a regression introduced by this
    task; do not attempt to fix it, it is out of scope for this plan. Do not skip or delete that test
    to make the count look cleaner — just do not treat it as a blocker for this plan's own gate.

    Finally, confirm the change set is contained: run `git status --short` and `git diff --name-only`
    and verify the only files this plan added or modified are `mobile/pubspec.yaml`,
    `mobile/pubspec.lock`, the three new files under `mobile/assets/illustrations/`, and the four
    presentation files listed in this plan's `files_modified` frontmatter. No SOS, location-tracking,
    geofence-registration, router-table, backend, color-token, or spacing-token file may appear in
    that diff — if one does, scope drifted and must be reverted.
  </action>
  <verify>
    <automated>cd mobile && flutter analyze && flutter test test/core/router/auth_flow_navigation_test.dart test/features/family/invite_member_screen_test.dart test/features/home/landing_role_flow_test.dart test/features/geofencing/safe_zone_flow_test.dart test/features/geofencing/safe_zone_router_flow_test.dart</automated>
  </verify>
  <done>
    `flutter analyze` reports zero issues across the mobile package; all five targeted test files
    pass; `git diff --name-only` (plus untracked new files) lists exactly the files declared in this
    plan's `files_modified` frontmatter and nothing else.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled asset -> `flutter_svg` renderer | Three static, developer-authored SVG files parsed at runtime by a new rendering dependency; no network fetch, no user-supplied SVG. |
| pub.dev dependency -> app binary | `flutter_svg` becomes a new compiled dependency of the shipped mobile app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3TJ-01 | Tampering | `assets/illustrations/*.svg` content | low | accept | Files are static and developer-authored (copied verbatim from the approved design in this plan), bundled at build time — never fetched at runtime, never user-suppliable, and inline SVG carries no `<script>`/executable content. |
| T-3TJ-02 | Denial of Service | `SvgPicture.asset` parse/render cost | low | accept | Each file is a few KB of simple shape markup (circles, paths, a handful of gradients-free fills) rendered on three non-critical screens (Welcome, family-circle creation/invite, Safe Zones empty state); none of these screens sit on the SOS pipeline, location tracking, or geofence-registration hot paths, so a slow parse — which will not happen at this size — could not delay any safety-critical flow. |
| T-3TJ-03 | Information Disclosure | Illustration content | low | accept | All three illustrations are generic, non-personalized iconography (silhouettes, shapes, a shield motif) with no user, location, or family data embedded or referenced. |
| T-3TJ-SC | Tampering | pub.dev `flutter_svg` install | low | accept | This ecosystem's package manager is `pub` (Dart/Flutter), which the project's mandated npm/pip/cargo Package Legitimacy Gate does not cover. `flutter_svg` is nonetheless a long-established, `flutter.dev`-verified-publisher package, confirmed at current stable `2.3.0` on pub.dev (checked 2026-08-20), pure-Dart rendering logic with no native platform code and no network access at runtime. |
</threat_model>

<verification>
1. `cd mobile && flutter pub get` — `flutter_svg` resolves cleanly into `pubspec.lock`.
2. `cd mobile && flutter analyze` — zero issues across the whole package.
3. `cd mobile && flutter test test/core/router/auth_flow_navigation_test.dart
   test/features/family/invite_member_screen_test.dart test/features/home/landing_role_flow_test.dart
   test/features/geofencing/safe_zone_flow_test.dart test/features/geofencing/safe_zone_router_flow_test.dart`
   — all pass (the one pre-existing, unrelated `live_member_marker_test.dart` failure documented in
   `.planning/debug/add-zone-not-working.md` is not part of this gate).
4. Read each of the four touched presentation files: `welcome_screen.dart`, `create_circle_screen.dart`,
   and `invite_member_screen.dart` each contain an `SvgPicture.asset('assets/illustrations/...')` call
   at the identified spot; `safe_zones_screen.dart`'s `_StateMessage` has the new `illustration` slot,
   used only by the `zones.isEmpty` call site.
5. Read `accept_invite_screen.dart` — unchanged from its pre-plan state.
6. `git diff --name-only` (plus untracked new files) — matches this plan's `files_modified`
   frontmatter exactly.
</verification>

<success_criteria>
- `flutter_svg` is a resolved dependency and the three approved SVGs are bundled under
  `mobile/assets/illustrations/`.
- WelcomeScreen, CreateCircleScreen, InviteMemberScreen (empty state), and SafeZonesScreen (empty
  state) each render their designated illustration in place of a prior stock Material icon.
- AcceptInviteScreen is deliberately left unmodified, with the reason recorded in this plan and in
  the eventual SUMMARY.md.
- No color, token, layout (beyond making room for the artwork), or unrelated-screen change is
  introduced; no photo-upload/user-photo functionality is added.
- `flutter analyze` is clean and every existing test that exercises these screens still passes
  without any test file being edited.
</success_criteria>

<output>
Create `.planning/quick/260820-3tj-wire-the-3-approved-svg-illustrations-he/260820-3tj-SUMMARY.md` when done
</output>