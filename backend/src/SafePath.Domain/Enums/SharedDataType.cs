namespace SafePath.Domain.Enums;

/// <summary>
/// Privacy-controlled data categories a family member can share independently per recipient.
/// </summary>
public enum SharedDataType
{
    LiveLocation,
    History,
    Wellness,
}
