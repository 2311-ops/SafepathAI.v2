{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Supabase": {
    "Url": "https://YOUR-PROJECT-REF.supabase.co",
    "Audience": "authenticated",
    "_serviceRoleKeyComment": "ServiceRoleKey is injected via backend/.env (Supabase__ServiceRoleKey) or host environment variables. Never commit a real service-role or secret key.",
    "ServiceRoleKey": "",
    "AvatarBucket": "avatar"
  },
  "ConnectionStrings": {
    "_comment": "DefaultConnection is injected via backend/.env (ConnectionStrings__DefaultConnection) or host environment variables.",
    "DefaultConnection": ""
  }
}
